# 多 Agent 状态管理与上下文同步

> 目标：专门回答一个生产级多 Agent 系统里最容易被低估的问题：多个 Agent 如何共享任务状态、同步上下文、交接中间结果、处理冲突、隔离私有记忆，并在 A2A 这类互操作协议下保持可审计和可恢复。

---

## 一、先给结论

多 Agent 状态管理的核心不是“让所有 Agent 共享所有上下文”，而是：

1. **把状态分层**：任务状态、私有上下文、共享工作记忆、工件、事件日志、长期记忆、权限上下文不能混在一起。
2. **只同步必要信息**：跨 Agent 同步的是任务目标、结构化事实、约束、工件引用、未决问题、责任边界，而不是完整 prompt、chain-of-thought、内部 memory。
3. **把 message 当通信，把 artifact 当证据，把 state patch 当事实更新，把 trace 当审计**。
4. **跨组织协作用 A2A 的 Task / Message / Artifact；同一应用内部编排用 LangGraph 这类显式状态图更自然**。
5. **冲突必须被设计出来**：谁能写哪些字段、并发写如何合并、旧上下文如何失效、错误记忆如何撤销，都要有机制。

一句话：

> 多 Agent 系统真正共享的不是“聊天记录”，而是一个有 schema、有版本、有来源、有权限、有生命周期的任务状态空间。

---

## 二、为什么多 Agent 状态管理很难

单 Agent 系统的上下文问题通常是“窗口不够、记忆不准、工具结果太长”。多 Agent 系统会额外引入下面这些问题：

| 问题 | 典型表现 | 后果 |
|------|----------|------|
| 状态碎片化 | 每个 Agent 只知道自己那段对话 | 重复工作、结论互相矛盾 |
| 上下文漂移 | Agent B 看到的是 Agent A 的旧摘要 | 基于过时事实继续推理 |
| 责任不清 | 多个 Agent 都在改同一个字段 | 最终状态不可解释 |
| Token 爆炸 | 把全量历史广播给所有 Agent | 延迟、成本、幻觉同时上升 |
| 私密泄露 | 把某个 Agent 的内部 prompt、工具凭证、用户隐私传给其他 Agent | 安全与合规事故 |
| 记忆污染 | 某个 Agent 写入错误事实，后续 Agent 全部继承 | 错误被系统性放大 |
| 部分失败 | 一个子 Agent 超时或重试，另一个已基于旧结果继续执行 | 结果不一致，恢复困难 |
| 审计困难 | 只保留自然语言聊天记录 | 无法回答“谁在什么时候依据什么改了什么” |

很多 demo 里多 Agent 看起来很顺，是因为任务短、Agent 少、没有并发、没有权限差异、没有长任务恢复。进入生产环境后，状态管理会从“框架细节”变成“系统主干”。

---

## 三、核心概念：不要把这些东西混为一谈

### 3.1 状态、上下文、记忆、工件、轨迹的区别

| 概念 | 定义 | 是否应跨 Agent 同步 | 示例 |
|------|------|---------------------|------|
| State | 当前任务可机器读写的事实快照 | 是，但要按字段授权 | `requirements.status = approved` |
| Context | 某个 Agent 本轮推理需要注入模型窗口的信息 | 只同步子集 | 任务摘要、相关文件、约束 |
| Memory | 跨任务或跨会话复用的长期经验/事实 | 谨慎同步，必须有来源与作用域 | “客户 A 偏好邮件审批” |
| Message | Agent 之间的通信事件 | 是，通常 append-only | “请补充法务风险评估” |
| Artifact | 可引用、可复查的产物 | 是，优先同步引用而非全文 | 报告、表格、代码 diff、审批单 |
| Trace | 执行过程记录 | 不用于推理全量同步，但用于审计和调试 | LLM 调用、工具调用、handoff、guardrail |
| Policy Context | 身份、权限、合规边界 | 必须传递，但不能被模型随意改写 | tenant、role、tool scope、approval level |

关键点：

- **State 是系统真相，不等于聊天历史**。
- **Context 是喂给模型的临时视图，不等于系统完整状态**。
- **Memory 是可召回的历史知识，不等于当前任务状态**。
- **Artifact 是证据和产物，不等于消息本身**。
- **Trace 是观察和审计，不应该原样塞回模型上下文**。

### 3.2 推荐的七层状态模型

生产级多 Agent 系统可以按七层拆分：

| 层级 | 作用 | 典型存储 | 读写者 |
|------|------|----------|--------|
| Run / Task State | 当前任务的结构化状态 | SQL / KV / LangGraph checkpoint / A2A Task store | Orchestrator、授权 Agent |
| Agent Local State | 单个 Agent 的私有中间状态 | Agent runtime / session store | 该 Agent 自己 |
| Shared Working Memory | 多 Agent 共享的短期工作台 | Redis / Postgres / document store / blackboard | 同一任务内 Agent |
| Event Log | 所有状态变化和通信事件 | append-only log / trace system | 系统写，多方读 |
| Artifact Store | 大对象、报告、文件、代码、证据 | object storage / git / blob store | Producer 写，Consumer 读 |
| Long-term Memory | 跨任务经验和用户偏好 | vector DB / graph / memory service | 受权限控制的 Agent |
| Policy / Identity Context | 身份、权限、租户、合规策略 | IAM / policy engine / token claims | 系统和网关 |

可以把它理解成：

```text
User Goal
   |
   v
Orchestrator / Supervisor
   |
   +-- Run / Task State        <- 当前任务的结构化真相
   +-- Event Log               <- 谁在何时做了什么
   +-- Artifact Store          <- 产物和证据
   +-- Shared Working Memory   <- 短期共享事实
   +-- Long-term Memory        <- 可召回历史
   +-- Policy Context          <- 身份、权限、合规
   |
   +-- Agent A Local Context   <- 私有 prompt / scratchpad / tool state
   +-- Agent B Local Context
   +-- Agent C Local Context
```

---

## 四、A2A 视角：同步的是 Task，不是内部大脑

### 4.1 A2A 的状态边界

A2A 的核心设计是让独立、可能互不信任、内部实现不透明的 Agent 系统协作。它暴露的是互操作边界，不是内部运行时。

A2A 里可以跨 Agent 交换的主要对象：

| A2A 对象 | 状态管理含义 |
|----------|--------------|
| Agent Card | 能力、认证、安全方案、支持的输入输出模式 |
| Task | 长任务工作单元，包含生命周期和状态 |
| Message | 用户与 Agent、Agent 与 Agent 之间的通信内容 |
| Part | Message 或 Artifact 的内容片段，如 text、file、data |
| Artifact | Agent 产出的结果，可以被后续 Agent 引用 |
| Task Status | submitted、working、input-required、completed、failed 等状态 |
| Streaming / Push | 任务进度和状态变化的同步方式 |

A2A 不应该做的事：

- 不共享对方 Agent 的 system prompt。
- 不复制对方内部 memory。
- 不要求对方暴露工具链和推理轨迹。
- 不把 chain-of-thought 当协议对象传递。
- 不把跨组织协作降级成“把我的全部上下文发给你”。

所以 A2A 下的正确心智是：

> 远程 Agent 是一个带能力声明、任务生命周期、消息接口和产物接口的黑盒服务；状态同步发生在 Task 合同层，而不是内部上下文层。

### 4.2 A2A Task 如何承载上下文同步

一个跨 Agent 的任务可以把同步信息拆成三类：

| 同步内容 | 建议承载方式 | 示例 |
|----------|--------------|------|
| 当前目标和约束 | Message 的 text/data part | “请在预算 5000 内制定行程” |
| 结构化任务事实 | DataPart / metadata / application state | `budget=5000, city=Tokyo, date=2026-07-10` |
| 产物和证据 | Artifact / file part / artifact reference | `flight_options_v2.csv` |

示例：调研 Agent 把结果交给写作 Agent，A2A message 不要传全部搜索过程，而应传结构化摘要和工件引用。

```json
{
  "role": "agent",
  "parts": [
    {
      "kind": "text",
      "text": "调研已完成。请基于下面的结构化结论生成面向管理层的报告。"
    },
    {
      "kind": "data",
      "data": {
        "task_context": {
          "goal": "生成 8 页以内的市场进入分析",
          "constraints": ["不使用未验证数据", "保留风险假设"],
          "facts": [
            {
              "id": "fact_001",
              "claim": "目标市场增长主要来自企业自动化预算",
              "confidence": 0.78,
              "evidence_refs": ["artifact:research_notes_v3"]
            }
          ],
          "open_questions": ["竞争对手价格仍需销售侧确认"]
        }
      }
    }
  ]
}
```

### 4.3 `task.id` 与 `context.id` 的工程含义

在长任务和多轮交互中，至少要区分：

- **task id**：一个具体工作单元的身份，例如“为客户 X 生成供应商尽调报告”。
- **context/session id**：一组相关任务或一段协作会话的上下文身份，例如“客户 X 的采购流程”。
- **trace id**：一次具体执行链路的可观测身份，例如“本次从 HR Agent 调 IT Agent 再调 Security Agent 的链路”。
- **artifact id**：一个可复查产物的身份，例如“账号开通审批单 v2”。

不要只用一个 `conversation_id` 承包所有语义。生产系统里至少需要：

```text
tenant_id
user_id / delegator_id
context_id
task_id
trace_id
artifact_id
state_version
```

这几个 id 的职责不同，混用会导致恢复、审计、权限和缓存都很难做。

### 4.4 A2A 下的上下文同步模式

| 模式 | 适用场景 | 优点 | 风险 |
|------|----------|------|------|
| Message Summary | 简单交接 | 实现容易、token 少 | 摘要丢信息 |
| Structured Handoff | 生产交接 | 可验证、可测试 | 需要 schema |
| Artifact Reference | 大文件/证据交接 | 省 token、可复查 | 需要权限和版本 |
| Task Continuation | 多轮任务 | 状态由被调 Agent 保持 | 调用方只能看到公开状态 |
| Event Stream | 长任务进度同步 | 实时性好 | 断线恢复要设计 |
| Push Notification | 异步任务 | 适合长耗时任务 | 回调安全与幂等复杂 |

推荐组合：

```text
短任务：Message Summary + final Artifact
长任务：Task Continuation + Streaming/Push + Artifact Reference
跨组织：Structured Handoff + explicit auth + minimal disclosure
高风险：Structured Handoff + human approval + immutable event log
```

---

## 五、状态同步的八种常见模式

### 5.1 全量上下文广播

做法：每个 Agent 都拿到完整对话历史、完整工具结果、完整中间过程。

优点：

- 最容易实现。
- demo 阶段效果直观。

问题：

- 成本和延迟增长很快。
- 所有 Agent 都暴露给所有隐私和注入内容。
- 角色边界失效，每个 Agent 都会尝试重新解释全局任务。
- 历史越长，模型越容易忽略中间关键信息。

生产建议：只用于很短、低风险、同信任域内的实验任务。

### 5.2 摘要交接

做法：上游 Agent 生成一段自然语言 summary，传给下游 Agent。

适合：

- 任务简单。
- 下游只需要大方向。
- 没有严格审计要求。

缺点：

- 摘要可能遗漏关键约束。
- 下游难以区分事实、假设、建议。
- 冲突时无法追溯证据。

改进方式：

```text
摘要 = 结论 + 证据引用 + 未决问题 + 置信度 + 版本
```

### 5.3 结构化 Handoff

做法：定义交接 schema，把任务目标、状态摘要、约束、证据、未决问题、下一步输出要求都显式传递。

推荐 schema：

```json
{
  "handoff_id": "handoff_20260607_001",
  "from_agent": "research_agent",
  "to_agent": "writer_agent",
  "task_id": "task_market_entry_001",
  "state_version": 12,
  "goal": "生成管理层报告",
  "current_state_summary": "调研完成，数据可信度中等，价格信息缺口仍存在。",
  "constraints": [
    "不得编造未验证数据",
    "所有关键结论必须引用 artifact"
  ],
  "relevant_artifacts": [
    {
      "artifact_id": "research_notes_v3",
      "type": "markdown",
      "version": 3,
      "access_scope": "task"
    }
  ],
  "open_questions": [
    "竞品 B 的企业版价格缺少公开来源"
  ],
  "forbidden_actions": [
    "不要直接联系客户",
    "不要修改预算字段"
  ],
  "expected_output": {
    "type": "executive_report",
    "max_pages": 8,
    "must_include": ["market_size", "risks", "assumptions"]
  },
  "return_contract": {
    "write_fields": ["draft_report", "risk_register"],
    "emit_artifacts": ["report.md"],
    "requires_review": true
  }
}
```

这是生产中最推荐的交接方式。

### 5.4 共享状态 Store

做法：所有 Agent 读写同一个结构化 state。

示例：

```json
{
  "task": {
    "id": "onboarding_001",
    "status": "working",
    "owner": "hr_supervisor"
  },
  "employee": {
    "name": "Alice",
    "department": "Finance",
    "start_date": "2026-07-01"
  },
  "subtasks": {
    "it_account": {"status": "completed", "artifact": "it_ticket_123"},
    "security_review": {"status": "input_required"},
    "payroll": {"status": "pending"}
  },
  "risks": [
    {
      "id": "risk_001",
      "source": "security_agent",
      "description": "Finance role requires elevated approval",
      "severity": "medium"
    }
  ]
}
```

优点：

- 当前状态清晰。
- 容易恢复。
- 容易做权限和审计。

难点：

- 必须定义 schema。
- 并发写需要 reducer 或事务。
- 需要状态迁移和版本管理。

### 5.5 Blackboard

做法：多个 Agent 向一个共享黑板写入发现、假设、计划、待办，其他 Agent 订阅或拉取。

适合：

- 研究型任务。
- 诊断型任务。
- 多角色共同补充事实。

黑板不要只是一段 markdown，最好分区：

```text
blackboard/
  facts/
  assumptions/
  questions/
  decisions/
  risks/
  artifacts/
  rejected_options/
```

每条记录至少包含：

```json
{
  "id": "fact_123",
  "type": "fact",
  "content": "供应商 A 支持 SOC2 报告下载",
  "source_agent": "vendor_research_agent",
  "evidence_refs": ["artifact:soc2_link_check"],
  "confidence": 0.92,
  "created_at": "2026-06-07T10:00:00Z",
  "valid_until": "2026-07-07T10:00:00Z"
}
```

### 5.6 Artifact-first 同步

做法：Agent 之间少传长文本，多传产物引用。

适合：

- 代码 Agent。
- Research Agent。
- 数据分析 Agent。
- 法务/财务/合规类 Agent。

设计原则：

- 大文件不塞进 prompt，只传 artifact id、摘要、权限、版本。
- 关键结论必须能追溯到 artifact。
- artifact 要不可变或版本化。
- 下游 Agent 使用前应重新读取需要的片段，而不是信任上游摘要。

这是解决 token 爆炸和审计问题的关键。

### 5.7 Event-sourced Context

做法：不直接覆盖状态，而是记录事件，再由 reducer 计算当前状态。

事件示例：

```json
{
  "event_id": "evt_001",
  "task_id": "contract_review_001",
  "type": "state.patch.proposed",
  "writer_agent": "legal_agent",
  "base_version": 17,
  "patch": [
    {"op": "add", "path": "/risks/-", "value": {"severity": "high", "text": "赔偿条款缺少上限"}}
  ],
  "evidence_refs": ["artifact:contract_clause_12"],
  "confidence": 0.86
}
```

优点：

- 可审计。
- 可回放。
- 可做时间旅行和错误撤销。
- 适合异步和长任务。

缺点：

- 实现复杂。
- 需要处理事件重放、schema 演进、幂等。

### 5.8 Retrieval-backed Shared Memory

做法：把共享知识放入可检索记忆或知识库，Agent 按需召回。

适合：

- 多轮客户服务。
- 企业知识工作流。
- 需要跨任务复用经验的场景。

关键要求：

- 记忆必须有 scope：user、tenant、project、task、agent。
- 记忆必须有 provenance：来源、写入者、时间、证据。
- 记忆必须能撤销或过期。
- 高风险事实不能只靠向量相似度召回，必须回源验证。

---

## 六、状态更新合同：Agent 不能随便“说了算”

### 6.1 State Patch 比自然语言结论更可靠

生产系统里，下游状态更新最好使用 patch，而不是让模型写一段“我已经完成了”。

推荐字段：

```json
{
  "patch_id": "patch_001",
  "task_id": "task_001",
  "base_state_version": 14,
  "writer_agent": "security_agent",
  "visibility": "task",
  "confidence": 0.84,
  "ttl": "30d",
  "evidence_refs": ["artifact:scan_report_002"],
  "operations": [
    {
      "op": "replace",
      "path": "/subtasks/security_review/status",
      "value": "completed"
    },
    {
      "op": "add",
      "path": "/risks/-",
      "value": {
        "id": "risk_007",
        "severity": "low",
        "description": "VPN group approval delayed by one day"
      }
    }
  ]
}
```

这样系统可以检查：

- `base_state_version` 是否过旧。
- `writer_agent` 是否有权限写这些字段。
- `evidence_refs` 是否存在。
- patch 是否符合 schema。
- 是否需要 human approval。

### 6.2 字段级所有权

不要让所有 Agent 都能改所有字段。建议给 state schema 增加所有权规则：

| 字段 | 可写 Agent | 审批要求 | 冲突策略 |
|------|------------|----------|----------|
| `/requirements` | product_agent、user_proxy | 用户确认 | 人工优先 |
| `/risk_register` | security_agent、legal_agent | 高风险需审批 | append + verifier |
| `/it_account/status` | it_agent | 无 | last event wins |
| `/budget` | finance_agent | 用户或经理审批 | 禁止自动覆盖 |
| `/final_answer` | supervisor | 需要完整性检查 | supervisor arbitration |

### 6.3 可见性级别

每条状态或记忆都应该有可见性：

| 可见性 | 含义 | 示例 |
|--------|------|------|
| private | 只给单个 Agent | 内部 scratchpad、临时分析 |
| team | 同一多 Agent 团队可见 | 项目事实、任务计划 |
| task | 当前 A2A Task 可见 | 子任务状态、工件引用 |
| tenant | 同一租户内可见 | 企业偏好、组织规则 |
| public | 可以跨组织或公开 | Agent Card、公开能力 |

默认策略应该是：

> private by default, explicit share by contract.

---

## 七、冲突解决：多 Agent 一定会写出矛盾状态

### 7.1 常见冲突类型

| 冲突 | 示例 | 解决思路 |
|------|------|----------|
| 写写冲突 | 两个 Agent 同时更新 `status` | version check + retry |
| 事实冲突 | Research Agent 说价格 100，Sales Agent 说价格 120 | evidence + verifier |
| 计划冲突 | Planner A 选择路线 1，Planner B 选择路线 2 | supervisor arbitration |
| 权限冲突 | Agent 想修改预算但无权限 | policy deny |
| 时间冲突 | Agent 基于旧状态执行 | stale read detection |
| 记忆冲突 | 长期记忆与当前任务事实不一致 | task state 优先 |

### 7.2 六种冲突策略

| 策略 | 适用 | 不适用 |
|------|------|--------|
| Last Write Wins | 低风险状态，如进度心跳 | 预算、合规、最终结论 |
| Reducer / Merge Function | 列表、消息、风险项、候选方案 | 单值强一致字段 |
| Source Priority | 官方 API 高于网页，高权限 Agent 高于普通 Agent | 同源数据冲突 |
| Verifier Agent | 事实核验、代码审查、风控评估 | 极低延迟链路 |
| Supervisor Arbitration | 计划选择、责任分配、最终答案 | 高频小字段更新 |
| Human Review | 高风险审批、不可逆操作 | 纯自动低风险任务 |

### 7.3 Reducer 的设计思路

Reducer 是多 Agent 状态管理里非常重要的概念。它规定“同一个字段收到多个更新时如何合并”。

示例：

```python
def merge_risks(old: list[dict], new: list[dict]) -> list[dict]:
    by_id = {item["id"]: item for item in old}
    for item in new:
        existing = by_id.get(item["id"])
        if existing is None:
            by_id[item["id"]] = item
        else:
            by_id[item["id"]] = {
                **existing,
                **item,
                "evidence_refs": sorted(set(existing.get("evidence_refs", []) + item.get("evidence_refs", []))),
                "confidence": max(existing.get("confidence", 0), item.get("confidence", 0)),
            }
    return list(by_id.values())
```

不是所有字段都能 reducer：

- `messages` 可以 append。
- `risks` 可以按 id merge。
- `artifacts` 可以版本化追加。
- `budget` 不能随便 merge。
- `approval_status` 不能由低权限 Agent 覆盖。

---

## 八、Handoff 设计：交接不是转发聊天记录

### 8.1 一个好的 handoff 应该包含什么

| 字段 | 作用 |
|------|------|
| goal | 下游 Agent 要完成什么 |
| current_state_summary | 当前状态摘要 |
| constraints | 不能违反的约束 |
| relevant_artifacts | 需要读取的工件 |
| open_questions | 未解决问题 |
| decisions_made | 已确定且不要重复讨论的决定 |
| forbidden_actions | 下游不能做什么 |
| write_scope | 下游可以更新哪些状态字段 |
| expected_output | 下游应该产出什么 |
| return_contract | 下游如何回传结果 |

### 8.2 Handoff 粒度

| 粒度 | 表现 | 适用 |
|------|------|------|
| Text-only | 一段自然语言说明 | demo、低风险任务 |
| Summary + Artifact | 摘要加证据引用 | 大多数内容生成/研究任务 |
| Structured Contract | schema 化交接 | 生产工作流 |
| State Capability Token | 交接时附带字段级读写权限 | 高安全/跨组织任务 |

### 8.3 Handoff 的反模式

- “你接着做”但不给目标和完成标准。
- 只传最后一条消息，不传关键约束。
- 传全量历史，让下游自己找重点。
- 不告诉下游哪些结论已验证、哪些只是猜测。
- 不定义下游可以写哪些状态。
- 不要求下游回传 artifact 或 state patch。

---

## 九、主流实现如何处理状态与上下文

### 9.1 A2A：跨系统的任务状态合同

A2A 的强项：

- 跨框架、跨组织、跨厂商。
- Agent Card 声明能力和认证。
- Task 提供长任务生命周期。
- Message / Part 承载交互内容。
- Artifact 承载结果。
- Streaming / Push 支持进度同步。
- Opaque Agent 设计保护内部实现。

A2A 的弱项：

- 不负责你的内部共享状态 schema。
- 不帮你做 reducer、memory、checkpoint。
- 不知道远程 Agent 内部如何推理。
- 只能约束协议边界，不能替代应用级治理。

A2A 的推荐工程用法：

```text
内部：
  LangGraph / workflow engine 管理细粒度状态

边界：
  A2A endpoint 暴露 Agent Card、Task、Message、Artifact

交接：
  Message DataPart 传结构化 handoff
  Artifact 传产物引用
  metadata 传 correlation id / trace id / state version

审计：
  A2A Task event + 内部 trace 对齐
```

### 9.2 LangGraph：显式 StateGraph + reducer + checkpoint

LangGraph 更像“多 Agent 的状态机/图运行时”，它的关键点是：

- 用 typed state 定义全局状态。
- 每个 node/agent 读 state 的一部分，返回 state update。
- reducer 决定并发更新如何合并。
- checkpointer 按 thread 保存每一步状态快照。
- 可以中断、恢复、时间旅行、human-in-the-loop。

简化示例：

```python
from typing import Annotated, TypedDict
from langgraph.graph import StateGraph
from langgraph.graph.message import add_messages
from langgraph.checkpoint.memory import MemorySaver

class AgentState(TypedDict):
    messages: Annotated[list, add_messages]
    task_status: str
    requirements: dict
    risks: list[dict]
    artifacts: list[dict]

def research_agent(state: AgentState):
    return {
        "messages": [{"role": "assistant", "content": "research done"}],
        "artifacts": [{"id": "research_notes_v1", "type": "markdown"}],
    }

def risk_agent(state: AgentState):
    return {
        "risks": [{"id": "risk_001", "severity": "medium"}],
    }

builder = StateGraph(AgentState)
builder.add_node("research", research_agent)
builder.add_node("risk", risk_agent)

graph = builder.compile(checkpointer=MemorySaver())
graph.invoke(
    {"messages": [], "task_status": "submitted", "requirements": {}, "risks": [], "artifacts": []},
    config={"configurable": {"thread_id": "task_001"}},
)
```

LangGraph 的适配思路：

| 需求 | LangGraph 做法 |
|------|----------------|
| 当前任务状态 | Typed State |
| 并发消息合并 | `add_messages` / custom reducer |
| 长任务恢复 | Checkpointer + `thread_id` |
| 人工审批 | Interrupt / resume |
| 多 Agent 路由 | Supervisor node / command / conditional edge |
| 外部互操作 | 在 node 内调用 A2A client |

最佳实践：

- 不要把所有内容都塞进 `messages`。
- 把可验证事实、风险、artifact、审批状态拆成独立字段。
- 对 append-only 字段使用 reducer。
- 对高风险字段使用显式审批节点。
- checkpoint 不是长期记忆，artifact store 不是 checkpoint 的替代。

### 9.3 AutoGen：消息线程和 Team State

AutoGen 的心智更接近“多个可对话 Agent 组成一个 team/group chat”。

它的状态特点：

- Agent 通过消息线程协作。
- GroupChat / Team manager 负责发言顺序或路由。
- 可以保存和加载 team/agent 状态。
- 很适合对话式协作、辩论、代码讨论、研究讨论。

典型模式：

```python
# 伪代码：具体 API 版本可能随 AutoGen 演进调整
state = await team.save_state()

# 持久化到数据库或文件
save_to_store(task_id, state)

# 恢复
state = load_from_store(task_id)
await team.load_state(state)
```

AutoGen 的优势：

- 多 Agent 对话自然。
- 适合 group chat、round-robin、selector、critic 等模式。
- 保存 team state 可以恢复消息线程和成员状态。

风险：

- 如果把共享状态主要放在聊天消息里，长任务会越来越难控。
- group chat 容易广播过多信息。
- 需要自己补结构化 task state、artifact store、权限边界。

建议：

```text
AutoGen message thread = 协作过程
外部 task state store = 当前结构化真相
artifact store = 工作产物
trace = 审计
```

### 9.4 CrewAI：任务流、角色分工、Memory/Knowledge

CrewAI 的心智是“多个角色化 Agent 组成 Crew，按 Process 执行任务”。

状态与上下文相关能力：

- Agent 有 role、goal、backstory、tools。
- Task 定义输入、期望输出、上下文依赖。
- Process 可以是 sequential、hierarchical 等。
- Memory 可以在 Crew 或 Agent 层面使用。
- Knowledge 可作为 agent/crew 级知识源。

示意：

```python
from crewai import Agent, Task, Crew, Process, Memory

shared_memory = Memory()

researcher = Agent(
    role="Researcher",
    goal="收集事实并保留来源",
)

writer = Agent(
    role="Writer",
    goal="基于已验证事实生成报告",
)

research_task = Task(
    description="调研目标市场，并输出带来源的事实列表",
    expected_output="facts.json + research_notes.md",
    agent=researcher,
)

write_task = Task(
    description="基于调研结果生成管理层报告",
    expected_output="report.md",
    agent=writer,
    context=[research_task],
)

crew = Crew(
    agents=[researcher, writer],
    tasks=[research_task, write_task],
    process=Process.sequential,
    memory=shared_memory,
)
```

CrewAI 的优势：

- 角色和任务抽象清晰。
- 适合顺序任务流和层级管理。
- memory/knowledge 对快速搭建有帮助。

需要补强的点：

- 高风险生产任务仍要外置结构化 state store。
- memory 召回结果要带来源与 scope。
- task output 不应只是一段自然语言，应尽量结构化并产出 artifact。
- hierarchical manager 不等于完整的状态治理。

### 9.5 OpenAI Agents SDK：handoff、context、sessions、tracing

OpenAI Agents SDK 的核心抽象是 Agent、Runner、tools、handoffs、guardrails、sessions、tracing。

与状态同步相关的点：

- `context` 可以传给 tool、guardrail、handoff。
- handoff 可以把控制权交给专业 Agent。
- session/memory 能维持多轮上下文。
- tracing 记录 LLM 调用、工具调用、handoff、guardrail 等事件。
- handoff input filter 可控制交接时哪些输入被传递。

示意：

```python
# 伪代码，表达设计重点
class RunContext:
    def __init__(self, user_id, tenant_id, task_id, state_version, permissions):
        self.user_id = user_id
        self.tenant_id = tenant_id
        self.task_id = task_id
        self.state_version = state_version
        self.permissions = permissions

triage_agent = Agent(
    name="Triage",
    instructions="判断任务类型，必要时 handoff 给专业 Agent。",
    handoffs=[legal_agent, finance_agent],
)

result = Runner.run_sync(
    triage_agent,
    input="审查这份合同的付款和赔偿条款",
    context=RunContext(
        user_id="u_123",
        tenant_id="t_acme",
        task_id="contract_001",
        state_version=8,
        permissions=["read_contract", "write_risks"],
    ),
)
```

建议：

- `context` 里放系统可验证的身份、任务、权限、版本，不要放无限增长的聊天历史。
- handoff 时使用 input filter 或结构化 payload 做最小必要交接。
- trace id 要和外部 task id / A2A task id 对齐。
- guardrail 结果应写入 event log，而不是只留在 SDK 内部。

### 9.6 MCP：共享上下文来源，不是 Agent 状态协议

MCP 主要解决模型/Agent 如何连接工具和上下文资源：

- Tools：可执行动作。
- Resources：可读取上下文数据。
- Prompts：可复用指令模板。

它不等于多 Agent 状态同步协议，但可以作为共享状态/共享记忆的访问层。

典型用法：

```text
Agent A / Agent B
   |
   +-- MCP server: project-memory
   |      +-- resources/read("state://task/123")
   |      +-- tools/call("append_fact", ...)
   |
   +-- MCP server: artifact-store
          +-- resources/read("artifact://report/v2")
          +-- tools/call("create_artifact", ...)
```

注意：

- MCP server 暴露资源和工具，不代表所有 Agent 都能任意读写。
- 权限要在 MCP server 或上游 gateway 里检查。
- Tool poisoning、prompt injection、数据外泄风险要单独治理。
- MCP 适合“给 Agent 手和资料”，A2A 适合“让 Agent 和 Agent 协商任务”。

### 9.7 选型矩阵

| 场景 | 推荐主机制 | 原因 |
|------|------------|------|
| 同一应用内部，多 Agent 状态复杂 | LangGraph / workflow engine | 显式 typed state、reducer、checkpoint |
| 跨组织、跨厂商 Agent 调用 | A2A | 标准 Task / Message / Artifact 边界 |
| 对话式 multi-agent brainstorming | AutoGen | group chat / team state 更自然 |
| 角色化业务流程、快速原型 | CrewAI | Agent/Task/Crew 抽象简单 |
| OpenAI 技术栈内的多 Agent handoff | OpenAI Agents SDK | handoff、context、guardrail、trace 集成 |
| 工具/资源/知识接入 | MCP | 标准化 tool/resource/prompt 接入 |
| 高可靠长任务 | Workflow engine + event log + artifact store | 可恢复、可审计、可重放 |

---

## 十、生产级架构参考

### 10.1 单应用内部多 Agent

```text
[API Gateway]
   |
[Orchestrator / LangGraph]
   |
   +-- State Store (Postgres / checkpoint)
   +-- Event Log (append-only)
   +-- Artifact Store (S3 / Git / Blob)
   +-- Memory Service (vector / graph / profile)
   +-- Policy Engine (RBAC / ABAC)
   |
   +-- Research Agent
   +-- Planning Agent
   +-- Tool Agent
   +-- Verifier Agent
   +-- Writer Agent
```

特点：

- Orchestrator 掌握全局状态。
- Agent 只拿到自己需要的 context view。
- Agent 返回 state patch 和 artifacts。
- Verifier / policy engine 决定是否接受更新。

### 10.2 跨组织 A2A 协作

```text
Company A Supervisor Agent
   |
   +-- Internal State Store
   +-- Internal Artifact Store
   |
   +-- A2A Client
          |
          v
Company B A2A Server
   |
   +-- Agent Card
   +-- Task Store
   +-- B Internal Agent Runtime
   +-- B Artifact Store
```

跨组织边界：

- A 公司不应该拿到 B 公司的内部 memory。
- B 公司不应该拿到 A 公司的全量用户上下文。
- 双方通过 Task、Message、Artifact、auth、metadata 交换最小必要信息。
- 每个组织内部用自己的 runtime 管状态，A2A 只定义边界合同。

### 10.3 状态生命周期

```text
submitted
  |
  v
working
  |
  +-- state.patch.proposed
  +-- artifact.created
  +-- handoff.started
  +-- input.required
  |
  v
reviewing
  |
  +-- approved
  +-- rejected
  |
  v
completed / failed / canceled
```

每个状态变更都应该有：

```text
event_id
task_id
actor
timestamp
base_version
new_version
operation
evidence_refs
trace_id
policy_decision
```

---

## 十一、安全与权限：上下文同步也是攻击面

### 11.1 最小披露

跨 Agent 同步时只传：

- 完成任务必要的信息。
- 明确授权可见的字段。
- 可撤销、可过期的 artifact 链接。
- 不可伪造的身份和权限声明。

不要传：

- system prompt。
- chain-of-thought。
- 工具凭证。
- 无关用户隐私。
- 其他租户数据。
- 未过滤的外部网页内容。

### 11.2 跨 Agent Prompt Injection

风险路径：

```text
外部网页 / 邮件 / 文档
   -> Research Agent 读取
   -> 写入 shared memory 或 handoff summary
   -> Writer Agent / Tool Agent 继承恶意指令
   -> 执行越权工具
```

防护：

- 外部内容作为 data，不作为 instruction。
- handoff payload 区分 `facts` 和 `instructions`。
- shared memory 写入前做来源标注和安全扫描。
- 工具执行前重新检查用户授权和任务边界。
- 高风险工具不接受来自其他 Agent 的自然语言授权。

### 11.3 记忆投毒

记忆投毒比单次 prompt injection 更危险，因为它会跨任务复用。

治理方式：

- 所有长期记忆必须带来源和写入者。
- 自动写入的记忆先进入 pending，必要时人工或 verifier 审核。
- 记忆有 TTL 和撤销机制。
- 高风险任务优先使用当前 task state，不盲信长期 memory。
- 召回结果必须显示 provenance，不能只给模型一段无来源文字。

---

## 十二、可观测与评估

### 12.1 必须观测的指标

| 指标 | 解释 |
|------|------|
| state_patch_count | 每个任务产生多少状态更新 |
| rejected_patch_rate | patch 被 policy / schema / verifier 拒绝比例 |
| stale_read_count | Agent 基于旧 state_version 写入的次数 |
| conflict_count | 写写冲突、事实冲突、计划冲突次数 |
| handoff_success_rate | handoff 后下游是否能一次完成 |
| handoff_rework_rate | 下游要求补上下文或返工比例 |
| artifact_reference_rate | 关键结论是否引用 artifact |
| memory_hit_rate | 召回记忆是否实际被使用 |
| memory_correction_rate | 记忆被撤销或修正比例 |
| context_tokens_per_agent | 每个 Agent 注入上下文 token |
| unnecessary_broadcast_rate | 广播给 Agent 但未使用的信息比例 |

### 12.2 Trace 设计

每一次多 Agent run 至少要能回答：

- 用户原始目标是什么？
- Orchestrator 把任务拆给了哪些 Agent？
- 每个 Agent 看到了哪些 context view？
- 每个 Agent 写了哪些 state patch？
- 哪些 patch 被接受、拒绝、修改？
- 最终答案引用了哪些 artifact？
- 哪些工具调用产生了不可逆副作用？
- 哪些地方发生过 handoff？
- 哪些上下文来自长期记忆？
- 哪些决策经过人工审批？

### 12.3 Handoff 质量评估

可以给 handoff 打分：

| 维度 | 好的表现 |
|------|----------|
| 完整性 | 下游完成任务不需要反复追问 |
| 精确性 | 事实、假设、约束区分清楚 |
| 可验证性 | 关键结论有 artifact/evidence |
| 最小性 | 没有传无关历史和隐私 |
| 可执行性 | 有明确 expected output 和 return contract |
| 安全性 | 权限、禁止动作、风险边界清楚 |

---

## 十三、场景设计：企业入职多 Agent

### 13.1 需求

新员工入职，需要 HR Agent、IT Agent、Security Agent、Finance Agent 协作：

- HR 收集员工信息。
- IT 开账号和设备。
- Security 做权限审批。
- Finance 录入薪资和成本中心。
- Supervisor 汇总进度并通知员工。

### 13.2 状态模型

```json
{
  "task_id": "onboarding_20260701_alice",
  "context_id": "employee_alice_onboarding",
  "status": "working",
  "employee": {
    "name": "Alice",
    "department": "Finance",
    "manager": "Bob",
    "start_date": "2026-07-01"
  },
  "subtasks": {
    "hr_profile": {"owner": "hr_agent", "status": "completed"},
    "it_account": {"owner": "it_agent", "status": "working"},
    "security_review": {"owner": "security_agent", "status": "input_required"},
    "payroll": {"owner": "finance_agent", "status": "pending"}
  },
  "artifacts": [
    {"id": "hr_form_v1", "type": "form", "producer": "hr_agent"}
  ],
  "risks": [],
  "approvals": []
}
```

### 13.3 Handoff：HR 到 IT

```json
{
  "from_agent": "hr_agent",
  "to_agent": "it_agent",
  "goal": "为新员工开通基础账号并准备设备",
  "current_state_summary": "HR 信息已收集完成，员工属于 Finance 部门。",
  "constraints": [
    "只可读取员工入职必要字段",
    "不得读取薪资字段",
    "Finance 权限需 Security 审批后才能开通"
  ],
  "relevant_artifacts": ["artifact:hr_form_v1"],
  "write_scope": [
    "/subtasks/it_account",
    "/artifacts"
  ],
  "expected_output": {
    "state_patch": true,
    "artifacts": ["it_ticket"]
  }
}
```

### 13.4 A2A 跨系统调用

如果 IT Agent 属于另一个组织或系统，可以通过 A2A：

```text
HR Supervisor
  -> discover IT Agent Card
  -> create A2A Task: "provision account"
  -> send structured handoff as Message DataPart
  -> receive input-required: "Finance role needs manager approval"
  -> collect approval
  -> continue same task.id
  -> receive Artifact: it_ticket_123
  -> update internal state
```

注意这里有两套状态：

- A 公司内部 onboarding state。
- IT Agent 暴露的 A2A Task state。

两者通过 `task_id`、`context_id`、`trace_id`、`artifact_id` 对齐，但不互相暴露内部 memory。

### 13.5 冲突处理

可能冲突：

- HR 说 Alice 属于 Finance，Finance 系统说成本中心不存在。
- IT 已开通账号，Security 后来拒绝高权限。
- Manager approval 过期。

处理：

- 员工基础信息以 HRIS 为权威源。
- 权限字段由 Security Agent 写。
- 成本中心由 Finance Agent 写。
- IT 只能写账号开通状态，不能写审批状态。
- 已执行副作用必须有补偿动作，例如撤销权限。

---

## 十四、反模式清单

- 把所有聊天记录广播给所有 Agent。
- 只依赖自然语言 summary，没有结构化 state。
- 只有 message，没有 artifact。
- 只有 final answer，没有 event log。
- 多个 Agent 都能改最终状态，没有字段级所有权。
- 没有 `state_version`，无法发现 stale write。
- 没有 conflict policy，冲突时靠模型“自己判断”。
- 把长期 memory 当事实真相，不做来源验证。
- 把外部网页内容当系统指令传给下游 Agent。
- 把 A2A 当成内部状态同步数据库。
- 把 MCP resource 当成无权限边界的共享磁盘。
- 没有 handoff contract，只说“继续处理”。
- artifact 不版本化，后续 Agent 不知道读的是哪一版。
- trace 只记录 LLM 文本，不记录状态 patch 和权限判断。

---

## 十五、设计决策树

### Q1：是否跨组织或跨厂商？

- 是：优先 A2A 暴露任务边界。
- 否：内部可以用 LangGraph / workflow engine 管细粒度状态。

### Q2：是否需要长任务恢复？

- 是：必须有 checkpoint 或 event log。
- 否：短 session state 也可以，但仍建议保留 artifact。

### Q3：是否有并发 Agent 写同一状态？

- 是：必须定义 reducer、锁、事务或仲裁。
- 否：顺序流程可以简单很多。

### Q4：是否涉及高风险操作？

- 是：字段级权限、approval、audit log、补偿动作必需。
- 否：可以先用结构化 handoff + artifact reference。

### Q5：是否需要跨任务复用经验？

- 是：引入 long-term memory，但要有 scope、provenance、TTL。
- 否：不要过早引入长期 memory。

---

## 十六、面试与系统设计高频问题

### Q1：多 Agent 要不要共享完整上下文？

不应该默认共享完整上下文。完整共享只适合短任务、低风险、同信任域 demo。生产系统应按最小必要原则共享结构化状态、摘要、工件引用和约束。每个 Agent 看到的是全局状态的一个 view，而不是全部历史。

### Q2：A2A 里的 Agent 之间能不能共享 memory？

A2A 不负责共享内部 memory。它的边界是 Task、Message、Artifact、Agent Card 等协议对象。跨 Agent 可以共享任务相关事实、工件和结构化 handoff，但不应该共享内部 prompt、长期 memory 或工具私有状态。如果多个 Agent 属于同一个应用，可以在内部用共享 memory service；这已经是应用架构，不是 A2A 协议本身。

### Q3：LangGraph 的 state 和 A2A 的 task 是什么关系？

LangGraph state 是应用内部的细粒度状态，适合表达图节点之间的读写、reducer、checkpoint。A2A task 是跨系统互操作的任务合同，适合表达远程 Agent 的工作单元和公开生命周期。一个常见架构是：内部用 LangGraph 管状态，外部通过 A2A task 暴露能力。

### Q4：handoff 最容易漏什么？

最容易漏三类信息：

- 已经确定的约束和禁止动作。
- 证据引用和未决问题。
- 下游可以写哪些状态、应该返回什么格式。

只传摘要会让下游 Agent 重新猜任务边界。

### Q5：如何避免共享 memory 被污染？

做四件事：

- 写入时记录 source、writer、timestamp、confidence、scope。
- 高风险记忆进入 pending 或需要 verifier。
- 召回时把 provenance 一起注入上下文。
- 支持撤销、过期和覆盖策略。

### Q6：多 Agent 并发更新状态时怎么做？

先区分字段类型：

- append-only 字段用 reducer。
- 单值低风险字段可 last-write-wins。
- 高风险字段要锁、事务、审批或 supervisor arbitration。
- 事实冲突要 evidence + verifier。
- 旧版本写入要拒绝或要求基于新版本重试。

### Q7：Artifact 和 Memory 有什么区别？

Artifact 是任务产物和证据，应该可引用、可版本化、可复查。Memory 是跨任务可召回的知识或经验，可能经过压缩和抽象。生产里关键结论应优先引用 artifact，memory 只能辅助召回，不应替代证据。

### Q8：MCP 能不能解决多 Agent 状态同步？

MCP 能提供共享资源和工具访问层，但它不是多 Agent 状态协议。你可以用 MCP server 暴露 task state、artifact、memory 工具，但仍要自己处理 schema、权限、冲突、审计。跨 Agent 任务协作更适合 A2A；内部状态机更适合 workflow engine。

---

## 十七、学习与落地清单

- [ ] 能区分 State / Context / Memory / Message / Artifact / Trace。
- [ ] 能解释为什么不应该广播完整上下文。
- [ ] 能画出七层状态模型。
- [ ] 能设计 structured handoff schema。
- [ ] 能设计 state patch 和字段级写权限。
- [ ] 能说明 A2A Task 与内部状态 store 的关系。
- [ ] 能用 LangGraph reducer/checkpointer 解释共享状态。
- [ ] 能说明 AutoGen group chat 的状态风险。
- [ ] 能说明 CrewAI memory/knowledge 与 task output 的关系。
- [ ] 能说明 OpenAI Agents SDK handoff/context/tracing 的落点。
- [ ] 能说明 MCP 是资源/工具协议而不是 Agent 状态协议。
- [ ] 能给出冲突解决策略。
- [ ] 能设计 artifact-first 的上下文同步。
- [ ] 能规划 memory provenance、TTL、撤销机制。
- [ ] 能为高风险任务增加 HITL 和审计。

---

## 十八、关键术语 Cheat Sheet

| 术语 | 含义 |
|------|------|
| Global State | 多 Agent 共享的当前任务结构化真相 |
| Local Context | 单个 Agent 私有上下文和中间状态 |
| Shared Working Memory | 当前任务内共享的短期事实和工作台 |
| Long-term Memory | 跨任务复用的经验、偏好、事实 |
| Artifact | 可引用、可复查、可版本化的任务产物 |
| Event Log | append-only 的状态变化和通信记录 |
| State Patch | Agent 提交的结构化状态更新 |
| Reducer | 多个更新写入同一字段时的合并函数 |
| Handoff | Agent 之间的控制权或任务交接 |
| Handoff Contract | 结构化交接协议，包括目标、约束、证据、返回格式 |
| Blackboard | 多 Agent 共享的事实/问题/决策工作区 |
| Provenance | 信息来源、写入者、时间、证据链 |
| Stale Write | 基于旧状态版本提交的写入 |
| Conflict Policy | 冲突发生时的合并、仲裁或审批规则 |
| Visibility Scope | private/team/task/tenant/public 等可见性 |
| A2A Task | A2A 协议里的长任务工作单元 |
| MCP Resource | MCP 暴露给模型/Agent 的上下文资源 |

---

## 十九、延伸阅读

- A2A 官方规范与概念：<https://a2a-protocol.org/latest/topics/key-concepts/>
- A2A GitHub specification：<https://github.com/a2aproject/A2A/blob/main/docs/specification.md>
- LangGraph Persistence：<https://docs.langchain.com/oss/python/langgraph/persistence>
- LangGraph 低层概念与状态图：<https://docs.langchain.com/oss/python/langgraph>
- AutoGen Managing State：<https://microsoft.github.io/autogen/dev/user-guide/agentchat-user-guide/tutorial/state.html>
- AutoGen Team / Group Chat API：<https://microsoft.github.io/autogen/0.5.3/reference/python/autogen_agentchat.teams.html>
- CrewAI Memory：<https://docs.crewai.com/en/concepts/memory>
- CrewAI Knowledge：<https://docs.crewai.com/en/concepts/knowledge>
- OpenAI Agents SDK：<https://openai.github.io/openai-agents-python/agents/>
- OpenAI Agents SDK Tracing：<https://github.com/openai/openai-agents-python/blob/main/docs/tracing.md>
- MCP Architecture：<https://modelcontextprotocol.io/docs/learn/architecture>
- MCP Server Concepts：<https://modelcontextprotocol.io/docs/learn/server-concepts>
- 本仓库相关文档：[06-多Agent协作.md](./06-多Agent协作.md)、[13-A2A与Agent互操作协议.md](./13-A2A与Agent互操作协议.md)、[16.2-Agent Loop上下文管理.md](./16.2-Agent%20Loop上下文管理.md)、[08.1-生产级Agent应用工程.md](./08.1-生产级Agent应用工程.md)

---

## 二十、核心要点速记

- 多 Agent 状态管理不是复制聊天记录，而是维护结构化、可审计、可授权的任务状态。
- A2A 同步 Task / Message / Artifact，不同步内部 prompt / memory / scratchpad。
- 内部编排优先使用显式 state、reducer、checkpoint；跨系统协作用 A2A。
- Handoff 必须结构化，至少包含目标、当前状态、约束、证据、未决问题、写权限和返回合同。
- Artifact-first 是控制 token、提升审计能力的关键。
- 长期 memory 必须有 scope、provenance、TTL 和撤销机制。
- 并发写状态一定会冲突，必须提前设计 reducer、仲裁和人工审批。
- Trace 不只是调试日志，而是解释最终结论和追责的基础设施。
