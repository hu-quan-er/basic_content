# Voice 实时性能优化与预测式执行

> 目标：专门回答「Voice / Realtime Agent 怎么保证性能」。[11-Voice与Realtime Agent](./11-Voice与Realtime%20Agent.md) 讲了延迟拆解和「流式一切」的基础,但把最能压延迟的一层——**预测式执行**——只用了几句带过。本文深入这一层:prequery（投机预查）、模型续写（interim 起草 → final 对齐）、预测式 endpointing、跨轮 KV 复用,以及「赌着提前做」的成本-质量权衡。

---

## 本文职责边界

| 本文负责 | 本文不展开 |
|---|---|
| 实时语音的延迟隐藏策略全景、预测式 endpointing、prequery/投机执行、模型续写与 interim/final 对齐、跨轮 prefix/KV 复用、暖连接、投机的成本-质量权衡、barge-in 与投机的交互 | Voice 架构范式 / pipeline 组件 / barge-in 基础 / 评估见 [11-Voice与Realtime Agent.md](./11-Voice与Realtime%20Agent.md)；底层推理加速（prefill/decode、speculative decoding、TTFT/TPOT）见 [19-推理加速底层系统.md](./19-推理加速底层系统.md)；Agent 通用延迟优化（工具并行、模型分层、缓存闭环）见 [19.1-Agent推理加速落地.md](./19.1-Agent推理加速落地.md)；成本与缓存治理见 [08-工程化性能成本可靠性.md](./08-工程化性能成本可靠性.md) |

---

## 一、一句话定义

**实时语音性能的本质,是把「用户说完 → Agent 出声」的等待,用五类手段压到人耳听不出来。** 其中最难、也最能拉开差距的,是**预测（predict）**:在用户还没说完、或还没确定要说什么时,就先把大概率要做的工作做了。

```text
缩短 shrink   ── 每一步本身更快（流式、低 TTFT 模型、量化）
重叠 overlap  ── 各步并行（流水线:ASR/LLM/TTS 同时跑）
隐藏 hide     ── 用填充语/backchannel 掩盖真实延迟（感知延迟↓）
预取 prefetch ── 提前把可能要的数据/连接备好（缓存、暖连接、prequery 检索）
预测 predict  ── 在信息不全时赌着提前生成（投机执行、模型续写、预测式 endpoint）
```

前三类 [11](./11-Voice与Realtime%20Agent.md) 和 [19.1](./19.1-Agent推理加速落地.md) 已覆盖。本文主攻后两类。

**核心判断**：实时语音的性能天花板,不在「把每一步做快」,而在「**敢不敢在信息不全时提前动手,并优雅地处理赌错**」。这是文本 Agent 用不上、语音场景独有的杠杆。

---

## 二、目标预算与最大的那块等待

口到耳（mouth-to-ear）目标一般是 **< 800ms** 才自然。串行链路的等待:

```text
用户停止说话
  → VAD/endpoint 判定「说完了」   ← 常是最大的一块（静音门限 300~800ms）
  → ASR 出 final                   100~400ms（流式 partial ~即时）
  → LLM TTFT                       200~500ms
  → 首句 TTS TTFB                  100~300ms
  → 出声
```

**关键观察**:两块最肥的延迟——「等 endpoint」和「等 LLM 想完」——恰好都发生在**用户其实已经把意思说得差不多的时候**。预测式执行就是去吃掉这两块:用**预测式 endpointing**砍掉等待判定的时间,用 **prequery + 模型续写**把 LLM/检索的工作提前到用户还在说的时候。

---

## 三、预测式 Endpointing：先砍掉最大的等待

纯 VAD（声学静音检测）要等一段静音（如 500ms）才敢判「说完了」,这段静音是白等的延迟。改进:

| 方式 | 机制 | 权衡 |
|---|---|---|
| 纯 VAD | 声学静音门限 | 简单;门限高则慢,低则易被句中停顿误触 |
| 语义 endpointing | 小模型/分类器基于 partial transcript 预测「这句话是否已完整」 | 能在静音很短时就判定说完,大幅提前;但可能抢跑打断用户 |
| 声学 + 语义双信号 | VAD 判「停了」+ 语义判「说完了」融合 | 最稳:短停顿不误触,完整句快触发 |

设计要点:
- **抢跑用置信度门控 + 可回退**：语义高置信才提前触发;若刚触发用户又开口,取消本轮(回退,配合 barge-in)。
- **预测式 endpoint 是 prequery 的扳机**：一旦预测「快说完了」,就提交投机执行（第四、五节）,让 LLM/检索在真正 endpoint 前就跑起来。

---

## 四、Prequery：投机预查 / 预测式执行

### 4.1 定义

**Prequery = 用 interim（未定稿）transcript,在 endpoint 之前就 speculatively 发起下游工作,让结果在用户说完的那一刻已经就绪或在途。** 把「用户还在说话」的这段时间榨出来并行预跑。

### 4.2 三类可预取对象

| 预取对象 | 触发信号 | 例 |
|---|---|---|
| 检索 / RAG prequery | partial 里已出现关键实体 | 听到「我上个月的账单」→ 提前检索该用户账单 |
| 工具参数 / 只读工具 prequery | 高置信意图 + 只读 | 听到「查一下明天北京到上海的航班」→ 提前备好查询参数、甚至预执行只读查询 |
| LLM draft prequery | partial 语义基本完整 | 用 partial 让 LLM 提前起草回复（[11](./11-Voice与Realtime%20Agent.md) 的「Speculative LLM」）|

### 4.3 提交 / 丢弃状态机

投机的灵魂是「赌错要能优雅回退」:

```text
partial 稳定 + 高置信 ──▶ speculate（发起检索/起草/只读预执行）
                              │ 结果先 hold,不出声、不落库
              final 到达 ─────┤
                              ├─ 对齐（前缀稳定 / 语义等价）─▶ commit：直接用,省掉这段等待
                              └─ 分叉（意思变了）───────────▶ discard：丢弃重来（用填充语盖住）
```

### 4.4 门控：什么能投机、什么绝不能

- **只投机可丢弃、无副作用的工作**：检索、只读查询、LLM 起草——赌错顶多浪费一次调用。
- **有副作用的动作绝不 speculative execute**：退款、下单、改套餐不能因为「听着像要退款」就先执行。可以**预备参数**,但执行必须等 final + 授权/审批（呼应 [20.3 平台鉴权](./20.3-Agent平台鉴权体系设计.md)、[16.3 HITL](./16.3-Agent%20Loop人在回路.md)）。
- **投机门控条件**：partial 连续几帧稳定 + 意图置信度高 + 目标是只读/可丢弃。不满足就老实等 final。

**核心判断**：prequery 和普通「工具并行」的区别在于——它在**信息尚未确定**时就动手,因此必须自带「对齐判定 + 丢弃回退」,并对副作用零容忍。

---

## 五、模型续写：interim 起草 → final 对齐

这是用户点名的「模型续写」,也是投机执行里最精细的一环。

### 5.1 问题

流式 ASR 的 interim transcript 会边说边改（「查一下我的…账单…哦是上个月的账单」）。若等 final 才让 LLM 动手,慢;若用 interim 起草,final 一变就白起草。**模型续写**要在两者间取平衡。

### 5.2 draft-and-verify：起草 → 校验 → 续写 或 重生成

机制上与 [19 speculative decoding](./19-推理加速底层系统.md) 同构,只是搬到了「对话轮」这一层:

```text
1. 起草：用 interim transcript 让 LLM 开始 decode 一段 draft（并建立 KV 前缀）
2. 校验：final 到达,比对 interim vs final
   ├─ 前缀稳定 / 语义等价 ─▶ 续写：复用已生成的 token 和 KV,从断点继续 decode
   │                          （已算的 prefill 和 token 不丢,直接省下这段时间）
   └─ 语义分叉 ───────────▶ 从分叉点回退重生成后半段(或轻量 patch),前半可复用则复用
3. 出声：只把「已确认对齐」的句子送 TTS,避免播了又要撤回
```

### 5.3 关键约束

- **KV 复用是主要收益来源**：draft 阶段建立的 system prompt + 历史 + 已确认前缀的 KV,对齐后直接续用,省掉重复 prefill（呼应 [19 prefix cache/RadixAttention](./19-推理加速底层系统.md)）。
- **只让"稳定段"驱动已出声的语音**：一段 transcript 要"稳定到基本不会再改"才允许它驱动已提交给 TTS 的音频,否则会出现"说错又改口"的破音。
- **续写与 TTS 分句重叠**：LLM 续写下一句时,上一句已在流式播放——生成与播报重叠,进一步压延迟。
- **防语义漂移**：过度乐观续写会让回复跟着一个错误的 interim 跑偏;要有稳定性判据 + 分叉即回退,不能一路续到底。

---

## 六、跨轮复用与暖连接：把固定成本摊掉

预测之外,语音会话有大量**每轮重复的固定开销**可以摊销:

| 手段 | 做法 | 收益 |
|---|---|---|
| Prefix / KV 跨轮复用 | system prompt + 会话历史是稳定长前缀,用 prompt caching / KV 复用 | 每轮省掉重复 prefill,TTFT 显著下降（[19](./19-推理加速底层系统.md)、[08](./08-工程化性能成本可靠性.md)）|
| 持久连接 | ASR/LLM/TTS 全用长连 websocket/gRPC stream | 免去每轮握手与冷启动 |
| 暖池 / 预热 | 会话开始即建立并预热各段连接和模型 | 首轮不吃冷启动 |
| 填充语预合成 | 「让我查一下」「稍等」等提前合成缓存 | 需要时零合成延迟直接播 |

对语音尤其划算:同一会话内前缀只会增长、不会重排,是 prefix cache 的理想场景。

---

## 七、隐藏与兜底：赌错、赌慢了怎么办

预测会 miss,链路会偶尔慢,必须有掩盖和降级:

- **填充语 / backchannel 掩盖**：LLM 思考 + 工具执行期间先播「好的,我来查一下」（感知延迟↓,非真实延迟）。投机**命中**就顺势接上真实回复;**未命中**就用填充语盖住重算的时间。
- **与投机配合的节奏**：填充语给投机争取窗口;别等填充语播完才启动工具,要并行启动（[11 Q5](./11-Voice与Realtime%20Agent.md)）。
- **barge-in 下投机产物必须可取消**：用户打断时,要能 cancel 正在跑的 LLM 生成、stop 正在播的 TTS、丢弃所有投机的 draft 和预合成音频。投机产物一律「可撤销」,否则打断会出现幽灵语音。
- **超时降级**：慢链路超阈值 → 走小模型快答 / 模板兜底 / 转人工,别让用户干等。

---

## 八、一次带投机的对话时序（把上面串起来）

```text
用户开始说「帮我查一下我上个月的账单」
  t0  流式 ASR 出 partial：「帮我查…我上个月…账单」
  t1  partial 稳定 + 语义 endpoint 预测「快说完了」
        └─▶ prequery：并行发起「检索该用户上月账单」(只读) + LLM 用 partial 起草回复
  t2  用户停顿 → VAD+语义确认 endpoint → final ASR = interim（对齐）
  t3  commit：账单检索结果已就绪、draft 前缀可续写 → LLM 续写定稿
  t4  首句 TTS 立即流式播放：「您上月账单是…」（后续句边生成边播）
```

对比不投机的串行版本,t1→t3 这段「检索 + 起草」被塞进了「用户还在说 + 判 endpoint」的时间里,口到耳延迟从「各步相加」变成「各步重叠」。

---

## 九、成本-质量-延迟三角：投机的账怎么算

投机不是免费的——它**用算力换延迟**,赌错就是纯浪费。是否划算取决于命中率:

- **只对高价值步骤投机**：预取一次慢检索/慢工具收益大;预取一个本就很快的步骤不值。
- **用便宜模型做 draft**：起草用小模型,定稿才用大模型（模型分层,见 [19.1](./19.1-Agent推理加速落地.md)）。
- **只读才预执行,写操作只预备**：把浪费和风险都压住。
- **要度量的指标**：投机命中率、浪费调用占比、mouth-to-ear P50/P95、endpoint 误判打断率、填充语触发率。命中率太低就收紧门控。

**核心判断**：投机是「拿钱和算力买延迟」,必须用命中率把这笔账管起来;盲目全投机会让成本翻倍而体验没变好。

---

## 十、核心问题（含解答）

### Q1：Prequery 和普通的「工具并行」有什么区别？

工具并行是在**意图已确定**后把独立工具并发跑;prequery 是在**意图还没定稿（用 interim transcript）**时就赌着提前发起。因此 prequery 必须自带「final 对齐判定 + 赌错丢弃回退」,且对有副作用的动作零容忍——这两点是普通并行没有的。

### Q2：哪些能投机预取，哪些绝对不能？

只读、可丢弃的能投机执行（检索、查询、LLM 起草）;有副作用的（退款、下单、改配置）**永远不能因为"听着像"就执行**,最多预备参数,执行必须等 final + 授权。这条红线直接关联 [20.3 鉴权](./20.3-Agent平台鉴权体系设计.md) 和 [16.3 HITL](./16.3-Agent%20Loop人在回路.md)。

### Q3：模型续写怎么处理 interim → final 的变化？

draft-and-verify:用 interim 起草并建 KV 前缀;final 到达时比对,前缀稳定/语义等价就**续写**（复用已生成 token 和 KV,省 prefill）,分叉就从分叉点**重生成**。只有"稳定段"才允许驱动已出声的语音,避免改口破音（§5）。

### Q4：语义 endpointing 的收益和风险？

收益:不必干等声学静音门限,能在很短停顿时就判「说完了」,砍掉最大的一块等待,并作为 prequery 的扳机。风险:抢跑打断还没说完的用户。对策:声学+语义双信号融合、置信度门控、可回退。

### Q5：投机会不会让成本爆炸？怎么控？

会,如果命中率低就是纯浪费。控制:只对高价值慢步骤投机、用便宜模型起草、只读才预执行、按 partial 稳定度和意图置信度门控、持续监控命中率和浪费占比（§9）。

### Q6：barge-in 时那些投机产物怎么办？

全部作废。用户一打断,立即 cancel 正在跑的 LLM 生成、stop 正在播的 TTS、丢弃所有 draft 和预合成音频。所以投机产物在设计上必须「可取消」,否则打断后会冒出该被丢弃的幽灵语音。

### Q7：E2E 语音模型（Speech-to-Speech）出来后，这些技巧还有用吗？

E2E 减少了 ASR→LLM→TTS 的 hop、天然低延迟,但**预测式 endpointing 和投机的思想仍适用**,只是层次变了:变成在语音-语音模型内部/前置做 turn-end 预测和投机响应。跨轮 KV 复用、暖连接、barge-in 取消同样需要。架构选型见 [11 Q2](./11-Voice与Realtime%20Agent.md)。

---

## 十一、场景设计题（含答案）

### 场景 1：电话客服查账单 / 改套餐

**要点**：
- **查账单（只读）**：partial 出现「账单」实体 → prequery 提前检索;语义 endpoint 预测触发 LLM 起草;final 对齐后续写定稿,首句立即播。
- **改套餐（有副作用）**：可以用 partial 预备好「改套餐」的参数和确认话术,但**绝不预执行**;必须 final 确认意图 + 用户口头确认（HITL）后才真正下发。
- **兜底**：检索慢 → 填充语「稍等,我帮您查一下」;用户打断 → 全部投机作废重来。
- **摊销**：会话级持久连接 + 前缀 KV 复用,每轮 TTFT 降下来。

### 场景 2：英语口语陪练（低延迟优先，容错高）

**要点**：
- 延迟体验 > 偶尔重算,可**激进**:语义 endpoint 抢跑 + partial 就起草点评。
- 赌错代价低（重生成一句点评而已）→ 门控可放松,追求最低口到耳延迟。
- 用小模型做实时起草/纠错,大模型只在需要详细讲解时介入。
- barge-in 灵敏（学习者常改口）,投机产物快速丢弃。

---

## 十二、自测清单

- [ ] 说清实时语音降延迟的五类手段,并指出哪两类是语音独有的杠杆。
- [ ] 指出串行链路里最肥的两块延迟,以及预测式执行怎么吃掉它们。
- [ ] 解释预测式 endpointing 的机制、收益和抢跑风险,以及如何回退。
- [ ] 讲清 prequery 的三类预取对象和「提交/丢弃」状态机。
- [ ] 说明什么能投机执行、什么只能预备参数,以及为什么。
- [ ] 讲清模型续写的 draft-and-verify:对齐续写 vs 分叉重生成,及 KV 复用收益。
- [ ] 说明为什么「只有稳定段能驱动已出声的语音」。
- [ ] 说清 barge-in 时投机产物必须可取消的原因。
- [ ] 用命中率解释投机的成本账,以及怎么门控。

---

## 十三、关键术语 Cheat Sheet

| 术语 | 含义 |
|---|---|
| Mouth-to-ear | 口到耳延迟,用户说完到 Agent 出声的总时延 |
| 五类降延迟手段 | 缩短 / 重叠 / 隐藏 / 预取 / 预测 |
| 预测式 Endpointing | 用语义预测「说完了」,不干等声学静音门限 |
| Prequery / 投机预查 | 用 interim transcript 在 endpoint 前预发起检索/起草/只读工具 |
| Interim / Final transcript | 流式 ASR 的未定稿 / 定稿文本 |
| 模型续写 draft-and-verify | interim 起草 → final 对齐 → 续写 or 分叉重生成 |
| Commit / Discard | 投机结果对齐则采用、分叉则丢弃 |
| Prefix / KV 跨轮复用 | 稳定长前缀的 KV 缓存复用,省重复 prefill |
| 填充语 / Backchannel | 掩盖真实延迟的「稍等/嗯」,感知延迟↓ |
| Barge-in cancel | 打断时取消 LLM 生成、停 TTS、丢弃投机产物 |
| 投机命中率 | 投机结果被采用的比例,决定划不划算 |

---

## 十四、延伸阅读

- OpenAI Realtime API、LiveKit Agents、Pipecat：实时编排与投机/流式实践
- Deepgram / 流式 ASR 的 interim vs final、endpointing 参数
- Speculative decoding（draft-and-verify）原理见 [19-推理加速底层系统.md](./19-推理加速底层系统.md)
- 本库关联：[11 Voice与Realtime Agent](./11-Voice与Realtime%20Agent.md)、[19 推理加速底层系统](./19-推理加速底层系统.md)、[19.1 Agent推理加速落地](./19.1-Agent推理加速落地.md)、[08 工程化性能成本可靠性](./08-工程化性能成本可靠性.md)、[20.3 平台鉴权](./20.3-Agent平台鉴权体系设计.md)、[16.3 HITL](./16.3-Agent%20Loop人在回路.md)

---

## 十五、核心要点

1. **五类手段,语音独有的杠杆是「预取 + 预测」**：缩短/重叠/隐藏是通用功课,prequery 和模型续写才是把口到耳压到自然区间的关键。
2. **先砍最大的等待**：预测式 endpointing 吃掉「等静音」,prequery/续写吃掉「等 LLM 想完」,把工作塞进用户还在说话的时间里。
3. **投机必须自带回退**：提交/丢弃状态机、稳定段才出声、barge-in 全作废——赌错要优雅,而不是出破音或幽灵语音。
4. **有副作用的动作零投机**：只读可预执行,写操作只能预备参数,执行仍等 final + 授权,安全红线不让位于延迟。
5. **用命中率管住投机的账**：投机是拿算力换延迟,只对高价值步骤、用便宜 draft、按置信度门控,并持续度量命中率和浪费占比。
