# 结构化输出与约束解码专题

> 目标：系统理解如何让 LLM 稳定输出 JSON、工具参数、计划图、状态补丁和最终结构化结果。结构化输出不是一句 "output JSON only"，而是一整套 schema 设计、约束生成、校验、修复、版本治理和性能权衡。

---

## 本文职责边界

| 本文负责 | 本文不展开 |
|---|---|
| JSON/schema、tool args、final output、state patch、eval result、约束解码、validator、repair loop、schema 版本治理 | Tool Gateway 的权限、幂等、审计、风险控制见 [23-Tool Gateway与工具平台专题.md](./23-Tool%20Gateway与工具平台专题.md)；Function Calling 基础见 [03-工具调用.md](./03-工具调用.md) |

---

## 一、一句话定义

**结构化输出是让模型输出可被程序直接消费的数据结构；约束解码是把模型每一步可生成的 token 限制在 schema/grammar 允许的集合内。**

它解决的问题是：

```text
自然语言输出:
  "我会先查订单，然后退款。"

结构化输出:
  {
    "steps": [
      {"id": "s1", "tool": "lookup_order", "args": {"order_id": "ORD-1001"}},
      {"id": "s2", "tool": "issue_refund", "depends_on": ["s1"]}
    ]
  }
```

生产 Agent 中，结构化输出是可靠执行的基础：

- plan 要可执行。
- tool args 要可校验。
- handoff contract 要可解析。
- state patch 要可合并。
- final answer 要可入库。
- eval result 要可统计。

---

## 二、为什么 prompt 约束不够

最弱的做法是：

```text
Please output valid JSON only.
```

失败方式很多：

| 失败 | 示例 |
|---|---|
| 包 Markdown | ```json ... ``` |
| 字段缺失 | 少了 `order_id` |
| 类型错误 | `"amount": "one hundred"` |
| enum 错误 | `"status": "done"` 但 schema 只允许 `completed` |
| 多余解释 | JSON 后面加一句 "Hope this helps" |
| 结构漂移 | 数组变对象 |
| 语义不合法 | JSON 合法，但 refund amount 超过订单金额 |

**核心判断**：prompt 是请求，不是约束。生产系统必须把"希望它对"变成"验证它对"。

---

## 三、结构化输出的五个层级

| 层级 | 做法 | 语法合法 | schema 合法 | 语义合法 | 适用 |
|---|---|---|---|---|---|
| L1 Prompt 约束 | 文本要求输出 JSON | 不保证 | 不保证 | 不保证 | demo |
| L2 JSON mode | 供应商保证合法 JSON | 较强 | 不保证 | 不保证 | 简单抽取 |
| L3 Tool / Function calling | 输出工具名和参数 | 强 | 中到强 | 不保证 | 工具调用 |
| L4 JSON Schema / Structured Outputs | 按 schema 输出 | 强 | 强 | 不保证 | 计划、结果、handoff |
| L5 Schema + semantic validator | schema 后再业务校验 | 强 | 强 | 强 | 生产系统 |

### 3.1 L1：Prompt 约束

适合低风险、失败可接受的场景。

```text
Return JSON only, no markdown.
```

不要用于：

- 支付、退款、权限变更。
- 状态补丁。
- Agent plan。
- 机器间协议。

### 3.2 L2：JSON mode

JSON mode 通常保证输出可被 JSON parser 解析，但不保证符合你的 schema。

```json
{"name": "Alice", "age": "twenty"}
```

这仍然可能是合法 JSON，但不是合法业务数据。

### 3.3 L3：Tool / Function calling

模型选择工具并输出参数：

```json
{
  "tool": "lookup_order",
  "arguments": {"order_id": "ORD-1001"}
}
```

工具参数 schema 比普通 JSON 输出更可靠，因为模型训练中见过大量 tool-use 样本，供应商 API 也会把工具调用作为特殊结构返回。

### 3.4 L4：JSON Schema / Structured Outputs

模型最终输出必须匹配 schema：

```json
{
  "type": "object",
  "properties": {
    "risk_level": {"type": "string", "enum": ["low", "medium", "high"]},
    "findings": {
      "type": "array",
      "items": {
        "type": "object",
        "properties": {
          "claim": {"type": "string"},
          "evidence_id": {"type": "string"}
        },
        "required": ["claim", "evidence_id"]
      }
    }
  },
  "required": ["risk_level", "findings"]
}
```

### 3.5 L5：Schema + 语义校验

Schema 只能保证形状，不能保证业务正确。

```json
{
  "order_id": "ORD-1001",
  "refund_amount": 500
}
```

如果订单金额是 120，这个 JSON 语法和 schema 都可能合法，但业务不合法。

所以生产要加 semantic validator：

```text
schema validation
  -> business validation
  -> policy validation
  -> evidence validation
  -> permission validation
```

---

## 四、两类 schema 不要混淆

### 4.1 Tool Args Schema

工具参数 schema 描述"模型想调用什么工具"：

```json
{
  "name": "issue_refund",
  "parameters": {
    "type": "object",
    "properties": {
      "order_id": {"type": "string"},
      "amount": {"type": "number"},
      "reason": {"type": "string", "enum": ["lost", "damaged", "duplicate"]}
    },
    "required": ["order_id", "amount", "reason"]
  }
}
```

它进入 Tool Gateway。

### 4.2 Final Output Schema

最终输出 schema 描述"任务结果是什么"：

```json
{
  "type": "object",
  "properties": {
    "status": {"type": "string", "enum": ["completed", "blocked", "failed"]},
    "summary": {"type": "string"},
    "evidence": {
      "type": "array",
      "items": {"type": "string"}
    }
  },
  "required": ["status", "summary", "evidence"]
}
```

它进入业务系统或前端。

### 4.3 State Patch Schema

多 Agent 或 runtime 场景还会有状态补丁 schema：

```json
{
  "op": "replace",
  "path": "/order/status",
  "value": "refunded",
  "evidence_ref": "tool_result:refund_001"
}
```

它进入 State Store，需要权限和冲突校验。

### 4.4 Eval Result Schema

评估器也要结构化：

```json
{
  "score": 0.82,
  "passed": true,
  "failure_modes": ["missing_citation"],
  "rationale": "The answer is mostly correct but one claim lacks evidence."
}
```

它进入 eval database。

---

## 五、约束解码的原理

普通生成：

```text
model logits
  -> sample from all vocabulary tokens
  -> token
```

约束解码：

```text
model logits
  -> grammar/schema state computes allowed tokens
  -> mask disallowed tokens
  -> sample from allowed tokens
  -> update grammar/schema state
```

它可以基于：

| 约束 | 示例 |
|---|---|
| JSON Schema | object、array、required、enum |
| CFG | 上下文无关文法 |
| FSM | 正则、简单 JSON 状态机 |
| Regex | 邮箱、日期、固定格式 |
| GBNF | llama.cpp 类 grammar |
| Tool schema | function arguments |

### 5.1 它能保证什么

约束解码通常能保证：

- JSON 语法合法。
- 字段类型合法。
- enum 合法。
- required 字段存在。
- 数组/对象结构合法。

### 5.2 它不能保证什么

它不能保证：

- 事实正确。
- 引用真实支持 claim。
- 金额符合业务规则。
- 用户有权限。
- 工具调用安全。
- 计划真的可执行。

所以约束解码要和 validator 组合。

---

## 六、Schema 设计原则

### 6.1 给模型友好的 schema

| 原则 | 说明 |
|---|---|
| 字段名语义明确 | `refund_amount` 比 `value` 好 |
| enum 少而清晰 | 不要给 30 个模糊选项 |
| 嵌套不要过深 | 2 到 3 层更稳 |
| required 不要过多 | 必填太多会增加失败和空字段 |
| description 要写边界 | 什么时候填、什么时候不填 |
| 数字写单位 | amount_usd、timeout_seconds |
| 避免 union 过复杂 | oneOf/anyOf 容易让模型混乱 |
| 输出证据引用 | 关键字段带 evidence_ref |

### 6.2 不要让模型生成可计算字段

如果字段可以由程序计算，不要让模型生成。

```json
{
  "items": [
    {"price": 10, "quantity": 2}
  ],
  "total": 20
}
```

`total` 应由程序计算，避免模型算错。

### 6.3 不要让模型生成权限结论

错误：

```json
{"can_refund": true}
```

更好的方式：

```json
{
  "requested_action": "issue_refund",
  "order_id": "ORD-1001",
  "amount": 120
}
```

权限由 Policy Engine 判断。

### 6.4 字段要可追责

对高风险输出，给每个关键字段加来源：

```json
{
  "risk": "high",
  "risk_reason": "Vendor SOC2 report expired.",
  "evidence_ref": "artifact:soc2_report:p3"
}
```

---

## 七、校验流水线

推荐生产流水线：

```text
raw model output
  -> parse JSON
  -> schema validation
  -> semantic validation
  -> policy validation
  -> evidence validation
  -> repair or reject
  -> persist / execute
```

### 7.1 Schema Validation

检查：

- required 字段。
- 类型。
- enum。
- 数组长度。
- 字符串格式。
- 额外字段。

### 7.2 Semantic Validation

检查业务规则：

| 规则 | 示例 |
|---|---|
| 金额范围 | refund <= order.amount |
| 时间关系 | start_time < end_time |
| 状态迁移 | shipped -> refunded 合法，canceled -> shipped 不合法 |
| 引用存在 | evidence_ref 必须指向 artifact |
| 计划可执行 | step depends_on 不成环 |
| 工具可用 | tool name 在 registry 里 |

### 7.3 Policy Validation

检查：

- 当前用户是否有权限。
- 当前 Agent 是否有 tool scope。
- 是否触发 HITL。
- 是否违反租户边界。
- 是否需要脱敏。

### 7.4 Evidence Validation

对 RAG、research、合规场景尤其重要：

```text
claim -> evidence_ref -> artifact span -> verifier
```

不能只要求模型输出引用，还要验证引用是否真的支持 claim。

---

## 八、Repair Loop

结构化输出失败后不要直接放弃，也不要无限重试。

### 8.1 Repair Prompt

把 validator error 反馈给模型：

```text
Your previous output failed validation.

Validation errors:
1. $.risk_level must be one of ["low", "medium", "high"], got "critical".
2. $.findings[0].evidence_id is required.

Return only corrected JSON matching the schema.
Do not change facts not mentioned by validation errors.
```

### 8.2 Repair 策略

| 错误 | 策略 |
|---|---|
| JSON parse 失败 | 允许修复 1 次 |
| schema 小错 | 回填字段级错误 |
| enum 错 | 给合法 enum |
| 缺 evidence | 要求补证据，补不了就降级 |
| 语义错误 | 不自动修复高风险字段，转 verifier/HITL |
| 权限错误 | 不修复，直接拒绝或转人工 |

### 8.3 重试上限

建议：

```text
parse/schema repair: max 1-2
semantic repair: max 1
policy violation: 0
high-risk action: HITL
```

无限 repair loop 会烧钱并掩盖 schema 设计问题。

---

## 九、结构化 Plan

Agent 计划是最典型的结构化输出。

### 9.1 Plan Schema

```json
{
  "goal": "Compare three vendors and recommend one.",
  "steps": [
    {
      "id": "s1",
      "kind": "tool",
      "tool": "web_search",
      "args": {"query": "Vendor A SOC2 report"},
      "depends_on": [],
      "success_criteria": ["At least one reliable source found"]
    },
    {
      "id": "s2",
      "kind": "model",
      "depends_on": ["s1"],
      "success_criteria": ["Extract security facts with evidence"]
    }
  ],
  "final_success_criteria": [
    "All recommendations have evidence",
    "No unsupported claims"
  ]
}
```

### 9.2 Plan Validator

Plan 不能只做 JSON parse：

| 校验 | 示例 |
|---|---|
| DAG 校验 | `depends_on` 不成环 |
| step id 校验 | 引用的 step 存在 |
| tool 校验 | tool 在 registry 中 |
| args 校验 | args 符合 tool schema |
| budget 校验 | step 数、成本、时间不超限 |
| risk 校验 | 高风险工具需要审批 |
| completeness 校验 | final criteria 能被 steps 支撑 |

---

## 十、状态补丁与多 Agent

多 Agent 不应该互相传自然语言结论后直接改共享状态，而应提交 state patch。

```json
{
  "patch_id": "patch_123",
  "writer": "risk_agent",
  "ops": [
    {
      "op": "add",
      "path": "/risks/-",
      "value": {
        "severity": "high",
        "description": "SOC2 report expired",
        "evidence_ref": "artifact:soc2:p3"
      }
    }
  ]
}
```

State Store 校验：

- writer 是否有权限写 `/risks`。
- value 是否符合 schema。
- evidence_ref 是否存在。
- 是否与当前版本冲突。
- 是否需要 reviewer。

---

## 十一、版本治理

Schema 一旦进入生产，就和 API 一样需要版本。

### 11.1 版本字段

```json
{
  "schema_version": "risk_report.v2",
  "risk_level": "high",
  "findings": []
}
```

### 11.2 兼容策略

| 变更 | 兼容性 |
|---|---|
| 新增 optional 字段 | 通常兼容 |
| 新增 required 字段 | 破坏兼容 |
| enum 新增值 | 下游可能不兼容 |
| 字段改名 | 破坏兼容 |
| 类型变更 | 破坏兼容 |
| 嵌套结构变更 | 通常破坏兼容 |

### 11.3 发布流程

```text
schema v2 draft
  -> offline eval
  -> shadow mode parse
  -> dual-write v1/v2
  -> downstream migration
  -> canary
  -> remove v1
```

---

## 十二、性能与成本

结构化输出会影响性能。

| 影响 | 原因 |
|---|---|
| prompt 更长 | schema 注入 token 多 |
| decode 可能更慢 | 每步要计算合法 token mask |
| batch 效率下降 | 不同请求 grammar state 不同 |
| retry 减少 | schema 成功率提高 |
| 端到端可能更快 | 少了 parse failure 和 repair |

**看总账**：

```text
普通输出总成本 =
  normal_generation + parse_failures * repair_cost + bad_action_cost

约束输出总成本 =
  constrained_generation + fewer_repairs + fewer_bad_actions
```

如果原始失败率高，约束解码即使单次慢，也可能端到端更快。

---

## 十三、主流实现

| 实现 | 能力 |
|---|---|
| OpenAI Structured Outputs | JSON Schema 结构化输出、工具参数严格模式 |
| OpenAI Agents SDK output_type | Agent final output schema |
| Anthropic Tool Use | `input_schema` 定义工具参数 |
| Gemini Structured Output | response schema / function calling |
| Pydantic / Zod | 应用侧 schema 定义与校验 |
| Guardrails AI | 输出校验和修复 |
| Outlines | 开源 grammar/JSON schema 约束生成 |
| XGrammar | 高性能 grammar-guided generation |
| vLLM guided decoding | 服务端 guided decoding |
| SGLang structured output | 面向 agent/program workload 的结构化生成 |
| llama.cpp grammar | GBNF grammar 约束 |

---

## 十四、场景设计：供应商风险评估

### 14.1 输出 schema

```json
{
  "schema_version": "vendor_risk.v1",
  "vendor_name": "Acme SaaS",
  "overall_risk": "medium",
  "findings": [
    {
      "category": "security",
      "severity": "high",
      "claim": "SOC2 report expired in 2025.",
      "evidence_ref": "artifact:soc2_report:p2",
      "confidence": 0.91
    }
  ],
  "open_questions": [
    "Need updated SOC2 report."
  ],
  "recommended_action": "request_more_info"
}
```

### 14.2 Validator

```text
1. schema_version must be supported
2. overall_risk in [low, medium, high]
3. each finding has evidence_ref
4. evidence_ref points to accessible artifact
5. high severity finding must have confidence >= 0.8
6. recommended_action in allowed workflow actions
7. if recommended_action=approve and high finding exists, require human approval
```

### 14.3 Failure Handling

| 失败 | 处理 |
|---|---|
| 缺 evidence_ref | repair once |
| evidence 不支持 claim | mark unsupported, route to verifier |
| recommended_action 越权 | reject and explain |
| schema_version unknown | migrate or reject |

---

## 十五、反模式

| 反模式 | 问题 |
|---|---|
| 只写 "输出 JSON" | 不可靠 |
| 只做 JSON parse | schema 和语义仍可能错 |
| schema 过大过深 | 模型混乱，约束解码变慢 |
| 让模型生成权限结论 | 安全边界错误 |
| 让模型生成可计算字段 | 增加错误面 |
| repair 无限重试 | 烧钱且掩盖设计问题 |
| schema 无版本 | 下游升级困难 |
| 关键结论无 evidence_ref | 无法审计和校验 |
| 把 structured output 当安全措施 | 格式合法不代表安全 |

---

## 十六、自测清单

- [ ] 能区分 JSON mode、tool calling、structured output、constrained decoding。
- [ ] 能解释为什么 schema validation 不等于 semantic validation。
- [ ] 能设计 tool args schema、final output schema、state patch schema。
- [ ] 能设计 repair loop 和重试上限。
- [ ] 能解释约束解码为什么可能变慢。
- [ ] 能判断哪些字段不该由模型生成。
- [ ] 能设计 schema versioning 和迁移策略。
- [ ] 能为关键输出加 evidence_ref。
- [ ] 能解释结构化输出在 Agent Runtime、Tool Gateway、Eval Harness 中的位置。

---

## 十七、高频问题

### Q1：JSON mode 和 Structured Outputs 有什么区别？

JSON mode 主要保证输出是合法 JSON，不保证字段符合你的 schema。Structured Outputs 会按照 JSON Schema 或类似结构约束输出，更适合生产系统。但两者都不能保证业务语义正确，所以还要应用侧 validator。

### Q2：约束解码是不是一定更快？

不是。它可能让单次 decode 变慢，因为每步要计算合法 token。但它减少解析失败、schema 修复和错误工具调用，所以端到端可能更快。要按真实 workload 评估。

### Q3：Function Calling 是不是就不需要校验了？

仍然需要。模型可能选错工具、填错参数、触发越权操作。Function Calling 只是让参数结构更稳定，不能替代 Tool Gateway 的 schema、权限、幂等、HITL 和审计。

### Q4：为什么 schema 不要太复杂？

复杂 schema 会增加模型理解难度，也会让 grammar/FSM 状态复杂，降低吞吐。生产里应把可计算字段交给程序，把复杂嵌套拆成多步输出或多个小 schema。

### Q5：如何处理 schema 升级？

把 schema 当 API 管理：版本号、兼容性规则、离线 eval、shadow parse、dual-write、canary、下游迁移。不要静默修改字段含义。

---

## 十八、关联阅读

- [02-Prompt工程与推理范式.md](./02-Prompt工程与推理范式.md)：结构化输出三种路线。
- [03-工具调用.md](./03-工具调用.md)：工具 schema 与 function calling。
- [14-Agent工程细节与可靠执行.md](./14-Agent工程细节与可靠执行.md)：结构化 plan 与校验。
- [19-推理加速底层系统.md](./19-推理加速底层系统.md)：约束解码性能。
- [20-Agent Runtime专题.md](./20-Agent%20Runtime专题.md)：runtime 如何消费结构化动作。

---

## 十九、官方资料入口

- OpenAI Structured Outputs: <https://platform.openai.com/docs/guides/structured-outputs>
- OpenAI Function Calling: <https://platform.openai.com/docs/guides/function-calling>
- OpenAI Agents SDK output types: <https://openai.github.io/openai-agents-python/agents/#output-types>
- Anthropic Tool Use: <https://docs.anthropic.com/en/docs/agents-and-tools/tool-use/overview>
- Gemini Structured Output: <https://ai.google.dev/gemini-api/docs/structured-output>
- vLLM Guided Decoding: <https://docs.vllm.ai/en/latest/features/structured_outputs.html>
- SGLang Structured Outputs: <https://docs.sglang.ai/advanced_features/structured_outputs.html>
- Outlines: <https://dottxt-ai.github.io/outlines/>
- XGrammar: <https://xgrammar.mlc.ai/docs/>
