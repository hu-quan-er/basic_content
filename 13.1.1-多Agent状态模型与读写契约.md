# 多 Agent 状态模型与读写契约

> 目标：把“多 Agent 状态管理”拆到底层工程对象：状态分层、状态目录、上下文视图、读写生命周期、一致性等级、冲突合并和存储边界。本篇只讨论内部状态模型，不讨论具体框架 API；框架落地见 [13.1.3-主流多Agent框架状态管理实现.md](./13.1.3-主流多Agent框架状态管理实现.md)。

---

## 一、核心判断

多 Agent 系统真正共享的不是完整聊天记录，而是一个有 schema、有版本、有来源、有权限、有生命周期的任务状态空间。

如果没有状态模型，系统会退化成：

- 每个 Agent 都凭自己看到的上下文猜当前进度。
- 事实、假设、计划、审批、产物混在一段自然语言里。
- 下游 Agent 不知道上游结论是不是已验证。
- 多个 Agent 并发写同一字段，最终状态无法解释。
- 出错后只能翻聊天记录，不能回放状态变化。

所以第一步不是选框架，而是定义：

```text
哪些信息是 state？
哪些信息只是 context？
哪些信息可以成为 memory？
哪些信息必须沉淀为 artifact？
哪些更新只是 proposal？
谁有权把 proposal 变成 committed state？
```

---

## 二、状态、上下文、记忆、工件、轨迹

| 概念 | 定义 | 是否跨 Agent 同步 | 典型错误 |
|------|------|-------------------|----------|
| State | 当前任务可机器读写的事实快照 | 是，但要按字段授权 | 把聊天历史当 state |
| Context | 某个 Agent 本轮推理需要的输入视图 | 只同步子集 | 给所有 Agent 广播全量上下文 |
| Memory | 跨任务复用的长期事实、偏好、经验 | 谨慎同步 | 把 memory 当当前任务真相 |
| Message | Agent 之间的一次通信事件 | 通常 append-only | 用 message 覆盖业务状态 |
| Artifact | 可引用、可复查、可版本化的产物 | 是，优先传引用 | 把大文件全文塞进 prompt |
| Trace | 执行过程与调用链记录 | 用于审计，不全量喂模型 | 把 trace 当业务数据库 |
| Policy Context | 身份、权限、租户、合规边界 | 必须传递，但不可由模型改写 | 让模型决定自己有没有权限 |

一句话区分：

```text
State = 系统当前相信什么
Context = 本轮让模型看到什么
Memory = 未来可能复用什么
Artifact = 可复查的证据和产物
Trace = 为什么会走到这里
Policy = 谁能看和谁能改
```

---

## 三、七层状态模型

推荐把多 Agent 状态拆成七层：

| 层级 | 作用 | 典型存储 | 读写者 |
|------|------|----------|--------|
| Run / Task State | 当前任务结构化真相 | SQL / KV / checkpoint | Orchestrator、授权 Agent |
| Agent Local State | 单 Agent 私有中间状态 | runtime / session | 该 Agent 自己 |
| Shared Working Memory | 当前任务内共享工作台 | Redis / Postgres / blackboard | 同一任务内 Agent |
| Event Log | 状态变化和通信事件 | append-only log | runtime 写，多方读 |
| Artifact Store | 文件、报告、代码、数据、证据 | object store / git / blob | producer 写，consumer 读 |
| Long-term Memory | 跨任务经验和用户偏好 | vector DB / graph / memory service | 受权限控制的 Agent |
| Policy / Identity Context | 身份、权限、租户、合规策略 | IAM / policy engine | system / gateway |

参考结构：

```text
User Goal
   |
   v
Orchestrator / Supervisor
   |
   +-- Run / Task State        <- 当前任务的结构化真相
   +-- Event Log               <- 谁在何时做了什么
   +-- Artifact Store          <- 产物和证据
   +-- Shared Working Memory   <- 短期共享事实
   +-- Long-term Memory        <- 可召回历史
   +-- Policy Context          <- 身份、权限、合规
   |
   +-- Agent A Local Context   <- 私有 prompt / scratchpad / tool state
   +-- Agent B Local Context
   +-- Agent C Local Context
```

关键原则：

- Agent Local State 默认不共享。
- Run / Task State 是当前任务真相源。
- Artifact Store 存证据，不存“谁该做什么”。
- Long-term Memory 只辅助召回，不能覆盖当前任务事实。
- Event Log 用来审计和回放，不直接替代查询态。

---

## 四、State Catalog：状态目录

很多系统有 state store，但没有 state catalog。结果是“所有人都能写所有字段”，最终状态自然不可控。

State Catalog 至少回答：

- 字段语义是什么？
- 字段 owner 是谁？
- 谁能读？
- 谁能写？
- 来源是什么？
- 一致性等级是什么？
- 什么时候过期？
- 能否跨组织传递？
- 冲突如何处理？

示例：

| 字段 | 类型 | Owner | 读者 | 写者 | 来源 | 可见性 | 一致性 | 过期策略 | 合并策略 |
|------|------|-------|------|------|------|--------|--------|----------|----------|
| `/goal` | task_goal | user_proxy | all task agents | user_proxy、supervisor | 用户输入 | task | strong | task end | replace by owner |
| `/requirements` | structured_facts | product_agent | planner、executor、verifier | product_agent | 用户确认 | task | strong | versioned | owner only |
| `/facts` | evidence_backed_facts | research_agent | writer、verifier | research_agent、verifier | artifact | task | eventual | TTL | append + verify |
| `/risks` | risk_register | security/legal | supervisor、writer | security_agent、legal_agent | analysis + evidence | task | eventual | task end | reducer |
| `/approval_status` | decision | human/supervisor | all task agents | approval service | workflow | task | strong | immutable | append-only |
| `/messages` | event_stream | runtime | scoped agents | runtime | agent events | scoped | append-only | retention policy | append |
| `/artifacts` | references | artifact service | scoped agents | producer agents | object store | scoped | versioned | retention policy | append |
| `/memory_candidates` | memory_pending | memory service | verifier | scoped agents | task outcome | tenant | review required | TTL/review | pending queue |

### 4.1 字段命名建议

使用能表达领域语义的路径，而不是宽泛的 `data`、`notes`、`context`：

```text
/task/goal
/task/constraints
/task/status
/requirements/confirmed
/facts/verified
/facts/candidates
/risks/open
/risks/resolved
/approvals/pending
/approvals/history
/artifacts/references
/memory/candidates
/handoffs/active
```

### 4.2 字段元数据

每个可共享状态字段建议带元数据：

```json
{
  "path": "/facts/verified/fact_001",
  "value": "供应商已提供 SOC2 Type II 报告",
  "owner": "security_agent",
  "writer": "vendor_research_agent",
  "source": "artifact:soc2_report_meta",
  "confidence": 0.95,
  "visibility": "task",
  "version": 12,
  "created_at": "2026-06-07T10:00:00Z",
  "expires_at": "2026-09-07T10:00:00Z"
}
```

没有 owner、source、version 的共享状态，很快会变成不可治理的自然语言堆积。

---

## 五、Context View：每个 Agent 看到的是状态投影

不要把 global state 原样塞给每个 Agent，而是为每个 Agent 生成 Context View。

生成流程：

```text
Global State
  -> select: 选择该 Agent 需要的字段
  -> filter: 按权限、租户、隐私、工具范围过滤
  -> retrieve: 从 artifact/memory 中召回相关片段
  -> summarize: 对长内容做任务相关摘要
  -> format: 转成模型输入结构
  -> budget: 按 token/latency/cost 截断
  -> tag: 标注来源、版本、可信度
```

示例：

| Agent | 应看到 | 不应看到 |
|-------|--------|----------|
| Research Agent | 目标、公开约束、已有资料、待验证问题 | 用户私密审批链、财务预算明细 |
| Finance Agent | 预算、成本中心、采购规则、相关 artifact | research scratchpad、法律内部意见 |
| Legal Agent | 合同条款、风险清单、审批要求 | 工资、个人隐私、无关聊天历史 |
| Writer Agent | 已验证 facts、artifact 引用、报告结构要求 | 未验证外部网页原文、工具凭证 |
| Verifier Agent | 候选结论、证据链、state patch | 其他 Agent 的私有 prompt |

Context View 本身也要被记录：

```json
{
  "context_view_id": "ctx_view_writer_001",
  "task_id": "task_001",
  "agent": "writer_agent",
  "state_version": 18,
  "included_fields": ["/goal", "/facts", "/risks", "/artifacts"],
  "excluded_fields": ["/private_notes", "/budget/raw"],
  "artifact_refs": ["artifact:research_notes_v3"],
  "memory_refs": ["memory:tenant_style_guide"],
  "token_budget": 12000
}
```

这样审计时可以回答：

```text
这个 Agent 当时看到了哪些事实？
哪些字段被过滤掉？
它基于哪个 state_version 写入？
最终错误是否来自 stale context？
```

---

## 六、状态读写生命周期

Agent 写共享状态不要直接 commit，而应提交 proposal。

推荐生命周期：

```text
read state view
  -> reason / act
  -> propose patch
  -> schema validate
  -> permission check
  -> evidence check
  -> conflict detection
  -> verifier / human approval if needed
  -> commit
  -> publish event
  -> refresh context views
```

对应事件：

| 阶段 | 事件 | 说明 |
|------|------|------|
| 读取 | `state.view.created` | 记录 Agent 看到的上下文投影 |
| 提议 | `state.patch.proposed` | Agent 提交结构化更新 |
| 校验 | `state.patch.validated` | schema、权限、证据检查通过 |
| 拒绝 | `state.patch.rejected` | 记录拒绝原因 |
| 合并 | `state.patch.merged` | reducer 或事务合并成功 |
| 提交 | `state.committed` | state_version 前进 |
| 发布 | `state.updated` | 通知订阅 Agent 刷新上下文 |
| 失效 | `context.view.invalidated` | 旧上下文不可再用于写入 |

黄金规则：

> Agent 只提交 proposal；系统负责 validation、authorization、merge、commit。

---

## 七、State Patch 合同

生产系统里，状态更新最好是结构化 patch，而不是一句“我完成了”。

```json
{
  "patch_id": "patch_001",
  "task_id": "task_001",
  "base_state_version": 14,
  "writer_agent": "security_agent",
  "visibility": "task",
  "confidence": 0.84,
  "ttl": "30d",
  "evidence_refs": ["artifact:scan_report_002"],
  "operations": [
    {
      "op": "replace",
      "path": "/subtasks/security_review/status",
      "value": "completed"
    },
    {
      "op": "add",
      "path": "/risks/open/-",
      "value": {
        "id": "risk_007",
        "severity": "low",
        "description": "VPN group approval delayed by one day"
      }
    }
  ]
}
```

系统接收 patch 后至少检查：

- `base_state_version` 是否过旧。
- `writer_agent` 是否有权写这些路径。
- `operations` 是否符合 schema。
- `evidence_refs` 是否存在且可读。
- 是否触发审批或 verifier。
- 是否与已有状态冲突。

---

## 八、字段级所有权与可见性

### 8.1 字段级所有权

| 字段 | 可写 Agent | 审批要求 | 冲突策略 |
|------|------------|----------|----------|
| `/requirements` | product_agent、user_proxy | 用户确认 | 人工优先 |
| `/risk_register` | security_agent、legal_agent | 高风险需审批 | append + verifier |
| `/it_account/status` | it_agent | 无 | last event wins |
| `/budget` | finance_agent | 用户或经理审批 | 禁止自动覆盖 |
| `/final_answer` | supervisor | 完整性检查 | supervisor arbitration |

### 8.2 可见性级别

| 可见性 | 含义 | 示例 |
|--------|------|------|
| private | 只给单个 Agent | scratchpad、临时分析 |
| team | 同一 Agent 团队可见 | 项目事实、任务计划 |
| task | 当前任务可见 | 子任务状态、工件引用 |
| tenant | 同一租户可见 | 企业偏好、组织规则 |
| public | 可以跨组织或公开 | Agent Card、公开能力 |

默认策略：

```text
private by default, explicit share by contract
```

---

## 九、一致性等级与冲突合并

### 9.1 一致性等级

| 一致性等级 | 适用字段 | 策略 |
|------------|----------|------|
| Strong | 审批、预算、权限、不可逆工具动作 | 事务、锁、人工确认、版本检查 |
| Causal | handoff、任务依赖、计划执行顺序 | event log、dependency id、happens-before |
| Eventual | facts、risks、候选方案、研究发现 | append + reducer + verifier |
| Best-effort | 进度提示、心跳、临时摘要 | last-write-wins |
| Immutable | 已发布 artifact、审计事件、审批记录 | append-only、版本化、禁止覆盖 |

### 9.2 常见冲突

| 冲突 | 示例 | 解决思路 |
|------|------|----------|
| 写写冲突 | 两个 Agent 同时更新 `status` | version check + retry |
| 事实冲突 | Research Agent 说价格 100，Sales Agent 说价格 120 | evidence + verifier |
| 计划冲突 | Planner A 选路线 1，Planner B 选路线 2 | supervisor arbitration |
| 权限冲突 | Agent 想修改预算但无权限 | policy deny |
| 时间冲突 | Agent 基于旧状态执行 | stale read detection |
| 记忆冲突 | 长期记忆与当前任务事实不一致 | task state 优先 |

### 9.3 Reducer 示例

Reducer 规定同一字段收到多个更新时如何合并。

```python
def merge_risks(old: list[dict], new: list[dict]) -> list[dict]:
    by_id = {item["id"]: item for item in old}
    for item in new:
        existing = by_id.get(item["id"])
        if existing is None:
            by_id[item["id"]] = item
            continue

        by_id[item["id"]] = {
            **existing,
            **item,
            "evidence_refs": sorted(
                set(existing.get("evidence_refs", []) + item.get("evidence_refs", []))
            ),
            "confidence": max(existing.get("confidence", 0), item.get("confidence", 0)),
        }
    return list(by_id.values())
```

适合 reducer 的字段：

- `messages`
- `risks`
- `facts.candidates`
- `artifacts`
- `open_questions`

不适合 reducer 的字段：

- `budget`
- `approval_status`
- `permissions`
- `final_decision`
- `external_side_effect_status`

---

## 十、存储边界

一个数据库解决不了所有状态问题。

| 存储 | 放什么 | 不放什么 |
|------|--------|----------|
| Relational DB | 当前 task state、字段权限、任务索引 | 大文件、完整 trace |
| Event Log | state patch、handoff、审批、工具副作用 | 当前查询热路径的唯一来源 |
| Object Store | artifact、报告、数据文件、代码包 | 高频小字段 |
| Vector / Memory Store | 可召回经验、偏好、知识片段 | 当前任务强一致状态 |
| Trace System | LLM/tool/agent 调用链路 | 业务真相 |
| Cache | context view、短期检索结果 | 不可丢的状态 |

常见错误：

- 把所有东西都塞进 `messages`。
- 把 artifact 全文塞进 checkpoint。
- 把当前任务状态放进向量库。
- 把 trace 当业务数据库。
- 把 cache 当真相源。

---

## 十一、落地清单

- [ ] 已区分 State / Context / Memory / Artifact / Trace。
- [ ] 已定义 State Catalog。
- [ ] 每个字段都有 owner、writer、reader、visibility。
- [ ] 每个 Agent 通过 Context View 获取上下文。
- [ ] 记录 `context_view_id` 和 `state_version`。
- [ ] Agent 只提交 State Patch，不直接 commit。
- [ ] Patch 有 schema、权限、证据、冲突检查。
- [ ] 高风险字段有 strong consistency 和人工审批。
- [ ] append-only 字段有 reducer。
- [ ] 长期 memory 有 scope、provenance、TTL、撤销机制。
- [ ] Artifact 版本化，不把大对象塞进 prompt。
- [ ] Event Log 可回放关键状态变化。

---

## 十二、核心要点

- 先设计状态目录，再接入框架。
- 每个 Agent 看到的是 Context View，不是 global state。
- Agent 写的是 proposal，系统提交的是 committed state。
- 当前任务强一致状态不要放进 memory store。
- Artifact 是证据，memory 是召回，trace 是审计，三者不能混用。
