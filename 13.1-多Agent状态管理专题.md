# 多 Agent 状态管理专题

> 目标：建立多 Agent 状态管理的总览框架，并提供分篇阅读路径。本文是入口文档；细节已拆到 5 篇子专题，避免单篇过长。

---

## 本文职责边界

| 本文负责 | 本文不展开 |
|---|---|
| 多 Agent 状态管理专题入口、核心原则、阅读路径和问题地图 | 具体读写契约见 [13.1.1](./13.1.1-多Agent状态模型与读写契约.md)；A2A handoff 见 [13.1.2](./13.1.2-A2A上下文同步与Handoff设计.md)；框架实现见 [13.1.3](./13.1.3-多Agent主流框架状态管理实现.md)；生产治理见 [13.1.4](./13.1.4-多Agent生产场景与治理清单.md)；端到端代码见 [13.1.5](./13.1.5-多Agent状态同步端到端代码示例.md) |

---

## 一、先给结论

多 Agent 状态管理的核心不是“让所有 Agent 共享所有上下文”，而是：

1. **把状态分层**：任务状态、私有上下文、共享工作记忆、工件、事件日志、长期记忆、权限上下文不能混在一起。
2. **只同步必要信息**：跨 Agent 同步的是任务目标、结构化事实、约束、工件引用、未决问题、责任边界，而不是完整 prompt、chain-of-thought、内部 memory。
3. **把 message 当通信，把 artifact 当证据，把 state patch 当事实更新，把 trace 当审计**。
4. **跨组织协作用 A2A 的 Task / Message / Artifact；同一应用内部编排用 LangGraph 这类显式状态图更自然**。
5. **冲突必须被设计出来**：谁能写哪些字段、并发写如何合并、旧上下文如何失效、错误记忆如何撤销，都要有机制。

一句话：

> 多 Agent 系统真正共享的不是“聊天记录”，而是一个有 schema、有版本、有来源、有权限、有生命周期的任务状态空间。

---

## 二、分篇阅读路径

| 文档 | 重点问题 | 适合什么时候读 |
|------|----------|----------------|
| [13.1.1-多Agent状态模型与读写契约.md](./13.1.1-多Agent状态模型与读写契约.md) | State / Context / Memory / Artifact / Trace 怎么拆；State Catalog、Context View、State Patch、Reducer 怎么设计 | 设计内部多 Agent 状态底座时 |
| [13.1.2-A2A上下文同步与Handoff设计.md](./13.1.2-A2A上下文同步与Handoff设计.md) | A2A 下 Task / Message / Artifact 如何承载上下文；跨组织如何做 handoff 和多轮同步 | 做 A2A 互操作或跨系统 Agent 调用时 |
| [13.1.3-主流多Agent框架状态管理实现.md](./13.1.3-多Agent主流框架状态管理实现.md) | A2A、LangGraph、AutoGen、CrewAI、OpenAI Agents SDK、Google ADK、LlamaIndex、Semantic Kernel、MCP 的状态原语和示例 | 选型或落地具体框架时 |
| [13.1.4-多Agent生产场景与治理清单.md](./13.1.4-多Agent生产场景与治理清单.md) | 企业入职、Deep Research、代码 Agent、客服、供应商尽调的状态模型、治理和反模式 | 做系统设计题或生产方案时 |
| [13.1.5-多Agent状态同步端到端代码示例.md](./13.1.5-多Agent状态同步端到端代码示例.md) | 一个完整可运行的 Python 示例：StateStore、Context View、Patch 校验、Artifact、Event Log、A2A input-required、远程结果导入 | 觉得框架片段看不懂、想先看到完整逻辑时 |

建议阅读顺序：

```text
先读本文总览
  -> 13.1.1 建状态模型
  -> 13.1.2 理解 A2A 边界和 handoff
  -> 13.1.5 跑通端到端代码示例
  -> 13.1.3 对照主流实现
  -> 13.1.4 看生产案例和检查清单
```

---

## 三、为什么多 Agent 状态管理很难

单 Agent 的上下文问题通常是“窗口不够、记忆不准、工具结果太长”。多 Agent 会额外引入：

| 问题 | 典型表现 | 后果 |
|------|----------|------|
| 状态碎片化 | 每个 Agent 只知道自己那段对话 | 重复工作、结论互相矛盾 |
| 上下文漂移 | Agent B 看到的是 Agent A 的旧摘要 | 基于过时事实继续推理 |
| 责任不清 | 多个 Agent 都在改同一个字段 | 最终状态不可解释 |
| Token 爆炸 | 把全量历史广播给所有 Agent | 延迟、成本、幻觉同时上升 |
| 私密泄露 | 把内部 prompt、工具凭证、用户隐私传给其他 Agent | 安全与合规事故 |
| 记忆污染 | 错误事实写入共享 memory | 错误被系统性放大 |
| 部分失败 | 一个子 Agent 超时，另一个已基于旧结果继续执行 | 恢复困难 |
| 审计困难 | 只保留自然语言聊天记录 | 无法回答“谁在什么时候依据什么改了什么” |

进入生产环境后，状态管理会从“框架细节”变成“系统主干”。

---

## 四、全局概念图

```text
User Goal
   |
   v
Orchestrator / Supervisor
   |
   +-- Run / Task State        <- 当前任务的结构化真相
   +-- Event Log               <- 谁在何时做了什么
   +-- Artifact Store          <- 产物和证据
   +-- Shared Working Memory   <- 短期共享事实
   +-- Long-term Memory        <- 可召回历史
   +-- Policy Context          <- 身份、权限、合规
   |
   +-- Agent A Context View    <- 只给 A 需要的状态投影
   +-- Agent B Context View
   +-- Agent C Context View
```

核心对象：

| 对象 | 定位 | 详细说明 |
|------|------|----------|
| State | 系统当前相信什么 | [13.1.1](./13.1.1-多Agent状态模型与读写契约.md) |
| Context View | 本轮让某个 Agent 看到什么 | [13.1.1](./13.1.1-多Agent状态模型与读写契约.md) |
| State Patch | Agent 提交的结构化状态更新 | [13.1.1](./13.1.1-多Agent状态模型与读写契约.md) |
| Handoff Contract | Agent 交接任务时的目标、约束、证据和返回格式 | [13.1.2](./13.1.2-A2A上下文同步与Handoff设计.md) |
| Artifact | 可复查、可版本化的产物 | [13.1.2](./13.1.2-A2A上下文同步与Handoff设计.md) |
| Framework State | 具体框架里的状态原语和恢复机制 | [13.1.3](./13.1.3-多Agent主流框架状态管理实现.md) |
| Governance | 安全、审批、可观测、反模式 | [13.1.4](./13.1.4-多Agent生产场景与治理清单.md) |

---

## 五、A2A 下的关键边界

A2A 让独立 Agent 系统互操作，但它不等于内部状态同步数据库。

A2A 适合同步：

- Task 生命周期。
- Message 和结构化 DataPart。
- Artifact 和产物引用。
- input-required 这类多轮状态。
- streaming / push 进度。
- correlation id / trace id / external task id。

A2A 不应该同步：

- 远程 Agent 的 system prompt。
- chain-of-thought。
- 内部 memory。
- 工具凭证。
- 未授权的用户隐私。
- 调用方全量上下文。

一句话：

> A2A 同步的是跨边界任务合同，不是把两个 Agent 的内部上下文揉成一个上下文。

详细展开见 [13.1.2-A2A上下文同步与Handoff设计.md](./13.1.2-A2A上下文同步与Handoff设计.md)。

---

## 六、主流实现怎么选

| 场景 | 推荐主机制 | 原因 |
|------|------------|------|
| 同一应用内部，多 Agent 状态复杂 | LangGraph / workflow engine | 显式 typed state、reducer、checkpoint |
| 跨组织、跨厂商 Agent 调用 | A2A | 标准 Task / Message / Artifact 边界 |
| 对话式 multi-agent brainstorming | AutoGen | group chat / team state 更自然 |
| 角色化业务流程、快速原型 | CrewAI | Agent/Task/Crew 抽象简单 |
| OpenAI 技术栈内 handoff | OpenAI Agents SDK | handoff、context、guardrail、trace 集成 |
| Google Gemini / Cloud 生态 | Google ADK | session state、events、artifacts、memory |
| RAG + 多阶段 workflow | LlamaIndex AgentWorkflow | workflow context、events、retrieval 集成 |
| .NET 企业应用 | Semantic Kernel Agents | AgentGroupChat、ChatHistory、Kernel plugins |
| 工具和资源接入 | MCP | 标准化 tools/resources/prompts |

如果还没有形成完整状态流的直觉，先运行 [examples/multi_agent_state_demo.py](./examples/multi_agent_state_demo.py)，并阅读 [13.1.5-多Agent状态同步端到端代码示例.md](./13.1.5-多Agent状态同步端到端代码示例.md)。之后再看 [13.1.3-主流多Agent框架状态管理实现.md](./13.1.3-多Agent主流框架状态管理实现.md)，会更容易判断每个框架到底帮你解决了哪一层。

---

## 七、生产设计最小清单

- [ ] 已定义 State / Context / Memory / Artifact / Trace 的边界。
- [ ] 已建立 State Catalog，字段有 owner、reader、writer、visibility。
- [ ] 每个 Agent 通过 Context View 获取上下文。
- [ ] Agent 写状态必须提交 State Patch。
- [ ] 高风险字段有权限、审批和 verifier。
- [ ] Artifact 版本化，关键结论引用 artifact。
- [ ] Handoff 使用结构化 contract，不转发全量聊天记录。
- [ ] A2A 只传最小必要上下文，不传内部 memory/prompt。
- [ ] 有 event log 和 trace，能回答“谁在何时依据什么改了什么”。
- [ ] 长期 memory 有 provenance、scope、TTL、撤销机制。

---

## 八、核心术语

| 术语 | 含义 |
|------|------|
| Global State | 多 Agent 共享的当前任务结构化真相 |
| Local Context | 单个 Agent 私有上下文和中间状态 |
| Context View | 从 global state 为某个 Agent 生成的最小必要视图 |
| Shared Working Memory | 当前任务内共享的短期事实和工作台 |
| Long-term Memory | 跨任务复用的经验、偏好、事实 |
| Artifact | 可引用、可复查、可版本化的任务产物 |
| Event Log | append-only 的状态变化和通信记录 |
| State Patch | Agent 提交的结构化状态更新 |
| Reducer | 多个更新写入同一字段时的合并函数 |
| Handoff | Agent 之间的控制权或任务交接 |
| Handoff Contract | 结构化交接协议，包括目标、约束、证据、返回格式 |
| Provenance | 信息来源、写入者、时间、证据链 |
| Stale Write | 基于旧状态版本提交的写入 |
| Conflict Policy | 冲突发生时的合并、仲裁或审批规则 |
| Visibility Scope | private / team / task / tenant / public 等可见性 |
| A2A Task | A2A 协议里的长任务工作单元 |
| MCP Resource | MCP 暴露给模型/Agent 的上下文资源 |

---

## 九、延伸阅读

- A2A Core Concepts：<https://a2a-protocol.org/latest/topics/key-concepts/>
- A2A Protocol Definition：<https://a2a-protocol.org/latest/specification/>
- LangGraph Persistence：<https://docs.langchain.com/oss/python/langgraph/persistence>
- AutoGen Managing State：<https://microsoft.github.io/autogen/dev/user-guide/agentchat-user-guide/tutorial/state.html>
- CrewAI Memory：<https://docs.crewai.com/en/concepts/memory>
- OpenAI Agents SDK Handoffs：<https://openai.github.io/openai-agents-python/handoffs/>
- Google ADK Sessions：<https://google.github.io/adk-docs/sessions/>
- MCP Architecture：<https://modelcontextprotocol.io/docs/learn/architecture>
- 本仓库相关文档：[06-多Agent协作.md](./06-多Agent协作.md)、[13-A2A与Agent互操作协议.md](./13-A2A与Agent互操作协议.md)、[16.2-Agent Loop上下文管理.md](./16.2-Agent%20Loop上下文管理.md)、[08.1-生产级Agent应用工程.md](./08.1-生产级Agent应用工程.md)
