# Buddy 架构分层与 Agent 运行时

> 目标：从产品分析进入系统设计。Buddy 型产品的架构重点不是"调用一个大模型接口"，而是如何把入口、上下文、Runtime、工具、权限、评估组合成一个可持续执行的工作台系统。

---

## 本文职责边界

| 本文负责 | 本文不展开 |
|---|---|
| Buddy 产品中的入口层、任务合同、上下文、Runtime、工具、权限和评估如何映射成产品架构 | Agent Runtime 通用机制见 [20-Agent Runtime专题.md](./20-Agent%20Runtime专题.md)；Tool Gateway 通用机制见 [23-Tool Gateway与工具平台专题.md](./23-Tool%20Gateway与工具平台专题.md)；生产级 Agent 总蓝图见 [08.1-生产级Agent应用工程.md](./08.1-生产级Agent应用工程.md) |

---

## 一、总体架构

```text
User
  -> Interaction Surface
  -> Task Contract Builder
  -> Context Builder
  -> Agent Runtime
      -> Planner / Router
      -> Executor Loop
      -> State Store / Checkpoint
      -> Budget / Stop Control
  -> Tool Gateway
      -> Tool Registry
      -> Permission / Policy
      -> Risk Scoring / HITL
      -> Execution Adapter
      -> Audit Log
  -> External Systems
  -> Verification / Eval
  -> Final Response / Artifact / Handoff
```

---

## 二、核心分层

| 层 | 作用 | WorkBuddy 类 | Qoder / CodeBuddy 类 |
|---|---|---|---|
| 交互入口 | 用户发起任务的位置 | IM、网页、桌面、办公套件 | IDE、CLI、代码仓库、PR |
| 任务契约 | 把自然语言转成可执行边界 | 目标、文件范围、收件人、是否可外发 | issue、repo、文件范围、测试要求 |
| 上下文构造 | 选择任务需要的信息 | 聊天、文档、表格、日程、知识库 | repo map、依赖、Git diff、测试、终端 |
| Agent Runtime | 驱动 plan/act/observe loop | 跨应用执行、长任务恢复 | 读改测循环、代码修复 |
| Tool Gateway | 工具治理和安全边界 | 企业 API、文档、表格、浏览器 | file edit、shell、git、test、deploy |
| 执行环境 | 真实副作用发生的位置 | 浏览器、桌面、SaaS、企业系统 | 本地/远程 workspace、容器、CI |
| 验证评估 | 判断是否完成和是否安全 | 文件/业务状态、用户确认 | 测试、构建、diff、review |

---

## 三、Agent Runtime 状态机

```text
created
  -> planning
  -> collecting_context
  -> running
  -> awaiting_tool
  -> awaiting_approval
  -> running
  -> verifying
  -> completed

异常路径：
running -> failed
running -> paused
running -> canceled
running -> timed_out
```

关键字段：

| 字段 | 说明 |
|---|---|
| `run_id` | 一次任务执行的唯一 ID |
| `task_contract` | 当前任务边界 |
| `current_plan` | 当前计划和子任务 |
| `state_summary` | 可压缩的运行状态摘要 |
| `artifact_refs` | 文件、报告、diff、截图、表格等产物引用 |
| `pending_action` | 等待用户确认、工具结果或远程任务 |
| `budget` | token、时间、工具次数、金额、最大轮数 |
| `checkpoint_ref` | 可恢复状态 |
| `trace_id` | 可观测链路 |
