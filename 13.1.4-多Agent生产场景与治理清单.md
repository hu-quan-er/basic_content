# 多 Agent 生产场景与治理清单

> 目标：把多 Agent 状态管理放进真实生产场景里看：企业入职、Deep Research、代码 Agent、客服工单、供应商尽调分别该如何设计状态、上下文、handoff、artifact、审批、可观测和安全治理。

---

## 一、生产级参考架构

### 1.1 单应用内部多 Agent

```text
[API Gateway]
   |
[Orchestrator / Workflow Engine]
   |
   +-- State Store
   +-- Event Log
   +-- Artifact Store
   +-- Memory Service
   +-- Policy Engine
   +-- Trace System
   |
   +-- Research Agent
   +-- Planning Agent
   +-- Tool Agent
   +-- Verifier Agent
   +-- Writer Agent
```

特点：

- Orchestrator 掌握全局状态。
- Agent 只拿到自己的 Context View。
- Agent 返回 State Patch 和 Artifact。
- Verifier / Policy Engine 决定是否接受更新。

### 1.2 跨组织 A2A 协作

```text
Company A Supervisor Agent
   |
   +-- Internal State Store
   +-- Internal Artifact Store
   |
   +-- A2A Client
          |
          v
Company B A2A Server
   |
   +-- Agent Card
   +-- Task Store
   +-- B Internal Agent Runtime
   +-- B Artifact Store
```

跨组织边界：

- A 公司不拿 B 公司内部 memory。
- B 公司不拿 A 公司全量用户上下文。
- 双方通过 Task、Message、Artifact、auth、metadata 交换最小必要信息。
- 每个组织内部用自己的 runtime 管状态，A2A 只定义边界合同。

---

## 二、场景 1：企业入职多 Agent

### 2.1 角色

| Agent | 责任 | 可写状态 |
|-------|------|----------|
| HR Agent | 收集员工信息 | `/employee`, `/subtasks/hr_profile` |
| IT Agent | 开账号和设备 | `/subtasks/it_account`, `/artifacts` |
| Security Agent | 权限审批 | `/approvals/security`, `/risks` |
| Finance Agent | 薪资和成本中心 | `/payroll`, `/cost_center` |
| Supervisor | 汇总与通知 | `/task/status`, `/final_message` |

### 2.2 状态模型

```json
{
  "task_id": "onboarding_20260701_alice",
  "context_id": "employee_alice_onboarding",
  "status": "working",
  "employee": {
    "name": "Alice",
    "department": "Finance",
    "manager": "Bob",
    "start_date": "2026-07-01"
  },
  "subtasks": {
    "hr_profile": {"owner": "hr_agent", "status": "completed"},
    "it_account": {"owner": "it_agent", "status": "working"},
    "security_review": {"owner": "security_agent", "status": "input_required"},
    "payroll": {"owner": "finance_agent", "status": "pending"}
  },
  "artifacts": [
    {"id": "hr_form_v1", "type": "form", "producer": "hr_agent"}
  ],
  "risks": [],
  "approvals": []
}
```

### 2.3 冲突处理

| 冲突 | 处理 |
|------|------|
| HR 说 Finance，Finance 系统说成本中心不存在 | HRIS 是员工组织关系权威源，Finance 是成本中心权威源 |
| IT 已开账号，Security 后来拒绝高权限 | 降级权限并记录补偿动作 |
| Manager approval 过期 | `approval_status` 变成 `expired`，IT 不得继续 |
| Finance 想读完整 HR 表单 | policy filter，只给必要字段 |

---

## 三、场景 2：Deep Research 多 Agent

### 3.1 角色

| Agent | 责任 |
|-------|------|
| Planner | 拆解研究问题 |
| Search Agent | 检索资料 |
| Source Verifier | 判断来源可信度 |
| Synthesizer | 生成结构化结论 |
| Critic | 找遗漏和冲突 |
| Writer | 生成最终报告 |

### 3.2 状态设计

```json
{
  "research_goal": "分析 2026 年企业 Agent 市场趋势",
  "questions": [
    {"id": "q1", "text": "主要增长驱动是什么", "status": "open"}
  ],
  "sources": [
    {
      "id": "src_001",
      "url": "https://example.com/report",
      "credibility": "medium",
      "checked_by": "source_verifier"
    }
  ],
  "facts_candidates": [],
  "facts_verified": [],
  "contradictions": [],
  "artifacts": []
}
```

### 3.3 上下文同步策略

| 阶段 | 同步内容 | 不同步内容 |
|------|----------|------------|
| Planner -> Search | 研究问题、关键词、排除范围 | planner scratchpad |
| Search -> Verifier | 来源列表、摘录、URL | 搜索过程完整日志 |
| Verifier -> Synthesizer | 已验证 facts、证据引用、可信度 | 未验证网页原文 |
| Synthesizer -> Critic | 候选结论、证据链 | 内部写作草稿 |
| Critic -> Writer | 冲突、遗漏、必须保留假设 | critic chain-of-thought |

### 3.4 关键治理

- 每条事实必须有 `source_id` 和 `artifact_ref`。
- 未验证事实只能进入 `facts_candidates`。
- Writer 只能使用 `facts_verified`。
- Critic 的反驳写入 `contradictions`，由 verifier 决定是否采纳。

---

## 四、场景 3：代码 Agent

### 4.1 角色

| Agent | 责任 |
|-------|------|
| Explorer | 阅读代码和定位影响范围 |
| Planner | 生成修改计划 |
| Implementer | 修改代码 |
| Test Runner | 执行验证 |
| Reviewer | 找 bug、回归风险 |

### 4.2 状态设计

```json
{
  "task": {
    "goal": "修复登录超时后的重试问题",
    "status": "working"
  },
  "code_context": {
    "files_read": [],
    "symbols": [],
    "assumptions": []
  },
  "plan": {
    "steps": [],
    "approved": false
  },
  "changes": {
    "files_modified": [],
    "diff_artifact": null
  },
  "verification": {
    "commands": [],
    "results": []
  },
  "review_findings": []
}
```

### 4.3 Artifact-first

代码 Agent 应把关键产物沉淀成 artifact：

- 代码 diff。
- 测试输出。
- 失败日志。
- 设计计划。
- review findings。

不要只在聊天记录里说“我改好了”。最终状态应引用：

```json
{
  "changes": {
    "files_modified": ["auth/session.go", "auth/session_test.go"],
    "diff_artifact": "artifact:diff_login_retry_v2"
  },
  "verification": {
    "commands": [
      {
        "cmd": "go test ./auth",
        "exit_code": 0,
        "artifact": "artifact:test_auth_20260607"
      }
    ]
  }
}
```

### 4.4 冲突处理

- Implementer 不应覆盖 Explorer 的发现，只能追加或修正并保留 provenance。
- Reviewer findings 不能直接改代码状态，要进入 `review_findings`。
- Test Runner 只写 verification，不写 final answer。
- 用户或 supervisor 决定是否接受 reviewer 建议。

---

## 五、场景 4：客服工单多 Agent

### 5.1 角色

| Agent | 责任 |
|-------|------|
| Triage Agent | 分类工单 |
| Policy Agent | 判断退款/补偿规则 |
| Account Agent | 查询用户订单 |
| Draft Agent | 生成回复 |
| Compliance Agent | 检查敏感话术和权限 |

### 5.2 状态模型

```json
{
  "ticket_id": "ticket_001",
  "customer": {
    "id": "cust_123",
    "tier": "gold"
  },
  "issue": {
    "category": "refund",
    "summary": "用户称订单未送达",
    "status": "triaged"
  },
  "order_context": {
    "order_id": "order_789",
    "delivery_status": "delivered_disputed"
  },
  "policy_decision": {
    "eligible": null,
    "requires_human": true
  },
  "draft_response": null,
  "approvals": []
}
```

### 5.3 关键约束

- Draft Agent 不能看到不必要的支付信息。
- Policy Agent 的结论必须引用规则 artifact。
- 退款是不可逆或高风险动作，需要 strong consistency 和审批。
- 用户历史偏好可以来自 memory，但当前订单状态必须来自订单系统。

---

## 六、场景 5：供应商尽调

### 6.1 角色

| Agent | 责任 |
|-------|------|
| Procurement Agent | 供应商基础信息 |
| Security Agent | 安全评估 |
| Legal Agent | 合同风险 |
| Finance Agent | 付款和预算 |
| Vendor A2A Agent | 供应商侧自动应答 |

### 6.2 跨组织同步

```text
Procurement Supervisor
  -> Vendor A2A Agent: 请求合规资料
  -> Security Agent: 审查 SOC2、渗透测试
  -> Legal Agent: 审查 DPA、赔偿条款
  -> Finance Agent: 审查付款条款
  -> Supervisor: 汇总风险和审批建议
```

### 6.3 状态边界

| 状态 | 是否可给供应商 Agent |
|------|----------------------|
| 请求资料清单 | 可以 |
| 内部预算上限 | 不可以 |
| 内部风险评分规则 | 不可以 |
| 合同条款问题 | 部分可以 |
| 审批链路 | 不可以 |
| 最终整改要求 | 可以 |

---

## 七、可观测指标

| 指标 | 解释 |
|------|------|
| `state_patch_count` | 每个任务产生多少状态更新 |
| `rejected_patch_rate` | patch 被 policy/schema/verifier 拒绝比例 |
| `stale_read_count` | Agent 基于旧 state_version 写入次数 |
| `conflict_count` | 写写冲突、事实冲突、计划冲突次数 |
| `handoff_success_rate` | handoff 后下游是否一次完成 |
| `handoff_rework_rate` | 下游要求补上下文或返工比例 |
| `artifact_reference_rate` | 关键结论是否引用 artifact |
| `memory_hit_rate` | 召回记忆是否实际被使用 |
| `memory_correction_rate` | 记忆被撤销或修正比例 |
| `context_tokens_per_agent` | 每个 Agent 注入上下文 token |
| `unnecessary_broadcast_rate` | 广播给 Agent 但未使用的信息比例 |

---

## 八、安全治理

### 8.1 Prompt Injection 传播路径

```text
外部网页 / 邮件 / 文档
  -> Research Agent 读取
  -> 写入 shared memory 或 handoff summary
  -> Writer Agent / Tool Agent 继承恶意指令
  -> 执行越权工具
```

防护：

- 外部内容作为 data，不作为 instruction。
- handoff payload 区分 `facts` 和 `instructions`。
- shared memory 写入前做来源标注和安全扫描。
- 工具执行前重新检查用户授权和任务边界。
- 高风险工具不接受来自其他 Agent 的自然语言授权。

### 8.2 Memory Poisoning

治理方式：

- 所有长期记忆必须带 source、writer、timestamp、scope。
- 自动写入的记忆先进入 pending。
- 高风险记忆需要 verifier 或人工确认。
- 记忆有 TTL 和撤销机制。
- 召回结果必须显示 provenance。

---

## 九、反模式清单

- 把所有聊天记录广播给所有 Agent。
- 只依赖自然语言 summary，没有结构化 state。
- 只有 message，没有 artifact。
- 只有 final answer，没有 event log。
- 多个 Agent 都能改最终状态，没有字段级所有权。
- 没有 `state_version`，无法发现 stale write。
- 没有 conflict policy，冲突时靠模型自己判断。
- 把长期 memory 当事实真相。
- 把外部网页内容当系统指令传给下游 Agent。
- 把 A2A 当成内部状态同步数据库。
- 把 MCP resource 当成无权限边界的共享磁盘。
- 没有 handoff contract，只说“继续处理”。
- artifact 不版本化。
- trace 只记录 LLM 文本，不记录状态 patch 和权限判断。

---

## 十、系统设计面试题

### Q1：多 Agent 要不要共享完整上下文？

不应该默认共享完整上下文。完整共享只适合短任务、低风险、同信任域 demo。生产系统应共享结构化状态、摘要、工件引用和约束。每个 Agent 看到的是全局状态的一个 view，而不是全部历史。

### Q2：A2A 里的 Agent 之间能不能共享 memory？

A2A 不负责共享内部 memory。它的边界是 Task、Message、Artifact、Agent Card。跨 Agent 可以共享任务相关事实、工件和结构化 handoff，但不应该共享内部 prompt、长期 memory 或工具私有状态。

### Q3：LangGraph state 和 A2A task 是什么关系？

LangGraph state 是应用内部的细粒度状态，适合表达节点之间的读写、reducer、checkpoint。A2A task 是跨系统互操作的任务合同，适合表达远程 Agent 的工作单元和公开生命周期。常见架构是内部用 LangGraph 管状态，外部通过 A2A task 暴露能力。

### Q4：handoff 最容易漏什么？

最容易漏三类信息：

- 已经确定的约束和禁止动作。
- 证据引用和未决问题。
- 下游可以写哪些状态、应该返回什么格式。

### Q5：如何避免共享 memory 被污染？

- 写入时记录 source、writer、timestamp、confidence、scope。
- 高风险记忆进入 pending 或需要 verifier。
- 召回时把 provenance 一起注入上下文。
- 支持撤销、过期和覆盖策略。

---

## 十一、生产落地清单

- [ ] 每个场景都有状态模型。
- [ ] 每个 Agent 有明确读写范围。
- [ ] 每个 handoff 有 contract。
- [ ] 每个关键结论有 artifact 或 evidence。
- [ ] 每个高风险动作有审批或 verifier。
- [ ] 每个状态更新有 `state_version`。
- [ ] 每个跨组织调用有 A2A task 映射表。
- [ ] 每个长期 memory 有 provenance 和 TTL。
- [ ] 每个任务可通过 trace 回答“谁改了什么”。
- [ ] 每个失败状态有恢复或补偿策略。
