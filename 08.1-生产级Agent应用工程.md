# 生产级 Agent 应用工程：从 Demo 到可运营系统

> 目标：系统回答"如何构建一个真正生产级的 Agent 应用"。本篇从产品边界、架构、状态、工具、安全、评估、可观测、成本、发布运维、合规和团队流程展开。它是《[08-工程化性能成本可靠性](./08-工程化性能成本可靠性.md)》的生产化子专题，重点不是某个框架 API，而是生产系统应具备的工程面。

---

## 一、先定义：什么叫生产级 Agent

一个生产级 Agent 不是"能跑通 demo 的 ReAct loop"，而是一个可以长期在线、可监控、可恢复、可审计、可控成本、可渐进发布、能处理异常和安全风险的业务系统。

| 维度 | Demo Agent | 生产级 Agent |
|------|------------|--------------|
| 目标 | 展示能力 | 稳定解决业务问题 |
| 输入 | 理想 prompt | 脏数据、恶意输入、模糊需求、多语言、多租户 |
| 工具 | 直接调用函数 | 权限、幂等、审计、限流、超时、沙箱 |
| 状态 | message history | 结构化 run state + checkpoint + durable execution |
| 成功判断 | 看起来回答了 | 可验证 success criteria + 业务指标 |
| 失败处理 | 抛错或重试 | 分类错误、降级、人工接管、恢复 |
| 安全 | prompt 里提醒 | 权限边界、策略引擎、DLP、HITL、隔离执行 |
| 评估 | 人肉试几条 | 离线回归集 + 在线指标 + failure review |
| 观测 | 打印日志 | trace/metrics/logs/cost/token/工具链路 |
| 发布 | 直接上线 | 灰度、A/B、回滚、模型版本锁定 |

**核心判断**：生产级 Agent = LLM + workflow/runtime + tool platform + state store + policy layer + eval/observability + ops process。LLM 只是其中一个不确定组件。

---

## 二、生产级 Agent 的 15 个工程面

| # | 工程面 | 必须回答的问题 |
|---|--------|----------------|
| 1 | 业务边界 | 为什么必须用 Agent，而不是普通 workflow / RAG / 单次 LLM？ |
| 2 | 成功标准 | 任务成功、失败、部分成功、需人工分别怎么定义？ |
| 3 | 自治等级 | 哪些动作可自动执行，哪些必须审批？ |
| 4 | 架构形态 | 单 Agent、workflow、plan-execute、多 Agent 怎么选？ |
| 5 | 状态模型 | run、step、tool call、artifact、memory 如何持久化？ |
| 6 | 上下文工程 | 每轮给模型什么，不给什么，如何压缩和召回？ |
| 7 | 工具平台 | schema、权限、幂等、超时、错误码、审计怎么做？ |
| 8 | 安全治理 | prompt injection、越权、数据泄露、供应链风险怎么控？ |
| 9 | HITL | 什么时候暂停，谁审批，审批后如何恢复？ |
| 10 | 模型策略 | 模型路由、fallback、版本锁定、reasoning budget 怎么定？ |
| 11 | 性能成本 | 延迟、吞吐、token、缓存、并发、预算怎么治理？ |
| 12 | 可靠性 | 重试、熔断、降级、checkpoint、灾备怎么设计？ |
| 13 | 评估体系 | 离线 eval、在线监控、failure dataset、A/B 怎么闭环？ |
| 14 | 可观测 | trace、metrics、logs、成本、质量、安全事件如何串起来？ |
| 15 | 发布运维 | CI/CD、灰度、回滚、incident response、变更审计怎么做？ |

如果这些问题没有答案，系统还处于 prototype 阶段。

---

## 三、先判断：到底该不该做 Agent

Anthropic 的经验总结很实用：先用最简单的方案，只有复杂度有明确收益时才增加 agentic behavior。生产系统尤其如此，因为 Agent 通常用延迟、成本、可预测性换灵活性。

### 3.1 选择矩阵

| 任务特征 | 推荐形态 |
|----------|----------|
| 路径固定、规则清楚 | 代码 workflow |
| 需要知识问答，但动作少 | RAG + 单次 LLM |
| 需要分类/抽取/改写 | 单次 LLM + schema |
| 需要多步但依赖图可预定义 | Workflow + LLM 节点 |
| 路径依赖中间发现，分支难穷举 | Agent / Plan-Execute |
| 需要长期运行、工具多、可能人工介入 | Durable Agent Runtime |
| 需要多个专业角色，但边界清楚 | Supervisor + specialist agents |

### 3.2 不该做 Agent 的信号

- 业务流程能画成稳定 DAG。
- 每个步骤都有确定性 API 或规则。
- 错误代价高，但无法做审批。
- 没有 eval 数据，也没有人工 review 能力。
- 只因为"Agent 很热"而做。
- 成本、延迟、审计要求比灵活性更重要。

### 3.3 适合做 Agent 的信号

- 任务路径依赖中间结果，无法提前穷举。
- 需要根据工具返回动态改变策略。
- 需要处理开放探索，如 research、debug、coding、复杂运营分析。
- 允许部分不确定性，但有验证器、HITL 或回滚。
- 用户愿意等待更长时间换更高完成度。
- 业务价值足以覆盖 token、工具和运维成本。

**核心结论**：生产级 Agent 的第一步是少做 Agent。能用 workflow 稳定解决的，不要交给模型动态决策。

---

## 四、参考架构：生产级 Agent 的系统分层

```
[Client / Product UI]
        ↓
[API Gateway / Auth / Tenant Context]
        ↓
[Agent Orchestrator]
  ├─ Plan / Step State Machine
  ├─ Context Builder
  ├─ Budget Manager
  ├─ Policy & Guardrail Hooks
  └─ Human-in-the-loop Hooks
        ↓
┌─────────────────────────────────────────────┐
│ Runtime Layer                                │
│  Model Gateway     Tool Gateway              │
│  Memory/RAG        Queue/Workers             │
│  Sandbox           Durable State Store       │
└─────────────────────────────────────────────┘
        ↓
[Observability + Eval + Audit + Cost Platform]
        ↓
[Release / Experiment / Incident Process]
```

### 4.1 核心模块职责

| 模块 | 职责 |
|------|------|
| API Gateway | 认证、租户、速率限制、请求追踪、输入大小限制 |
| Agent Orchestrator | 计划、执行、状态流转、工具调用、重试、暂停恢复 |
| Context Builder | 从 state/memory/RAG/tool result 组装每轮最小充分上下文 |
| Model Gateway | 模型路由、fallback、版本锁定、token 统计、prompt caching |
| Tool Gateway | 工具注册、schema 校验、权限、幂等、审计、限流、沙箱 |
| Policy Layer | 输入/输出/工具/计划级 guardrails 和审批策略 |
| Durable Store | run state、step、artifact、checkpoint、conversation、audit |
| Queue/Workers | 长任务、异步任务、批处理、重试隔离 |
| Eval Platform | 离线 eval、回放、judge、人工标注、failure dataset |
| Observability | trace、metrics、logs、cost、安全事件、业务指标 |
| Release Platform | prompt/model/tool/schema 版本、灰度、A/B、回滚 |

### 4.2 一条生产请求的链路

```
1. 用户请求进入，绑定 user_id / tenant_id / trace_id
2. 输入安全检查：大小、类型、注入风险、权限上下文
3. Orchestrator 创建 run，写入 durable state
4. Planner 生成 typed plan 或选择 workflow path
5. Plan validator 做 schema / DAG / 权限 / 预算 / 风险校验
6. Executor 按状态机执行 step
7. Tool Gateway 执行工具：权限、幂等、超时、审计
8. Context Builder 将 observation 压缩为 state/evidence
9. 必要时 verifier/critic 检查结果
10. 高风险动作进入 HITL
11. Finalizer 输出结构化结果
12. Trace 入库，自动 eval，失败样本进入 failure pool
```

---

## 五、业务边界与需求建模

### 5.1 先写任务合同

生产 Agent 的需求不应该只写"帮用户完成 X"，而要写任务合同：

| 字段 | 示例 |
|------|------|
| 任务目标 | 根据合同和订单记录判断是否可退款 |
| 输入 | 用户问题、订单 ID、合同版本、用户权限 |
| 输出 | refund_decision、reason、evidence、confidence、next_action |
| 成功标准 | 决策与人工审核一致率 > 95%，引用正确率 > 98% |
| 失败标准 | 数据缺失、权限不足、工具超时、风险过高 |
| 允许动作 | 查询订单、查询合同、生成建议 |
| 禁止动作 | 自动退款、修改订单、发送外部邮件 |
| 人工介入 | 金额 > $500 或 confidence < 0.8 |
| SLO | p95 < 8s，超时返回可解释部分结果 |
| 成本预算 | 平均 < $0.08/run，p95 < $0.25/run |

### 5.2 定义完成状态

至少区分：

| 状态 | 含义 | 用户看到什么 |
|------|------|--------------|
| `succeeded` | 达到成功标准 | 完整答案/动作完成 |
| `partial_success` | 核心目标部分完成 | 哪些完成、哪些缺失 |
| `needs_human` | 风险或不确定性过高 | 等待人工处理 |
| `blocked` | 缺权限/缺输入/外部系统不可用 | 明确请求补充或稍后 |
| `failed_retryable` | 临时失败 | 自动重试或排队 |
| `failed_fatal` | 不可恢复失败 | 安全失败说明 |

不要让模型用自由文本说"完成了"。完成必须由系统根据结构化条件判断。

---

## 六、自治等级与控制面

Agent 的风险来自"模型能决定下一步并调用工具"。生产系统要明确自治等级。

| 等级 | 能力 | 适用 |
|------|------|------|
| L0 建议 | 只生成建议，不执行动作 | 法务、医疗、财务建议 |
| L1 草稿 | 生成草稿，用户手动提交 | 邮件、报告、工单回复 |
| L2 受限执行 | 低风险动作自动执行 | 查询、分类、内部标签 |
| L3 审批执行 | 高风险动作需 HITL | 退款、发邮件、改配置 |
| L4 策略内自治 | 在明确策略和预算内执行 | 批量运营、低风险 IT 自动化 |
| L5 高自治 | 长期独立运行、跨系统动作 | 极少数场景，需要强治理 |

### 6.1 控制面的四个闸门

| 闸门 | 检查什么 | 例子 |
|------|----------|------|
| 输入闸 | 用户输入、文件、网页是否安全 | prompt injection、PII、恶意文件 |
| 计划闸 | plan 是否越权、超预算、含危险动作 | 删除数据、转账、外发 |
| 工具闸 | 每次 tool call 是否允许 | 参数、权限、频率、幂等 |
| 输出闸 | 最终输出是否合规、可解释 | 敏感信息、误导性声明、缺引用 |

OpenAI Agents SDK 文档也强调 guardrails 不同位置的适用范围：输入/输出 guardrails 并不天然覆盖每个工具调用，因此生产系统要有 tool-level guardrails。

---

## 七、Orchestrator：把 Agent 变成状态机

### 7.1 不要只存 message history

生产 Agent 需要结构化 run state：

```json
{
  "run_id": "run_123",
  "tenant_id": "t1",
  "user_id": "u1",
  "goal": "判断订单是否可退款",
  "status": "running",
  "budget": {
    "max_steps": 10,
    "max_cost_usd": 0.2,
    "deadline_ms": 15000
  },
  "plan": {"steps": []},
  "steps": [],
  "artifacts": [],
  "risk": {"level": "medium", "requires_human": false},
  "trace_id": "trace_abc"
}
```

状态比上下文更重要。上下文是每轮给模型看的投影，state 才是系统事实源。

### 7.2 Step 状态机

```
pending
  ↓
running
  ↓
succeeded ─────────→ next step
  │
  ├── failed_retryable → retry
  ├── failed_recoverable → replan / compensate
  ├── needs_human → pause
  ├── skipped → next step
  └── failed_fatal → abort / fallback
```

每个 step 至少记录：
- input。
- expected output。
- tool/model used。
- attempts。
- start/end time。
- token/cost。
- observation。
- artifact refs。
- error type。
- verifier result。

### 7.3 Durable Execution

长任务必须能在进程重启、worker 崩溃、上游超时后恢复：

| 能力 | 含义 |
|------|------|
| Checkpoint | 每个关键 step 后保存 state |
| Resume | 从最后成功 step 继续 |
| Replay | 用历史 trace 复现执行路径 |
| Compensation | 对已执行副作用做补偿动作 |
| Pause/Resume | 人工审批、等待外部事件后继续 |
| Idempotency | 重试不会重复扣款/下单/发信 |

LangGraph 这类 runtime 把 durable execution、persistence、HITL 作为核心能力；如果自研，也要提供等价机制。

---

## 八、Plan：从自然语言变成 typed DAG

### 8.1 Plan Contract

自然语言 plan 不可执行。生产 plan 应该是 typed DAG：

```json
{
  "goal": "对比三份合同中的退款条款",
  "steps": [
    {
      "id": "load_contracts",
      "type": "tool",
      "tool": "document_search",
      "depends_on": [],
      "input": {"contract_ids": ["c1", "c2", "c3"]},
      "success_criteria": ["找到三份合同", "返回条款引用"],
      "risk_level": "low",
      "timeout_ms": 3000,
      "max_attempts": 2
    },
    {
      "id": "compare_terms",
      "type": "llm",
      "depends_on": ["load_contracts"],
      "output_schema": "ComparisonResult",
      "success_criteria": ["每个结论都有 evidence"]
    }
  ]
}
```

### 8.2 Plan 校验

Plan 不只做 JSON parse：

| 校验 | 问题 |
|------|------|
| Schema | 字段类型、必填项、enum 是否合法 |
| DAG | 是否有环、依赖是否存在 |
| Tool | 工具是否存在，参数是否符合 schema |
| Permission | 当前用户是否能用这些工具和数据 |
| Budget | step/token/cost/time 是否超限 |
| Risk | 是否含高风险动作 |
| Completeness | success criteria 是否可被步骤支撑 |

### 8.3 Replan 规则

生产系统不应允许模型随意推翻目标。推荐：
- 默认执行初始 plan。
- 只有 step 失败、证据不足、用户补充信息时局部 replan。
- replan 必须解释变更原因。
- replan 仍要过 plan validator。
- replan 次数计入预算。

---

## 九、Context 与 Memory：让模型只看该看的

### 9.1 Context Builder

Context Builder 的职责是从 state 和外部数据中生成当前模型调用的最小充分上下文：

```
System + Tool Schema + Policy
  + Current Goal
  + Relevant State
  + Required Evidence
  + Recent Observations
  - Irrelevant History
  - Raw Tool Dumps
  - Untrusted Instructions
```

### 9.2 信息分层

| 信息 | 存储位置 | 注入策略 |
|------|----------|----------|
| system policy | prompt prefix / policy store | 每轮稳定注入 |
| tool schema | tool registry | 每轮或按需注入 |
| run state | DB/checkpoint | 摘要注入 |
| raw artifacts | object store | 只放引用 |
| RAG evidence | vector/search index | 按需召回 |
| user preferences | profile/memory | 权限和时效校验后注入 |
| conversation history | message store | 截断/摘要/近期窗口 |

### 9.3 Evidence Card

工具返回和 RAG 结果不要全量塞进上下文。推荐压成 evidence card：

```json
{
  "evidence_id": "ev_42",
  "source": "contract:c1:section_5.2",
  "claim": "退款窗口为付款后 30 天",
  "quote": "用户可在付款后 30 天内申请退款",
  "confidence": 0.93,
  "permissions": ["refund_agent"],
  "raw_ref": "s3://contracts/c1.pdf#page=8"
}
```

### 9.4 防止上下文污染

外部网页、文件、邮件、RAG 文档都应默认视为不可信数据。处理方式：
- 明确标注 untrusted content。
- 不允许 untrusted content 覆盖 system/developer policy。
- 检测和剥离潜在 prompt injection。
- 工具返回只作为 evidence，不作为指令。
- 高风险动作只使用可信数据源和结构化字段。

---

## 十、Tool Gateway：生产 Agent 的真正边界

工具是 Agent 接触真实世界的接口，必须像生产 API 一样治理。

### 10.1 Tool Contract

每个工具至少定义：

| 字段 | 示例 |
|------|------|
| name | `refund_lookup` |
| purpose | 查询退款资格，不执行退款 |
| input_schema | JSON Schema / Pydantic / Zod |
| output_schema | 结构化输出 |
| auth_scope | `refund:read` |
| side_effect | none / read / write / external |
| timeout | 2s |
| retry_policy | 429/5xx 可重试，4xx 不重试 |
| idempotency | 写操作必填 |
| rate_limit | per user / tenant / tool |
| audit_level | none / normal / strict |
| risk_level | low / medium / high |

### 10.2 工具错误分类

| 错误 | 处理 |
|------|------|
| 400 参数错误 | 反馈 validator error，允许模型修复一次 |
| 401/403 权限错误 | 不重试，进入 blocked 或 HITL |
| 404 数据不存在 | 让模型解释缺数据，不编造 |
| 409 冲突 | 读取最新状态后 replan |
| 422 业务规则失败 | 作为 observation，不当成系统异常 |
| 429 限流 | 指数退避或降级 |
| 5xx 临时错误 | 重试 + 熔断 |
| timeout | 取消/重试/返回部分结果 |

### 10.3 Side Effect 分级

| 等级 | 示例 | 控制 |
|------|------|------|
| Read-only | 搜索、查询订单 | 权限 + 限流 |
| Internal write | 打标签、建草稿 | 幂等 + 审计 |
| External action | 发邮件、退款、改配置 | HITL + 双重确认 |
| Irreversible action | 删除数据、转账、生产变更 | 默认禁止或强审批 |

### 10.4 沙箱和隔离

代码执行、浏览器、shell、文件系统、MCP server 都应隔离：
- 容器/VM 沙箱。
- 只读挂载默认。
- 网络 egress allowlist。
- secrets 不进 prompt、不进工具输出。
- 每次 run 使用短期凭证。
- 文件扫描和大小限制。
- 输出 DLP。
- 工具依赖版本锁定。

**核心判断**：不要把"模型不会乱来"作为安全边界。真正边界在 Tool Gateway、权限系统、沙箱和审批。

---

## 十一、安全、权限与治理

### 11.1 Agent 威胁模型

| 风险 | 说明 | 控制 |
|------|------|------|
| Prompt Injection | 用户或外部内容诱导模型忽略规则 | 信任分层、输入扫描、工具闸 |
| Indirect Injection | 网页/RAG/邮件里藏指令 | untrusted 标记、内容净化 |
| Tool Abuse | 模型调用高风险工具 | 最小权限、审批、限流 |
| Data Exfiltration | 把敏感数据输出给未授权用户 | ABAC/RBAC、DLP、输出检查 |
| Excessive Agency | 模型拥有过大权限和目标 | 自治等级、预算、HITL |
| Supply Chain | 工具/MCP/plugin 被投毒 | registry、签名、版本锁定 |
| Memory Poisoning | 长期记忆被写入恶意事实 | 写入审核、来源、过期 |
| Unbounded Consumption | token/tool 调用失控 | budget、rate limit、circuit breaker |

OWASP LLM Top 10 和 Agentic Skills Top 10 都把 prompt injection、过度代理、工具/供应链风险作为核心风险面。NIST AI RMF 则更强调治理、测量、管理和风险闭环。

### 11.2 权限模型

推荐组合：
- 用户身份：谁发起。
- 租户：数据隔离。
- Agent 身份：哪个 agent 在执行。
- Tool scope：工具权限。
- Data scope：可访问哪些对象。
- Action scope：读/写/外发/删除。
- Context scope：哪些内容可进入模型上下文。

不要让 Agent 继承用户所有权限。更安全的方式是：

```
effective_permission = user_permission ∩ agent_scope ∩ tool_scope ∩ policy
```

### 11.3 MCP / 外部工具治理

MCP 等标准化工具协议能降低集成成本，但也扩大了供应链和权限面。生产使用要考虑：
- 私有 registry，不随意接入未知 server。
- server 签名和版本锁定。
- OAuth / scoped authorization。
- tool manifest 安全 review。
- 工具描述防投毒。
- 每个 MCP server 的网络和文件权限隔离。
- 对 tool call 做集中审计。

---

## 十二、Human-in-the-loop 不是一个 Approve 按钮

HITL 要设计成运行时机制，而不是最后让人兜底。

### 12.1 触发条件

| 触发 | 示例 |
|------|------|
| 风险 | 退款金额过高、外发邮件、删除数据 |
| 低置信 | evidence 不足、模型分歧 |
| 权限 | 当前 agent 无法访问必要数据 |
| 预算 | 成本/时间即将超限 |
| 异常 | 工具连续失败、状态冲突 |
| 用户体验 | 需求模糊，需要用户选择 |

### 12.2 审批界面应该展示什么

- 目标和当前计划。
- 为什么暂停。
- 待审批动作。
- 工具参数。
- 证据和来源。
- 可能影响。
- 可选操作：批准、拒绝、修改、补充信息、终止。
- 审批人和时间。

### 12.3 审批后如何恢复

审批结果必须写回 state：

```json
{
  "approval_id": "ap_1",
  "step_id": "refund_execute",
  "decision": "approved_with_changes",
  "modified_input": {"amount": 120},
  "reviewer": "ops_42",
  "reason": "金额低于政策上限",
  "timestamp": "2026-06-07T10:00:00Z"
}
```

然后由状态机恢复，而不是重新从头跑。

---

## 十三、模型策略与 Provider 抽象

### 13.1 模型分层

| 阶段 | 模型策略 |
|------|----------|
| Intent/router | 小模型或规则 |
| Planner | 强模型或 reasoning model |
| Tool args | 中小模型 + structured output |
| Summarizer | 小模型 |
| Critic/verifier | 强模型或规则/测试 |
| Final response | 中等模型，优化 UX |

### 13.2 Provider Gateway

生产系统不应把业务逻辑直接绑死在某个 provider SDK：

| 能力 | 作用 |
|------|------|
| model alias | `planner_v3` 映射到具体模型版本 |
| version lock | 防止 provider 自动升级导致行为漂移 |
| fallback | 主模型失败切备用 |
| quota routing | 避开 RPM/TPM 限制 |
| cost tracking | 按 run/tenant/feature 统计 |
| prompt caching | 稳定前缀缓存 |
| redaction | prompt 出口前脱敏 |
| replay | 保存 model input/output 用于排障 |

### 13.3 Reasoning Budget

Reasoning model 很适合 planner 和 critic，但不应默认所有步骤 high effort：
- 简单任务 low。
- 复杂规划 medium/high。
- 失败恢复可临时升档。
- 超预算后降级或 HITL。
- 记录 reasoning token，用于成本归因。

---

## 十四、性能、成本与可靠性

本节是《[08-工程化性能成本可靠性](./08-工程化性能成本可靠性.md)》在生产 Agent 上的落地。

### 14.1 延迟预算

把总 SLO 拆给各部分：

| 部分 | 预算示例 |
|------|----------|
| API auth/gateway | 100ms |
| planner LLM | 1500ms |
| tool calls | 3000ms |
| executor/summarizer | 1500ms |
| final response TTFT | 800ms |
| total p95 | 8000ms |

如果一个长任务无法满足同步 SLO，就改成异步任务 + 进度流 + 通知。

### 14.2 成本预算

每个 run 都应记录：
- input tokens。
- output tokens。
- reasoning tokens。
- tool cost。
- cache hit。
- retry cost。
- human review cost。
- provider cost。

成本控制手段：
- max_steps。
- max_tokens。
- max_tool_calls。
- max_cost_usd。
- model routing。
- prefix/tool/semantic cache。
- RAG top-k 限制。
- 输出长度预算。

### 14.3 可靠性手段

| 手段 | Agent 特化 |
|------|------------|
| Timeout | run/step/tool/model 多级超时 |
| Retry | 按错误类型重试，不盲目重试 |
| Circuit breaker | 某工具/模型异常率高时熔断 |
| Bulkhead | 不同租户/任务队列隔离 |
| Fallback | 模型、工具、流程、人工降级 |
| Idempotency | 所有副作用工具必备 |
| Checkpoint | 每个关键 step 后持久化 |
| Compensation | 已执行副作用可回滚或补偿 |

---

## 十五、评估体系：先有 Eval，再谈上线

Anthropic 的 agent eval 文章强调：Agent 多轮、会调用工具、会修改状态，因此比单次回答更难评估。生产系统必须同时评估 outcome、step 和 trajectory。

### 15.1 三层评估

| 层级 | 指标 |
|------|------|
| Outcome | 任务成功率、业务 KPI、用户满意、人工接管率 |
| Step | 工具选择、参数正确率、schema 合规、证据正确 |
| Trajectory | 步数、绕路、循环、错误恢复、成本效率 |

### 15.2 Eval 数据集

至少包含：
- golden path。
- 边界条件。
- 模糊输入。
- 权限不足。
- 工具失败。
- 对抗输入。
- 历史线上失败。
- 高价值业务 case。

### 15.3 自动评估组合

| 评估器 | 适用 |
|--------|------|
| 规则/单元测试 | 格式、计算、代码、SQL |
| Tool simulator | 工具调用路径 |
| LLM-as-Judge | 开放式答案、计划质量 |
| Human review | 高风险、低置信、抽样校准 |
| Static analysis | 代码 Agent、配置变更 |
| Browser/UI test | GUI/Web Agent |

### 15.4 评估驱动开发

推荐流程：

```
定义任务合同
  ↓
写 eval case
  ↓
实现 agent
  ↓
离线回放
  ↓
失败聚类
  ↓
修 prompt/tool/context/runtime
  ↓
灰度 A/B
  ↓
线上 failure 入库
```

没有 eval 的 Agent，上线后只能靠用户发现问题。

---

## 十六、可观测性：Trace 是生产 Agent 的黑匣子

### 16.1 必采字段

| 类型 | 字段 |
|------|------|
| Run | run_id、trace_id、tenant、user、feature、version |
| Model | provider、model、prompt version、tokens、latency、cost |
| Tool | tool name、args hash、result ref、latency、error code |
| State | step status、attempt、checkpoint id |
| Context | context size、RAG docs、cache hit、memory ids |
| Safety | guardrail result、policy decision、risk level |
| Eval | score、judge、failure category |
| Business | conversion、resolution、CSAT、refund amount |

OpenTelemetry GenAI semantic conventions 已经把 model spans、agent spans、events、metrics 纳入标准化方向。即使内部字段不同，也建议对齐类似结构，避免每个团队自造一套不可汇总的日志。

### 16.2 Trace 视图

一个好 trace 应该能回答：
- 用户输入是什么。
- Agent 为什么选择这个 plan。
- 每一步看到了什么上下文。
- 调了哪些工具，参数是什么。
- 哪个 guardrail 放行/拦截。
- 哪个 step 最慢/最贵。
- 哪个证据支持最终结论。
- 如果失败，失败在哪一层。

### 16.3 指标面板

| 面板 | 指标 |
|------|------|
| 质量 | success rate、judge score、human override |
| 延迟 | TTFT、E2E、tool latency、queue time |
| 成本 | cost/run、tokens/run、cache hit |
| 可靠性 | error rate、retry rate、timeout、fallback |
| 安全 | guardrail trip、blocked tool call、DLP events |
| 业务 | resolution rate、conversion、AHT、deflection |

---

## 十七、发布、灰度与回滚

Agent 的版本不只是代码版本：

| 版本对象 | 示例 |
|----------|------|
| prompt | system prompt、few-shot、planner prompt |
| model | provider + model snapshot |
| tool schema | 参数、枚举、字段描述 |
| tool implementation | API 行为、错误码 |
| RAG index | 文档版本、embedding 模型 |
| policy | guardrail、审批规则 |
| eval set | 测试集版本 |
| orchestration | graph/state machine 代码 |

### 17.1 发布流程

```
PR
  ↓
静态检查：schema、prompt lint、tool manifest
  ↓
离线 eval：golden + failure set
  ↓
shadow traffic：只观察不影响用户
  ↓
canary：1% 流量
  ↓
A/B：质量/成本/延迟对比
  ↓
full rollout
  ↓
持续监控 + 自动回滚条件
```

### 17.2 回滚条件

- 任务成功率下降超过阈值。
- p95 延迟显著上升。
- cost/run 超预算。
- tool error 或 timeout 激增。
- guardrail trip 激增。
- 人工接管率上升。
- 安全事件出现。

---

## 十八、数据、隐私与合规

### 18.1 数据生命周期

| 阶段 | 控制 |
|------|------|
| 输入 | PII 检测、权限检查、大小限制 |
| Prompt 构造 | 最小必要、脱敏、租户隔离 |
| 模型调用 | provider 数据策略、区域、保留期 |
| 工具调用 | scope、audit、secret 管理 |
| 存储 | 加密、TTL、访问审计 |
| 训练/eval 复用 | 用户同意、脱敏、采样策略 |
| 删除 | Right to delete、artifact 清理 |

### 18.2 敏感信息原则

- 不把 secrets 放进 prompt。
- 不把完整原始数据放进 trace 默认展示。
- PII 做字段级脱敏。
- eval 数据集要脱敏和权限隔离。
- 线上 failure review 要有审计。
- 第三方模型调用要明确数据出境和保留策略。

---

## 十九、用户体验：让不确定系统可理解

生产 Agent 的 UX 要处理等待、不确定、暂停、失败。

### 19.1 进度可见

Agent 流式不只是 token：

```json
{"type": "started", "run_id": "r1"}
{"type": "plan", "summary": "将查询订单和合同条款"}
{"type": "tool_start", "tool": "order_lookup"}
{"type": "tool_result", "tool": "order_lookup", "status": "ok"}
{"type": "needs_human", "reason": "退款金额超过自动阈值"}
{"type": "answer_delta", "text": "根据合同..."}
{"type": "done"}
```

### 19.2 用户控制

应该支持：
- 取消。
- 暂停。
- 修改目标。
- 选择方案。
- 审批动作。
- 查看证据。
- 查看历史和执行记录。

### 19.3 失败表达

坏表达：

> 出错了，请稍后重试。

好表达：

> 我查到了订单信息，但合同检索服务超时，所以无法确认退款条款。你可以稍后重试，或让我把当前结果提交给人工客服继续处理。

---

## 二十、团队流程与所有权

生产 Agent 横跨产品、后端、ML、数据、安全、运维和法务。必须明确 ownership。

| 对象 | Owner |
|------|-------|
| 业务成功指标 | Product / Business |
| Agent orchestration | Backend / AI Platform |
| Prompt/model config | AI Engineer |
| Tools/API | Domain service team |
| Eval set | AI + Domain expert |
| Safety policy | Security / Risk / Legal |
| Observability | Platform / SRE |
| Incident response | On-call owner |

### 20.1 变更管理

Prompt、tool schema、model version、RAG corpus 都要当代码管理：
- PR review。
- diff 可读。
- eval 必跑。
- 版本可回滚。
- 变更有 owner。
- 上线有监控窗口。

---

## 二十一、生产成熟度模型

| 等级 | 状态 | 特征 |
|------|------|------|
| L0 Demo | 能跑 | 单 prompt、无 eval、无 trace |
| L1 Prototype | 可试用 | 有工具、有基础日志、有人工观察 |
| L2 Internal Beta | 可控试点 | 有 schema、state、超时、基础 eval |
| L3 Production | 可运营 | trace、eval、HITL、限流、成本、灰度 |
| L4 Critical Workflow | 高可靠 | durable execution、审计、SLO、incident、合规 |
| L5 Autonomous Operations | 高自治 | 策略内自优化、多系统动作、强治理、持续风控 |

大多数企业 Agent 首个可用目标应是 L3，而不是 L5。

---

## 二十二、常见生产失败模式

| 失败模式 | 根因 | 对策 |
|----------|------|------|
| Agent 做了错误动作 | 权限过大、无审批 | 最小权限 + HITL + tool guardrails |
| 越跑越贵 | loop 无预算、history 膨胀 | max_steps + compaction + cost budget |
| 回答看似合理但事实错 | 无 grounding 或证据错 | RAG eval + 引用校验 + verifier |
| 工具重复执行副作用 | 重试无幂等 | idempotency key + side-effect log |
| 上下文被污染 | 外部内容当指令 | untrusted 标记 + prompt injection 防护 |
| 无法复现线上问题 | 没 trace/version | run trace + prompt/model/tool version |
| eval 线上不一致 | 测试集不代表真实流量 | real traffic sampling + failure pool |
| 模型升级后退化 | 未锁版本、无回归 | model alias + eval gate + canary |
| HITL 形同虚设 | 审批信息不足 | 展示证据、风险、参数和替代方案 |
| 多 Agent 互相推责 | ownership 不清 | specialist contract + supervisor state |

---

## 二十三、生产上线 Checklist

### 23.1 产品与范围

- [ ] 明确为什么需要 Agent，而不是 workflow/RAG/单次 LLM。
- [ ] 定义成功、失败、部分成功、人工介入。
- [ ] 定义用户可见体验、等待策略和失败文案。
- [ ] 定义 SLO 和成本预算。

### 23.2 架构与运行时

- [ ] 有结构化 run state，不只依赖 message history。
- [ ] 有 step 状态机和 checkpoint。
- [ ] 有 typed plan 或固定 workflow。
- [ ] 支持 pause/resume/replay。
- [ ] 支持 cancellation。

### 23.3 工具与权限

- [ ] 每个工具有 schema、timeout、error taxonomy。
- [ ] 写操作有 idempotency key。
- [ ] 高风险动作有 HITL。
- [ ] 工具权限按用户、租户、agent scope 交集计算。
- [ ] 工具调用全量审计。

### 23.4 安全与合规

- [ ] 外部内容标记为 untrusted。
- [ ] 有 prompt injection 和 output handling 防护。
- [ ] secrets 不进入 prompt/trace。
- [ ] PII 脱敏和保留策略明确。
- [ ] 沙箱隔离代码/浏览器/shell/MCP。

### 23.5 评估与观测

- [ ] 有 golden set、failure set、adversarial set。
- [ ] eval 覆盖 outcome/step/trajectory。
- [ ] trace 能串起 model/tool/state/policy/cost。
- [ ] 有线上质量、延迟、成本、安全面板。
- [ ] 失败样本进入数据飞轮。

### 23.6 发布运维

- [ ] prompt/model/tool/RAG/policy 都有版本。
- [ ] 上线前跑离线 eval。
- [ ] 支持 shadow/canary/A/B。
- [ ] 有自动回滚阈值。
- [ ] 有 incident runbook 和 owner。

---

## 二十四、场景设计：企业客服退款 Agent

### 24.1 需求

用户问："我这个订单能退款吗？如果能，请帮我申请。"

### 24.2 不同自治等级

| 等级 | 方案 |
|------|------|
| L0 | 只解释政策，建议用户点退款按钮 |
| L1 | 生成退款申请草稿 |
| L2 | 自动判断低金额可退款并创建内部工单 |
| L3 | 金额超过阈值进入人工审批 |
| L4 | 在政策和预算内自动处理批量退款 |

### 24.3 推荐生产架构

```
User
  ↓
Intent Router
  ↓
Refund Agent Orchestrator
  ├─ order_lookup(read-only)
  ├─ policy_search(RAG)
  ├─ eligibility_checker(code/rules)
  ├─ risk_policy
  ├─ HITL approval
  └─ refund_create(write, idempotent)
```

### 24.4 关键设计

- 退款资格判断优先用规则和政策证据，不让模型自由决定。
- LLM 负责解释政策、处理模糊语言、生成用户沟通文案。
- `refund_create` 是高风险写工具，必须有 idempotency key。
- 金额、用户等级、欺诈风险决定是否 HITL。
- 所有结论带 evidence card。
- 工具失败时返回部分结果，不编造退款状态。
- 线上监控：自动退款成功率、人工推翻率、误拒/误批、成本、p95。

---

## 二十五、高频面试题（含答案）

### Q1：生产级 Agent 和 Demo Agent 最大区别是什么？

**答**：

Demo Agent 证明模型能完成一条理想路径；生产级 Agent 要处理异常、权限、安全、状态恢复、成本、观测、评估和发布。最大区别是：生产系统把 LLM 当不确定组件，用状态机、工具网关、策略层、评估和可观测把它约束起来。

---

### Q2：构建生产 Agent 第一件事是什么？

**答**：

不是选框架，而是判断是否真的需要 Agent。能用固定 workflow、RAG 或单次 LLM 解决的，不要上动态 Agent。只有任务路径依赖中间结果、分支难穷举、需要模型动态决策时，Agent 的成本和不确定性才有价值。

---

### Q3：为什么不能只存 message history？

**答**：

message history 是给模型看的上下文，不是系统事实源。生产系统需要知道 run 状态、step 状态、attempt、工具结果、artifact、预算、审批、错误、版本和 trace。没有结构化 state，就无法恢复、审计、重放、统计和可靠判断完成。

---

### Q4：工具调用为什么是生产 Agent 的安全边界？

**答**：

模型输出只是意图，工具才会读写真实系统。风险动作发生在工具层，所以权限、schema、幂等、限流、超时、审计、沙箱和 HITL 都必须在 Tool Gateway 实现。不能只靠 prompt 告诉模型"不要做坏事"。

---

### Q5：Agent 的 guardrails 应该放在哪里？

**答**：

至少四处：输入、计划、工具、输出。输入 guardrail 处理用户输入和文件；计划 guardrail 拦截越权和高风险步骤；工具 guardrail 在每次调用前后校验参数、权限和结果；输出 guardrail 检查敏感信息、事实性和合规。只在最终输出做 guardrail 太晚。

---

### Q6：如何设计 Agent 的 eval？

**答**：

分三层：outcome 看任务是否完成，step 看工具和中间结果是否正确，trajectory 看路径是否高效和可恢复。数据集要包含 golden path、边界、工具失败、权限不足、对抗输入和线上失败。能用规则/测试就不用 LLM judge，高风险 case 要人工校准。

---

### Q7：如何控制 Agent 成本？

**答**：

先拆成本：模型 input/output/reasoning tokens、工具成本、重试成本、人工成本。再控制：max_steps、max_tokens、max_cost、模型路由、prompt caching、上下文压缩、工具结果摘要、semantic/tool cache、早停、异步批处理。最关键是避免无意义 loop 和完整 history 膨胀。

---

### Q8：生产 Agent 如何处理长任务？

**答**：

必须 durable execution：每个关键 step checkpoint，状态持久化，worker 崩溃后能 resume；高风险或等待外部事件时 pause；重试要幂等；已执行副作用要有 compensation；用户端通过流式进度或后台通知感知进展。

---

### Q9：多 Agent 是不是更生产级？

**答**：

不是。多 Agent 增加通信、状态、成本和责任边界复杂度。只有当专业职责清晰、上下文隔离有价值、并行收益明显时才用。生产里更常见的是 workflow + 少数 specialist，而不是一群自由对话的 Agent。

---

### Q10：上线生产 Agent 的最低门槛是什么？

**答**：

至少要有：清晰任务合同、结构化 state、工具权限和幂等、超时/重试/降级、基础安全 guardrails、离线 eval、trace、成本预算、灰度发布和回滚机制。如果涉及写操作或敏感数据，还必须有 HITL、审计和合规控制。

---

## 二十六、关键术语 Cheat Sheet

| 术语 | 一句话 |
|------|--------|
| Production Agent | 可运营、可观测、可恢复、可审计的 Agent 系统 |
| Agent Orchestrator | 管理 plan、state、tool、retry、HITL 的运行时 |
| Run State | 一次任务的结构化事实源 |
| Typed DAG | 可执行、可校验、可恢复的计划图 |
| Tool Gateway | 工具权限、schema、幂等、审计和沙箱层 |
| Policy Layer | 输入/计划/工具/输出的策略和 guardrail |
| Durable Execution | 崩溃后能从 checkpoint 恢复的执行模式 |
| HITL | 人在回路，用于审批、补充信息和风险控制 |
| Evidence Card | 可引用、可权限校验的证据摘要 |
| Failure Pool | 线上失败样本池，用于 eval 和改进 |
| Shadow Traffic | 新版本旁路运行，不影响用户结果 |
| Canary | 小流量灰度验证 |
| Model Gateway | 模型路由、fallback、版本、成本和脱敏层 |

---

## 二十七、延伸阅读

- Anthropic — [Building effective agents](https://www.anthropic.com/engineering/building-effective-agents)
- Anthropic — [Demystifying evals for AI agents](https://www.anthropic.com/engineering/demystifying-evals-for-ai-agents)
- OpenAI — [Agents SDK guide](https://platform.openai.com/docs/guides/agents-sdk/)
- OpenAI Agents SDK — [Guardrails](https://openai.github.io/openai-agents-python/guardrails/)
- LangGraph — [Overview: durable execution, HITL, persistence](https://docs.langchain.com/oss/python/langgraph)
- OpenTelemetry — [Semantic conventions for generative AI systems](https://opentelemetry.io/docs/specs/semconv/gen-ai/)
- OWASP — [Top 10 for LLM Applications](https://owasp.org/www-project-top-10-for-large-language-model-applications)
- OWASP — [Agentic Skills Top 10](https://owasp.org/www-project-agentic-skills-top-10/)
- NIST — [AI Risk Management Framework](https://www.nist.gov/itl/ai-risk-management-framework)
- Model Context Protocol — [Authorization](https://modelcontextprotocol.io/docs/tutorials/security/authorization)
- Model Context Protocol — [Security Best Practices](https://modelcontextprotocol.io/docs/tutorials/security/security_best_practices)
- Google SRE Book — resilience、SLO、error budget 思想

### 本仓库关联
- 《[08-工程化性能成本可靠性](./08-工程化性能成本可靠性.md)》：延迟、成本、可靠性手段清单。
- 《[07-评估与可观测性](./07-评估与可观测性.md)》：Agent eval 与 trace 体系。
- 《[09-安全与对齐](./09-安全与对齐.md)》：Prompt Injection、权限隔离、Guardrails。
- 《[14-Agent工程细节与可靠执行](./14-Agent工程细节与可靠执行.md)》：结构化计划、状态机、异常兜底。
- 《[16-Agent Loop专题](./16-Agent%20Loop专题.md)》：Agent loop、停止条件、上下文增长。
- 《[19.1-Agent推理加速落地](./19.1-Agent推理加速落地.md)》：Agent 延迟和成本优化。

---

## 二十八、核心要点速记

1. 生产级 Agent 是系统工程，不是聪明 prompt。
2. 第一原则：能不用 Agent 就不用；能用 workflow 就不要交给模型动态决策。
3. 生产 Agent 必须有结构化 run state，message history 只是上下文投影。
4. Plan 要变成 typed DAG，执行要变成状态机。
5. Tool Gateway 是真实安全边界：权限、schema、幂等、审计、沙箱、HITL 都在这里。
6. Guardrails 要覆盖输入、计划、工具、输出四层。
7. 外部内容默认不可信，RAG/网页/邮件只能作为 evidence，不能当系统指令。
8. 长任务必须 durable execution：checkpoint、resume、pause、replay、compensation。
9. Eval 要覆盖 outcome、step、trajectory；线上 failure 必须进入数据飞轮。
10. Trace 是 Agent 的黑匣子，必须串起模型、工具、状态、策略、成本和业务结果。
11. prompt/model/tool/schema/RAG/policy 都是版本化发布对象。
12. 上线门槛：任务合同、state、工具治理、安全、eval、trace、预算、灰度、回滚。
