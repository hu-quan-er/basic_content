# Agent Runtime 主流实现对比

> 目标：从 Runtime 原语的角度比较主流 Agent 框架，而不是只比较 API 写法。重点看：执行模型、状态管理、checkpoint、HITL、工具治理、多 Agent、上下文/记忆、可观测与生产适配。

---

## 本文职责边界

| 本文负责 | 本文不展开 |
|---|---|
| 按 Runtime 原语比较 LangGraph、Temporal、OpenAI/Claude SDK、AutoGen、CrewAI 等实现 | Runtime 基础概念见 [20-Agent Runtime专题.md](./20-Agent%20Runtime专题.md)；可运行最小示例见 [20.1-Agent Runtime完整实现.md](./20.1-Agent%20Runtime完整实现.md) |

---

## 一、对比维度

评价一个 Agent Runtime，不只看"能不能调用工具"，而要看下面这些维度：

| 维度 | 关键问题 |
|---|---|
| 执行模型 | 是 while loop、graph、workflow、team chat，还是 durable workflow？ |
| 状态模型 | 状态是否显式？谁读写？是否可 merge？ |
| 持久化 | 是否有 checkpoint、session、thread、save/load？ |
| 暂停恢复 | 是否支持 human approval、interrupt、resume？ |
| 工具治理 | 工具 schema、权限、重试、幂等、沙箱怎么做？ |
| 多 Agent | handoff、group chat、supervisor、hierarchy、remote task 如何建模？ |
| 上下文工程 | 历史、记忆、artifact、RAG 如何进入 prompt？ |
| 可观测 | 是否有事件流、trace、span、token/cost 统计？ |
| 生产部署 | 是否容易接 API、队列、数据库、权限、审计、灰度？ |

---

## 二、总览矩阵

| 实现 | Runtime 形态 | 状态/持久化 | 暂停恢复 | 多 Agent | 最适合 |
|---|---|---|---|---|---|
| LangGraph | 显式状态图 runtime | state + checkpointer + thread | interrupt/resume 强 | subgraph/supervisor/swarm | 复杂状态机、长流程、生产 Agent |
| OpenAI Agents SDK | Runner 驱动的轻量 runtime | session + 应用状态 | 需结合应用层审批 | handoff 一等公民 | OpenAI-native Agent、工具、handoff、trace |
| Google ADK | Agent 应用 runtime | session/state/artifact/memory | 可通过事件和 session 承载 | sub-agent/multi-agent | Google 生态、应用化 Agent |
| AutoGen | 多 Agent 消息 runtime | team/message state save/load | 可用 user proxy/termination 组合 | group chat/team 强 | 对话式多 Agent 协作 |
| CrewAI | 角色任务流程 runtime | crew/task/flow 状态 | 流程级 HITL 能力 | sequential/hierarchical/process | 业务角色分工、流程编排 |
| LlamaIndex Workflows | 事件驱动 workflow runtime | context/state | workflow step 可中断扩展 | AgentWorkflow | RAG + Agent + workflow |
| Semantic Kernel Agents | 企业插件式 agent runtime | chat history/thread | selection/termination 策略 | AgentGroupChat | .NET/企业插件生态 |
| Letta/MemGPT | 长期记忆 agent runtime | memory blocks + archival/recall | 长会话状态 | 可组合 agent | 长期个性化/记忆型 Agent |
| Temporal + 自研 Agent | durable workflow runtime | event sourced workflow history | signal/query/timer/activity | 需自建 Agent 抽象 | 强可靠、长事务、严格审计 |

---

## 三、LangGraph

官方入口：

- Overview: <https://docs.langchain.com/oss/python/langgraph/overview>
- Persistence: <https://docs.langchain.com/oss/python/langgraph/persistence>
- Interrupts: <https://docs.langchain.com/oss/python/langgraph/interrupts>

### 3.1 Runtime 模型

LangGraph 的核心不是"聊天框架"，而是一个面向 Agent 的图运行时：

```text
StateGraph
  nodes: model node / tool node / router node / human node
  edges: next step routing
  state: shared structured state
  reducers: 并发或多分支状态合并
  checkpointer: 持久化 thread/run 状态
```

典型执行：

```text
input
  -> node: call_model
  -> conditional edge
  -> node: tools
  -> node: call_model
  -> END
```

### 3.2 状态管理

LangGraph 的强项是显式状态。开发者定义一个 state schema，每个 node 读写 state 的一部分。

```python
class State(TypedDict):
    messages: Annotated[list[AnyMessage], add_messages]
    order: dict | None
    approval: dict | None
```

要点：

| 原语 | 作用 |
|---|---|
| `StateGraph` | 定义状态和节点 |
| node | 接收 state，返回 state patch |
| reducer | 多个 patch 如何合并 |
| checkpointer | 按 thread 保存状态 |
| `thread_id` | 一条持续会话/运行链路 |

### 3.3 Checkpoint 与 durable execution

LangGraph 的 checkpointer 让每一步后的状态可恢复。实际生产中可以用内存、SQLite、Postgres 等 checkpointer。

这解决了几个核心问题：

- worker 崩溃后可从最近状态继续
- human interrupt 后可 resume
- 可以查看历史状态
- 可以做 time travel/debug
- 多轮 session 不必把所有状态塞进 prompt

### 3.4 HITL：interrupt/resume

LangGraph 的 `interrupt` 适合把人类输入变成运行时暂停点：

```text
node wants high-risk action
  -> interrupt({action, args, risk})
  -> runtime persists state
  -> human response arrives
  -> graph resumes
```

这比在业务代码里同步等待审批更接近生产模型。

### 3.5 适合场景

- 任务路径复杂，有多分支、多循环、多 Agent。
- 必须显式控制状态读写。
- 需要 checkpoint、resume、interrupt。
- 需要 debug 每个 step。
- Agent 不是一次聊天，而是一个长流程。

### 3.6 需要补足的点

LangGraph 给了强 runtime 原语，但生产系统仍需自己补：

- 工具权限、幂等和审计
- 租户隔离
- 业务 API 层
- deployment/worker/queue 策略
- 成本与预算治理
- 组织级 policy

---

## 四、OpenAI Agents SDK

官方入口：

- Running agents: <https://openai.github.io/openai-agents-python/running_agents/>
- Handoffs: <https://openai.github.io/openai-agents-python/handoffs/>
- Sessions: <https://openai.github.io/openai-agents-python/sessions/>
- Tracing: <https://openai.github.io/openai-agents-python/tracing/>

### 4.1 Runtime 模型

OpenAI Agents SDK 的核心抽象是：

```text
Agent
  instructions
  tools
  handoffs
  guardrails

Runner
  run / run_sync / run_streamed
  drives model-tool-handoff loop
```

`Runner` 会持续执行：

```text
model call
  -> maybe tool call
  -> maybe handoff
  -> maybe final output
```

直到产生 final output 或达到限制。

### 4.2 状态与 session

SDK 提供 session 能力来保存对话上下文。它适合轻量会话型 Agent，但如果你要做复杂业务状态，仍建议在应用层维护结构化 `RunState`。

推荐生产组合：

```text
OpenAI Agent/Runner
  + App RunState
  + DB checkpoint
  + Tool Gateway
  + approval workflow
  + tracing export
```

### 4.3 Handoff

OpenAI Agents SDK 把 handoff 作为一等能力：一个 Agent 可以把任务交给另一个 Agent。

Runtime 设计时要注意：

| 问题 | 建议 |
|---|---|
| handoff 传什么 | contract + 必要上下文，不传全量历史 |
| handoff 后谁负责 | 明确 active agent |
| 子 Agent 结果如何合并 | 结构化输出或 state patch |
| handoff 失败怎么办 | fallback 或转人工 |

### 4.4 Tracing

SDK 内置 tracing，适合观察：

- model calls
- tool calls
- handoff
- guardrail
- run flow

生产中还应把 trace 与业务 `run_id`、`tenant_id`、`user_id` 对齐。

### 4.5 适合场景

- 希望快速构建 OpenAI-native Agent。
- 工具调用和 handoff 是主需求。
- 想使用官方 tracing、guardrails、structured output。
- Agent 复杂度中等，应用层可以承载业务 runtime。

### 4.6 需要补足的点

- 复杂 checkpoint 和 state migration
- 高风险工具审批状态机
- 跨系统队列/worker
- 副作用工具幂等
- 多租户权限和审计

---

## 五、Google Agent Development Kit

官方入口：

- ADK Docs: <https://google.github.io/adk-docs/>
- Sessions: <https://google.github.io/adk-docs/sessions/>
- Artifacts: <https://google.github.io/adk-docs/artifacts/>

### 5.1 Runtime 模型

Google ADK 更像一个 Agent 应用开发运行时，强调：

```text
Agent
Runner
Session
State
Event
Artifact
Memory
Tool
```

这套命名本身就很接近生产 runtime 的拆分。

### 5.2 Session、State、Artifact

ADK 的重要价值是把运行中不同对象区分开：

| 对象 | 含义 |
|---|---|
| Session | 一段用户与 Agent 的交互容器 |
| State | session 内的结构化状态 |
| Event | 运行过程中产生的事件 |
| Artifact | 文件、大对象、中间产物 |
| Memory | 可跨 session 使用的记忆 |

这比只维护 chat messages 更工程化。

### 5.3 多 Agent

ADK 支持多 Agent 组合，适合把主 Agent、工具型 Agent、专门领域 Agent 分层。

Runtime 侧要关注：

- 子 Agent 是否共享 session state
- artifact 引用如何传递
- 子 Agent 结果如何写回
- 事件流如何合并
- 权限是否继承或降权

### 5.4 适合场景

- 使用 Google/Gemini 生态。
- 希望用 session/state/artifact/memory 的应用化模型。
- 构建企业内部 Agent 应用。
- 需要把 Agent 运行过程变成明确事件流。

---

## 六、AutoGen

官方入口：

- AgentChat Teams: <https://microsoft.github.io/autogen/stable/user-guide/agentchat-user-guide/tutorial/teams.html>
- State: <https://microsoft.github.io/autogen/dev/user-guide/agentchat-user-guide/tutorial/state.html>

### 6.1 Runtime 模型

AutoGen 的特色是多 Agent 消息运行时：

```text
AssistantAgent
UserProxyAgent
Team / GroupChat
Termination condition
Message passing
```

它更像"多角色协作对话 runtime"，而不是显式状态图。

### 6.2 Team State

AutoGen 支持保存和加载 team/agent 状态，适合：

- 中断后恢复多 Agent 对话
- 保存 group chat 历史
- 记录每个 Agent 的状态
- 继续未完成协作

### 6.3 Termination

多 Agent 对话最容易出现的问题是停不下来。AutoGen 通常通过 termination condition 控制：

```text
max messages
keyword termination
external condition
task completion signal
```

生产中应额外加：

- 成本预算
- 工具次数预算
- wall-clock timeout
- 无进展检测
- 人工接管条件

### 6.4 适合场景

- 研究员、规划员、执行员、审阅员这类对话式协作。
- 需要多个 Agent 互相发消息。
- 需要快速实验多 Agent pattern。
- 需要保存/恢复 team 状态。

### 6.5 需要补足的点

- 强类型业务状态
- 工具副作用治理
- 审批状态机
- 多 Agent 上下文最小化传递
- 生产级权限/审计

---

## 七、CrewAI

官方入口：

- Crews: <https://docs.crewai.com/en/concepts/crews>
- Processes: <https://docs.crewai.com/en/concepts/processes>
- Flows: <https://docs.crewai.com/en/concepts/flows>

### 7.1 Runtime 模型

CrewAI 以业务角色和任务为中心：

```text
Agent
Task
Crew
Process
Flow
```

它适合表达：

```text
Researcher -> Writer -> Reviewer
Sales Agent -> Enrichment Task -> Outreach Task
```

### 7.2 Process

常见 process：

| Process | 含义 |
|---|---|
| sequential | 任务顺序执行 |
| hierarchical | manager/supervisor 分配任务 |
| flow | 更显式的事件/步骤控制 |

CrewAI 的 runtime 重点是"谁做哪个任务、按什么流程做"。

### 7.3 适合场景

- 业务流程天然能拆成角色和任务。
- 快速搭建 research/report/sales/ops 类多 Agent。
- 需要相对清晰的顺序或层级协作。
- 非常适合教学和原型。

### 7.4 需要补足的点

CrewAI 的抽象比较高层，生产里通常还要补：

- 底层 checkpoint 和 resume 策略
- 工具幂等和权限
- 精细状态 schema
- 高风险 HITL 状态机
- 复杂 conflict merge

---

## 八、LlamaIndex Workflows / AgentWorkflow

官方入口：

- AgentWorkflow: <https://docs.llamaindex.ai/en/stable/examples/agent/agent_workflow_basic/>
- Workflows: <https://docs.llamaindex.ai/en/stable/module_guides/workflow/>

### 8.1 Runtime 模型

LlamaIndex 的强项是 RAG 和数据连接，Workflows 把 agent 步骤变成事件驱动流程：

```text
Event
  -> Step
  -> Context
  -> Event
```

`AgentWorkflow` 则提供更高层的 Agent 执行封装。

### 8.2 Context 与 RAG

如果 Agent 的核心工作是：

- 查文档
- 查索引
- 多步检索
- 总结生成
- 结构化抽取

LlamaIndex 的 runtime 与 RAG 组件结合会很自然。

### 8.3 适合场景

- 企业知识库 Agent。
- 文档分析、多步检索、生成报告。
- 需要把检索节点、处理节点、Agent 节点串成 workflow。
- 需要大量数据连接器和 index 能力。

### 8.4 需要补足的点

- 强审批流程
- 复杂多 Agent 状态合并
- 跨系统 durable execution
- 细粒度工具权限治理

---

## 九、Semantic Kernel Agents

官方入口：

- Semantic Kernel Agents: <https://learn.microsoft.com/en-us/semantic-kernel/frameworks/agent/>

### 9.1 Runtime 模型

Semantic Kernel 的核心是 enterprise plugin/kernel 生态，Agents 在此之上提供：

```text
Agent
AgentThread / ChatHistory
AgentGroupChat
Selection Strategy
Termination Strategy
Kernel plugins
```

它适合 Microsoft/.NET 企业技术栈。

### 9.2 AgentGroupChat

`AgentGroupChat` 关注多 Agent 对话中的两个关键问题：

| 策略 | 作用 |
|---|---|
| selection strategy | 下一轮由哪个 Agent 发言 |
| termination strategy | 什么时候结束 |

这和 Runtime 的调度器很接近。

### 9.3 适合场景

- .NET/C# 企业应用。
- 已经使用 Semantic Kernel plugin。
- 需要把 Agent 接入 Microsoft 生态。
- 需要多 Agent group chat 和可配置选择/终止策略。

### 9.4 需要补足的点

- durable checkpoint
- 高风险工具审批
- 复杂长期状态迁移
- 跨 Agent state patch 合并

---

## 十、Letta / MemGPT 类长期记忆 Runtime

官方入口：

- Letta Docs: <https://docs.letta.com/>

### 10.1 Runtime 模型

Letta/MemGPT 类系统的重点不是短任务 loop，而是长期存活的记忆型 Agent。

常见记忆结构：

| 记忆 | 含义 |
|---|---|
| core memory | 始终在上下文中的关键事实 |
| archival memory | 长期外部存储，可检索 |
| recall memory | 对话历史或事件回忆 |
| working context | 当前 prompt 窗口 |

### 10.2 Runtime 特征

这类 runtime 通常关注：

- agent identity
- memory self-editing
- long-lived session
- background memory compaction
- recall/retrieve/write policies

### 10.3 适合场景

- 长期个人助理。
- 用户偏好持续累积。
- 长期项目伴随式 Agent。
- 需要显式研究记忆写入/召回机制。

### 10.4 风险

- 错误记忆会长期污染行为。
- 隐私和删除权要求更高。
- memory injection 可能带来新的攻击面。
- 需要记忆评估和过期策略。

---

## 十一、Temporal + 自研 Agent Runtime

官方入口：

- Temporal Docs: <https://docs.temporal.io/>

### 11.1 Runtime 模型

Temporal 本身不是 Agent 框架，而是 durable workflow runtime。它提供：

```text
Workflow
Activity
Signal
Query
Timer
Retry policy
Workflow history
```

这些能力非常适合做生产 Agent 的底座。

### 11.2 为什么 Agent 可能需要 Temporal

如果任务具备下面特征，普通 in-process loop 不够：

| 特征 | 例子 |
|---|---|
| 长时间运行 | 深度研究、供应商尽调、代码迁移 |
| 需要外部等待 | 等审批、等邮件、等第三方回调 |
| 多个副作用工具 | 下单、退款、发通知、改配置 |
| 强审计要求 | 金融、医疗、企业内部系统 |
| 需要可靠重试 | worker 崩溃不能丢任务 |

### 11.3 组合方式

```text
Temporal Workflow
  -> Agent Runtime Step
  -> Model Activity
  -> Tool Activity
  -> Approval Signal
  -> Resume
```

一种稳健设计是：

- Temporal 管长事务、重试、timer、signal。
- Agent Runtime 管模型动作、工具契约、上下文、策略。
- 数据库管业务状态。
- Trace 系统管可观测。

### 11.4 适合场景

- 企业级长任务 Agent。
- 需要强恢复和审计。
- 有大量外部系统副作用。
- 需要把 Agent 变成可运营后台任务。

### 11.5 代价

- 开发复杂度更高。
- LLM 非确定性与 workflow determinism 要隔离，通常把 LLM 调用放 activity。
- 状态 schema 和版本迁移需要设计。
- 不适合只做轻量聊天 demo。

---

## 十二、MCP 与 A2A 在 Runtime 中的位置

### 12.1 MCP 不是 Runtime

MCP 解决的是工具、资源、提示词如何被 Host 发现和调用。

```text
Agent Runtime
  -> Tool Gateway
  -> MCP Client
  -> MCP Server
  -> tool/resource
```

Runtime 仍然要负责：

- 是否允许调用这个 MCP tool
- 参数是否安全
- 结果是否可注入上下文
- 是否需要审批
- 调用失败如何重试
- 工具结果如何写 event log

### 12.2 A2A 不是 Runtime

A2A 解决的是 Agent 与 Agent 跨组织、跨进程、跨供应商协作。

```text
Local Agent Runtime
  -> Handoff Router
  -> A2A Task
  -> Remote Agent Runtime
```

Runtime 仍然要负责：

- 本地 run 与远程 task 的映射
- context envelope 的最小化
- remote result 的导入
- state patch 合并
- 超时、取消、重试
- 审计和权限

---

## 十三、选型建议

### 13.1 按问题选

| 你的主要问题 | 优先考虑 |
|---|---|
| 状态分支复杂，需要 checkpoint/interrupt | LangGraph |
| 想最快构建 OpenAI tool/handoff Agent | OpenAI Agents SDK |
| 使用 Gemini/Google 生态并重视 session/artifact | Google ADK |
| 多个 Agent 要通过对话协作 | AutoGen |
| 业务流程天然是角色 + 任务 | CrewAI |
| 核心是文档/RAG/workflow | LlamaIndex Workflows |
| 企业 .NET/plugin 集成 | Semantic Kernel |
| 长期记忆和个性化 | Letta/MemGPT |
| 长事务、强可靠、审计 | Temporal + 自研 runtime |

### 13.2 按团队成熟度选

| 阶段 | 推荐 |
|---|---|
| 原型验证 | OpenAI Agents SDK / CrewAI / LlamaIndex AgentWorkflow |
| 多 Agent 实验 | AutoGen / CrewAI / LangGraph supervisor |
| 生产任务流 | LangGraph / Google ADK / 自研 runtime |
| 严格可靠执行 | LangGraph + DB checkpointer，或 Temporal + 自研 runtime |
| 企业平台化 | 统一 RunState + ToolGateway + Policy + Trace，再适配多个框架 |

### 13.3 按架构风险选

| 风险 | 更重要的 Runtime 能力 |
|---|---|
| 无限循环 | max turns、termination、progress detector |
| 上下文污染 | context builder、trust boundary、prompt injection guard |
| 重复副作用 | idempotency、transaction log、compensation |
| 状态丢失 | checkpoint、event log、resume |
| 多 Agent 混乱 | state owner、handoff contract、context envelope |
| 成本失控 | budget manager、model router、cache |
| 线上不可排查 | trace、event replay、structured eval |

---

## 十四、一个统一参考抽象

如果要自研或封装多框架，可以用下面这组接口作为统一层：

```text
Runtime.create_run(agent_id, input, session_id) -> run_id
Runtime.step(run_id) -> RunStatus
Runtime.resume(run_id, resume_input) -> RunStatus
Runtime.cancel(run_id)
Runtime.get_state(run_id) -> RunState
Runtime.stream_events(run_id) -> EventStream

ModelAdapter.invoke(context, tools, schema) -> ModelAction
ToolGateway.dispatch(tool_call, principal, policy_context) -> ToolResult | PendingApproval
ContextBuilder.build(run_state) -> ModelContext
CheckpointStore.save(run_id, state)
EventLog.append(event)
PolicyEngine.evaluate(action, state) -> allow | deny | require_approval
```

这层抽象的好处：

- 可以把不同框架放到同一业务平台下。
- 不被某个 SDK 的状态格式锁死。
- 方便统一审计、权限、成本、trace。
- 方便做 A/B 和灰度。

---

## 十五、对比结论

没有一个框架等于完整生产级 Agent Runtime。

更准确的判断是：

```text
OpenAI Agents SDK:
  很适合快速获得官方 runner/tool/handoff/tracing 能力。

LangGraph:
  最像显式 Agent 状态机 runtime，适合复杂流程和恢复。

Google ADK:
  把 session/state/artifact/memory/event 拆得比较工程化。

AutoGen:
  强在多 Agent 对话和 team 协作。

CrewAI:
  强在业务角色和任务流程表达。

LlamaIndex:
  强在 RAG 数据工作流和 AgentWorkflow。

Semantic Kernel:
  强在企业 plugin/kernel 生态。

Letta:
  强在长期记忆 Agent。

Temporal:
  强在 durable execution，但需要自己定义 Agent Runtime 抽象。
```

生产选型时，建议先设计自己的 `RunState / ToolGateway / Policy / Checkpoint / EventLog`，再决定底层用哪个框架承载 loop、graph 或 workflow。否则很容易把业务可靠性绑定在某个 demo 级抽象上。
