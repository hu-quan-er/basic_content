# A2A 上下文同步与 Handoff 设计

> 目标：专门分析 A2A 协议下多 Agent 如何同步任务状态、传递上下文、进行 handoff、处理多轮任务和跨组织边界。本篇关注“协议边界和交接合同”，内部状态模型见 [13.1.1-多Agent状态模型与读写契约.md](./13.1.1-多Agent状态模型与读写契约.md)。

---

## 一、A2A 的关键边界

A2A 面向独立 Agent 系统之间的互操作。远程 Agent 对调用方来说是黑盒：

- 调用方能看到 Agent Card、Task、Message、Artifact、公开状态。
- 调用方不应该看到远程 Agent 的 system prompt。
- 调用方不应该复制远程 Agent 的内部 memory。
- 调用方不应该要求远程 Agent 暴露 chain-of-thought。
- 远程 Agent 也不应该拿到调用方的全量上下文。

所以 A2A 下的状态同步不是“共享内部大脑”，而是：

```text
通过 Task 对齐工作单元
通过 Message 交换必要上下文
通过 DataPart 传结构化状态投影
通过 Artifact 返回可复查产物
通过 status/stream/push 同步进度
通过 metadata 关联 trace、版本和外部系统 id
```

---

## 二、A2A 对象与状态管理含义

| A2A 对象 | 状态管理含义 | 设计要点 |
|----------|--------------|----------|
| Agent Card | 能力、认证、端点、技能、输入输出模式 | 只暴露公开能力，不暴露内部实现 |
| Task | 长任务工作单元，有生命周期 | 与内部 task 做 id 映射 |
| Message | 一次通信事件 | 可放 text、file、data part |
| Part | Message / Artifact 的内容片段 | data part 适合结构化 handoff |
| Artifact | 远程 Agent 的产物 | 产物应可引用、可版本化 |
| Task Status | submitted、working、input-required、completed、failed 等 | 驱动调用方状态机 |
| Streaming / Push | 进度和状态变化同步 | 要做断线恢复和幂等 |
| contextId | 相关任务的逻辑上下文 | 不等于完整会话历史 |

---

## 三、A2A Context Envelope

跨边界传递上下文时，建议使用最小上下文包。

```json
{
  "context_envelope": {
    "schema_version": "1.0",
    "tenant_ref": "tenant_hash_or_external_org_id",
    "requester": {
      "type": "agent",
      "agent_id": "company_a.procurement_agent",
      "delegated_by": "user_123"
    },
    "task_ref": {
      "external_task_id": "a2a_task_789",
      "caller_task_id": "internal_task_456",
      "context_id": "vendor_onboarding_2026_q3",
      "trace_id": "trace_abc"
    },
    "goal": "完成供应商安全尽调",
    "constraints": [
      "不得访问供应商报价以外的商业敏感信息",
      "所有结论必须引用 artifact"
    ],
    "state_snapshot": {
      "state_version": 22,
      "facts": [
        {
          "id": "fact_001",
          "claim": "供应商已提供 SOC2 Type II 报告",
          "evidence_refs": ["artifact:soc2_report_meta"],
          "confidence": 0.95
        }
      ],
      "open_questions": [
        "渗透测试报告是否在 12 个月内"
      ]
    },
    "artifact_refs": [
      {
        "id": "soc2_report_meta",
        "kind": "file",
        "access": "scoped_url",
        "expires_in": "24h"
      }
    ],
    "return_contract": {
      "expected_artifacts": ["risk_assessment_report"],
      "expected_data_fields": ["risk_level", "required_remediations"],
      "max_turns": 5
    }
  }
}
```

关键规则：

- `state_snapshot` 是内部状态投影，不是全量 state。
- `artifact_refs` 传引用，不默认传全文。
- `tenant_ref` 可以是外部引用，不必泄露真实内部 id。
- `return_contract` 明确远程 Agent 应返回什么。
- 敏感权限、凭证、内部 prompt 不进入 envelope。

---

## 四、内部状态到 A2A 对象的映射

| 内部对象 | A2A 边界对象 | 映射规则 |
|----------|--------------|----------|
| Agent capability registry | Agent Card | 只暴露公开能力、认证方式、输入输出模式 |
| Internal task state | A2A Task | 暴露任务生命周期和必要摘要 |
| State patch | Message DataPart / internal event | 对外传 result/proposal，不暴露内部 reducer |
| Context view | Message parts | 只传最小必要上下文 |
| Artifact metadata | Artifact / FilePart | 大对象传引用，权限外置 |
| Trace span | metadata / external trace id | 只传 correlation id |
| Long-term memory | 不直接映射 | 必要时转成已授权事实或偏好 |
| Policy decision | task error / input-required / metadata | 传结果，不传内部策略细节 |

---

## 五、A2A 多轮同步状态机

跨组织多轮任务不要把每一轮当成新任务。典型流程：

```text
caller creates task
  -> remote task submitted
  -> remote task working
  -> remote emits progress event
  -> remote returns input-required
  -> caller gathers missing approval/data
  -> caller sends message to same task.id
  -> remote continues working
  -> remote completes with artifact
  -> caller imports artifact into internal state
```

调用方内部维护映射表：

| 字段 | 说明 |
|------|------|
| `internal_task_id` | 本系统任务 id |
| `remote_agent_id` | 被调 Agent |
| `a2a_task_id` | 远程 A2A task |
| `context_id` | 相关任务上下文 |
| `last_remote_status` | 最近一次远程状态 |
| `last_remote_event_id` | 去重和恢复用 |
| `state_version_at_call` | 发起调用时的内部状态版本 |
| `imported_artifacts` | 已导入的远程产物 |
| `pending_input_request` | 远程要求补充的输入 |

这样可以处理：

- 断线重连。
- 重复 webhook。
- 远程 task 已完成但本地导入失败。
- 本地状态在远程执行期间已变化。
- 同一个 context 下有多个远程 task。

---

## 六、Handoff Contract

Handoff 不是“把聊天记录转发给另一个 Agent”，而是控制权和责任边界的交接。

一个生产级 handoff 至少包含：

| 字段 | 作用 |
|------|------|
| `goal` | 下游 Agent 要完成什么 |
| `current_state_summary` | 当前状态摘要 |
| `constraints` | 不能违反的约束 |
| `relevant_artifacts` | 需要读取的工件 |
| `open_questions` | 未解决问题 |
| `decisions_made` | 已确定且不要重复讨论的决定 |
| `forbidden_actions` | 下游不能做什么 |
| `write_scope` | 下游可以更新哪些状态字段 |
| `expected_output` | 下游应该产出什么 |
| `return_contract` | 下游如何回传结果 |

示例：

```json
{
  "handoff_id": "handoff_20260607_001",
  "from_agent": "research_agent",
  "to_agent": "writer_agent",
  "task_id": "task_market_entry_001",
  "state_version": 12,
  "goal": "生成管理层报告",
  "current_state_summary": "调研完成，数据可信度中等，价格信息缺口仍存在。",
  "constraints": [
    "不得编造未验证数据",
    "所有关键结论必须引用 artifact"
  ],
  "relevant_artifacts": [
    {
      "artifact_id": "research_notes_v3",
      "type": "markdown",
      "version": 3,
      "access_scope": "task"
    }
  ],
  "open_questions": [
    "竞品 B 的企业版价格缺少公开来源"
  ],
  "forbidden_actions": [
    "不要直接联系客户",
    "不要修改预算字段"
  ],
  "expected_output": {
    "type": "executive_report",
    "max_pages": 8,
    "must_include": ["market_size", "risks", "assumptions"]
  },
  "return_contract": {
    "write_fields": ["draft_report", "risk_register"],
    "emit_artifacts": ["report.md"],
    "requires_review": true
  }
}
```

---

## 七、A2A 下的六种同步模式

| 模式 | 适用场景 | 优点 | 风险 |
|------|----------|------|------|
| Message Summary | 简单交接 | 实现容易、token 少 | 摘要丢信息 |
| Structured Handoff | 生产交接 | 可验证、可测试 | 需要 schema |
| Artifact Reference | 大文件/证据交接 | 省 token、可复查 | 需要权限和版本 |
| Task Continuation | 多轮任务 | 状态由被调 Agent 保持 | 调用方只能看到公开状态 |
| Event Stream | 长任务进度同步 | 实时性好 | 断线恢复要设计 |
| Push Notification | 异步任务 | 适合长耗时任务 | 回调安全与幂等复杂 |

推荐组合：

```text
短任务：Message Summary + final Artifact
长任务：Task Continuation + Streaming/Push + Artifact Reference
跨组织：Structured Handoff + explicit auth + minimal disclosure
高风险：Structured Handoff + human approval + immutable event log
```

---

## 八、导入远程结果

远程 Agent 返回结果后，不要直接覆盖内部 state，应走导入流程：

```text
receive remote artifact/message
  -> verify remote task id
  -> verify sender/auth
  -> deduplicate by event id
  -> validate artifact schema
  -> map remote fields to internal fields
  -> create state patch
  -> run policy/verifier
  -> commit internal state
  -> link remote artifact to internal artifact store
```

导入 patch 示例：

```json
{
  "patch_id": "import_a2a_result_001",
  "source": "a2a:security_vendor_agent",
  "remote_task_id": "a2a_task_789",
  "base_state_version": 22,
  "operations": [
    {
      "op": "replace",
      "path": "/vendor_security/risk_level",
      "value": "medium"
    },
    {
      "op": "add",
      "path": "/artifacts/references/-",
      "value": {
        "artifact_id": "remote:risk_assessment_report",
        "local_copy": "artifact:vendor_risk_report_v1",
        "producer": "security_vendor_agent"
      }
    }
  ]
}
```

---

## 九、跨组织安全边界

### 9.1 最小披露

跨 A2A 传递时只传：

- 完成任务必要的信息。
- 明确授权可见的字段。
- 可撤销、可过期的 artifact 链接。
- 不可伪造的身份和权限声明。

不要传：

- system prompt。
- chain-of-thought。
- 工具凭证。
- 无关用户隐私。
- 其他租户数据。
- 未过滤的外部网页内容。

### 9.2 跨 Agent 注入风险

风险路径：

```text
外部网页 / 邮件 / 文档
  -> Research Agent 读取
  -> 写入 handoff summary
  -> Tool Agent 继承恶意指令
  -> 执行越权工具
```

防护：

- 外部内容作为 data，不作为 instruction。
- handoff payload 区分 `facts` 和 `instructions`。
- shared memory 写入前做来源标注和安全扫描。
- 工具执行前重新检查用户授权和任务边界。
- 高风险工具不接受来自其他 Agent 的自然语言授权。

---

## 十、场景：HR Agent 调 IT Agent

```text
HR Supervisor
  -> discover IT Agent Card
  -> create A2A Task: provision account
  -> send structured handoff as Message DataPart
  -> receive input-required: Finance role needs manager approval
  -> collect approval
  -> continue same task.id
  -> receive Artifact: it_ticket_123
  -> update internal onboarding state
```

HR 到 IT 的 handoff：

```json
{
  "from_agent": "hr_agent",
  "to_agent": "it_agent",
  "goal": "为新员工开通基础账号并准备设备",
  "current_state_summary": "HR 信息已收集完成，员工属于 Finance 部门。",
  "constraints": [
    "只可读取员工入职必要字段",
    "不得读取薪资字段",
    "Finance 权限需 Security 审批后才能开通"
  ],
  "relevant_artifacts": ["artifact:hr_form_v1"],
  "write_scope": [
    "/subtasks/it_account",
    "/artifacts"
  ],
  "expected_output": {
    "state_patch": true,
    "artifacts": ["it_ticket"]
  }
}
```

注意这里有两套状态：

- HR 系统内部 onboarding state。
- IT Agent 暴露的 A2A Task state。

两者通过 `task_id`、`context_id`、`trace_id`、`artifact_id` 对齐，但不互相暴露内部 memory。

---

## 十一、A2A 落地清单

- [ ] Agent Card 只暴露公开能力和认证方式。
- [ ] 内部 task id 与 A2A task id 有映射表。
- [ ] context envelope 只包含最小必要上下文。
- [ ] data part 传结构化事实，artifact 传产物。
- [ ] 每个远程结果导入为 internal state patch。
- [ ] streaming/push 事件有去重 id。
- [ ] input-required 能恢复到同一 task。
- [ ] artifact link 有权限和过期时间。
- [ ] 远程 Agent 结果不直接覆盖高风险字段。
- [ ] trace id 能贯通内部和 A2A 边界。
- [ ] 跨组织不共享 prompt、chain-of-thought、内部 memory。

---

## 十二、延伸阅读

- A2A Core Concepts：<https://a2a-protocol.org/latest/topics/key-concepts/>
- A2A Protocol Definition：<https://a2a-protocol.org/latest/specification/>
- A2A GitHub specification：<https://github.com/a2aproject/A2A/blob/main/docs/specification.md>
- 本仓库：[13-A2A与Agent互操作协议.md](./13-A2A与Agent互操作协议.md)
