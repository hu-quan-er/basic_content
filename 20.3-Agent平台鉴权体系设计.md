# Agent 平台鉴权体系设计

> 目标：系统回答「一个 Agent Runtime 管理平台的鉴权体系到底该怎么设计」。鉴权在整个知识库里被反复提到（工具权限、A2A、安全生命周期），但从未被当作一个完整体系单独设计过。本文把它当作平台的一等子系统来展开：**是谁（认证）、代表谁（委派）、被允许对什么资源做什么（授权）、凭证怎么管、这条链路怎么在多跳里安全传递**。

---

## 本文职责边界

| 本文负责 | 本文不展开 |
|---|---|
| 平台级鉴权体系设计：主体与身份模型、认证（AuthN）、授权（AuthZ / PEP-PDP-PAP）、委派与 On-Behalf-Of、Token 架构与 Token Exchange、凭证与密钥管理、混淆代理防护、多租户隔离、端到端鉴权流程 | 威胁建模与安全生命周期全景见 [27-Agent安全生命周期专题.md](./27-Agent安全生命周期专题.md)；工具平台的注册/幂等/审计见 [23-Tool Gateway与工具平台专题.md](./23-Tool%20Gateway与工具平台专题.md)；模型调用治理见 [26-Model Gateway与模型治理专题.md](./26-Model%20Gateway与模型治理专题.md)；A2A 跨组织协议见 [13-A2A与Agent互操作协议.md](./13-A2A与Agent互操作协议.md)；Runtime 生命周期见 [20-Agent Runtime专题.md](./20-Agent%20Runtime专题.md)；HITL 见 [16.3-Agent Loop人在回路.md](./16.3-Agent%20Loop人在回路.md) |

---

## 一、一句话定义

**鉴权体系要在每一次调用上回答三件事，并把答案安全地沿多跳链路传下去：**

```text
谁在调用？          → AuthN（认证）：证明主体身份
它代表谁在调用？     → Delegation（委派）：区分「执行者」与「授权来源」
它被允许做这件事吗？ → AuthZ（授权）：主体 × 动作 × 资源 × 上下文 → 决策
```

在传统 Web 系统里这三件事被压缩成「用户登录后拿一个 session，后端查它有没有权限」。**Agent 平台里这个模型直接失效**，因为执行动作的不再是用户本人，而是一个代表用户、自主决策、还会再调下游的 Agent。

**核心判断**：Agent 鉴权的难点不在「有没有权限」，而在「**这次动作的授权，到底是谁的、怎么在 Agent 多跳链路里不失真地传下去**」。

---

## 二、为什么 Agent 的鉴权比传统系统难

| 难点 | 传统系统 | Agent 平台 |
|---|---|---|
| 主体爆炸 | 用户 + 后端服务 | 用户 + Agent + 子 Agent + 工具 + 模型，每个都是独立 principal |
| 委派链 | 一跳（用户→后端） | 多跳（用户→Runtime→工具→外部 SaaS→…），授权要逐跳传 |
| 非确定性 | 代码路径固定，权限可静态预判 | 模型自主决定调哪个工具，权限必须每步动态判 |
| 混淆代理 | 后端权限受代码约束 | Agent 常持高权限，被注入后会「替攻击者行权」（confused deputy） |
| 长时/异步 | 请求几秒内结束 | 一个 run 可能挂起数小时到数天，用户 session token 早已过期 |
| 供应链 | 依赖固定 | 工具/MCP/第三方 SaaS 各有各的凭证和授权模型 |

这六条决定了：**不能把用户的登录态直接透传给 Agent 当作万能钥匙**。这是整个体系的第一原则。

---

## 三、四类主体与身份模型

先把「谁」讲清楚。平台里至少有四类 principal，各有独立身份：

| 主体 | 身份来源 | 凭证类型 | 生命周期 |
|---|---|---|---|
| 终端用户 End User | 企业 IdP / OIDC 登录 | ID Token / 会话 | 用户会话级 |
| Agent（工作负载） | 工作负载身份（SPIFFE SVID / workload OIDC / mTLS 证书） | 短期证书 / 服务 token | 部署级，短期轮换 |
| Run / 会话主体 | 由「用户 × Agent」派生 | run-scoped capability token | 单个 run，短期 |
| 下游工具 / 系统 | 各系统自身身份体系 | audience-bound access token | 单次调用 / 连接级 |

关键设计:

- **Agent 有独立身份,不是用户 token 的透明代理**。每个 Agent 部署有自己的工作负载身份(类似微服务的 service identity),用短期证书而非长期 API key。这样审计能区分「用户干的」和「Agent 替用户干的」,被攻破也能单独吊销。
- **Run 是一个派生主体**。一次执行的有效身份 = 用户身份 ⊕ Agent 身份,在 run 启动时固化,贯穿整个 run,写进每条 trace 和每次下游调用。
- 子 Agent、工具各自还有身份,委派时逐层降权(见第五节)。

---

## 四、认证层（AuthN）：证明「你是谁」

现有文档几乎没写认证,这里补齐。三条认证路径:

| 场景 | 机制 | 要点 |
|---|---|---|
| 用户 → 平台 | OIDC / 企业 SSO | 拿到用户 ID Token,提取 `sub`/`tenant`/`groups`,作为委派的授权来源 |
| Agent / 工作负载 → 平台 | SPIFFE SVID / workload OIDC / mTLS | **不用长期 API key**;用可自动轮换的短期证书证明「我是哪个 Agent 部署」 |
| 服务 → 服务(Runtime↔Gateway↔工具) | mTLS + 短期服务 token | 内部调用双向认证,防止任意进程冒充 Runtime 调 Tool Gateway |

**反模式**:给每个 Agent 发一个写死在配置里的长期 API key。一旦泄漏无法区分调用者、无法快速吊销、无法审计到具体部署。用工作负载身份 + 短期证书替代。

---

## 五、委派与 On-Behalf-Of（本体系最难、也最缺的部分）

这是 Agent 鉴权的核心。问题:**Agent 代表用户去调工具、工具再调外部 SaaS,怎么把「用户的授权」传下去,又不把用户的原始凭证交给整条链?**

### 5.1 反模式:透传用户 token

```text
用户 session token ──直接给──▶ Agent ──原样带──▶ 每个工具 ──原样带──▶ 外部 SaaS
```

后果:token 暴露面爆炸(链上每一跳都能拿它干任何事)、无法降权(工具只需读订单,却拿到用户全部权限)、无法归因(下游只看到「用户」,分不清是用户还是 Agent 发起)、无法及时撤销。

### 5.2 正确做法:逐跳换发、降权、绑定受众

用 **OAuth 2.0 Token Exchange(RFC 8693)** 的思路:每跳都不是「原样转发」,而是「拿旧凭证换一个新的、更小、绑定到下一跳受众、短期的凭证」。

```text
用户身份 + Agent 身份
        │  run 启动:mint 一个 run-scoped capability token
        │  scope = 这个 run 真正需要的最小集合
        ▼
     Runtime
        │  调工具前:token exchange → downscope 到「该工具 required scope」
        │  audience = 该工具 / 该 SaaS
        ▼
   Tool Gateway ──▶ 工具 ──▶ 外部 SaaS
```

三个关键机制:

1. **降权(downscoping)**：每跳只换到「下一跳真正需要的 scope」。读订单的工具永远拿不到写账单的权限。
2. **受众绑定(audience binding)**：换出的 token 的 `aud` 指定唯一接收方,被别处窃取也用不了(防止工具 A 拿到的 token 被用去调工具 B)。
3. **委派链留痕(actor / delegation claim)**：token 里同时记录 `sub`(代表谁——用户)和 `act`(实际执行者——哪个 Agent)。审计能完整还原「用户授权 → Agent 执行 → 工具落地」,这正是 confused deputy 溯源的关键。

```json
{
  "sub": "user:alice",            // 授权来源:代表谁
  "act": { "sub": "agent:refund-bot@v3" },  // 实际执行者
  "aud": "tool:billing-api",      // 只有账单工具能用
  "scope": "billing:refund:write",// 已降到最小
  "run_id": "run_8842",
  "tenant": "acme",
  "exp": 1799999999               // 短期
}
```

### 5.3 子 Agent 与 A2A 跨组织

- **子 Agent**：主 Agent 派子 Agent 时继续降权,子 Agent 的 scope ⊆ 父的 scope,不能提权。
- **跨组织 A2A**：委派要跨信任域,需要双方 IdP 建立联邦信任,远端只接受降权后、绑定到本域受众的 token,并对 `act` 链做校验。协议细节见 [13-A2A与Agent互操作协议.md](./13-A2A与Agent互操作协议.md)。

**核心判断**：委派的黄金规则是「**每跳换发、只减不增、绑定受众、全程留痕**」。任何「原样透传原始凭证」都是设计缺陷。

---

## 六、授权层（AuthZ）：判定「能不能做」

### 6.1 PEP / PDP / PAP / PIP 四角色

把「判权限」拆成职责清晰的四个角色(沿用 XACML 术语),避免权限逻辑散落在各处:

| 角色 | 职责 | 在平台里是谁 |
|---|---|---|
| PEP（执行点） | 拦截调用,问 PDP,执行决策 | Runtime 的 run admission、Tool Gateway、Model Gateway、RAG 检索层 |
| PDP（决策点） | 根据策略 + 主体 + 资源 + 上下文出决策 | 中央 Policy Engine（策略即代码,如 OPA / Cedar） |
| PAP（管理点） | 编写、版本化、发布策略 | 策略仓库 + 审核流程 |
| PIP（信息点） | 给 PDP 补充属性(用户 group、资源 owner、风险分) | 用户目录、资源元数据、Risk Scoring |

**要点**：决策集中(一个 PDP,策略即代码),执行分布(多个 PEP)。不要让每个工具各写各的 if-else 权限判断。

### 6.2 有效权限 = 多重交集（本库已有,这里讲清「每项从哪来、在哪评估」）

```text
effective_permission =
    user_permission        // AuthN 拿到的用户授权(PIP:用户目录)
  ∩ tenant_policy          // 租户级策略(PIP:租户配置)
  ∩ agent_role             // 这个 Agent 被允许的能力域
  ∩ run_session_scope      // 本次 run 委派下来的 scope(第五节 capability token)
  ∩ tool_required_scope    // 目标工具声明需要的最小 scope
  ∩ data_policy            // 数据分级 / DLP 约束
```

> 说明:本交集模型在 [23-Tool Gateway §4.4](./23-Tool%20Gateway与工具平台专题.md) 和 [27 §6.2](./27-Agent安全生命周期专题.md) 都出现过,本文是其唯一完整设计出处;那两处只保留局部视角并链接到这里。

关键:**取交集,不是取并集**。Agent 的有效权限永远 ≤ 用户权限,且 ≤ 本次 run 委派的 scope。任何一项不满足即拒。

### 6.3 RBAC / ABAC / ReBAC 怎么选

| 模型 | 适合 | Agent 场景够不够 |
|---|---|---|
| RBAC（角色） | 「谁属于什么角色」 | 不够:同一角色对不同资源应有不同权限 |
| ABAC（属性） | 「按主体/资源/环境属性判」 | 需要:按租户、数据分级、金额阈值、时间、风险分动态判 |
| ReBAC（关系） | 「A 是否是资源 R 的 owner/成员」 | 常需要:「只能操作自己创建的工单/自己账户的数据」 |

Agent 平台通常 **RBAC 打底(粗粒度能力域)+ ABAC/ReBAC 细化(资源级、租户级、关系级)**。纯 RBAC 撑不住「Agent 只能退自己经手的订单」这类要求。

### 6.4 决策不只是 allow/deny

沿用 [27 §6.1](./27-Agent安全生命周期专题.md) 的多值决策:`allow / deny / require_approval(转 HITL)/ require_redaction(脱敏后继续)/ require_sandbox / degrade(降级为草稿建议)`。授权层要能输出「有条件放行」,而不是二元开关。

---

## 七、凭证与密钥管理

三类凭证要分开治理,且**模型永远看不到明文**:

| 凭证类型 | 例子 | 存放 / 注入 |
|---|---|---|
| 平台自身凭证 | Runtime↔Gateway 的服务 token | 工作负载身份自动签发,短期轮换 |
| 用户委托的第三方凭证 | 用户授权的 Gmail / GitHub OAuth token | Secret Vault 按 user+tool 存,per-user connection,自动刷新,可撤销 |
| 工具/系统凭证 | 数据库口令、内部 API key | Vault 存,调用时由 Tool Gateway 注入,不进上下文 |

原则:

- **凭证与模型上下文严格隔离**：任何 secret 不进 prompt / 不进 memory / 不进 trace 明文(引用 [04.2 上下文工程](./04.2-Agent上下文工程.md) 的「凭证不进 context」)。模型只发出「调用 X 工具」的意图,凭证由 Tool Gateway 在执行时注入。
- **per-user 授权与刷新**：C 端助理连用户自己的 SaaS,每个用户单独 OAuth 授权、单独存 token、单独刷新和撤销;用户撤销授权即刻失效。
- **短期优先**：能用短期 token 就不用长期;必须长期的(如刷新令牌)只存 Vault,绝不下发到 Agent 运行上下文。

---

## 八、三个必须做对的控制

### 8.1 混淆代理（Confused Deputy）防护

Agent 常有比单个用户更高的静态权限。若不加约束,一旦被提示注入,攻击者就能借 Agent 的高权限行权。防护:

- 每次工具调用的授权,基于**本次调用的真实委派链(用户→Agent 的 capability token)**,而不是 Agent 的静态角色。
- 有效权限取交集(§6.2),Agent 静态权限再高,也被「本 run 委派的 scope」和「用户权限」夹住。
- **不可信输入不能提权**：来自网页/文档/工具返回的内容属 untrusted,不能改变授权决策(信任边界见 [27](./27-Agent安全生命周期专题.md)、[09](./09-安全与对齐.md)）。

### 8.2 HITL 作为临时提权

高危动作不该给 Agent 常备权限,而是**通过审批临时、一次性地扩张 run 的有效 scope**:

```text
Agent 想执行高危动作 → PDP 返回 require_approval
  → 人工审批 → 签发一个「一次性、绑定该 approval_id、仅此动作、短期」的提权 token
  → 执行 → 立即回收
```

审批 id 进审计,scope 用完即失效。详见 [16.3-Agent Loop人在回路](./16.3-Agent%20Loop人在回路.md)。

### 8.3 撤销、熔断与多租户隔离

- **短期 + 可撤销**：token 短 TTL + 中央吊销表,权限变更/异常即时生效,不必等 token 自然过期。
- **Run kill-switch**：能一键停掉某个 run / 某个 Agent 部署 / 某个用户的所有委派。
- **多租户强隔离**：`tenant` claim 全程强绑定,所有资源访问带 tenant 过滤;跨租户访问默认拒绝,是最高优先级的硬约束。

---

## 九、端到端鉴权流程（把整套串起来）

以「Agent 帮用户 Alice 退一笔订单」为例:

```text
1. 认证    Alice 经 OIDC 登录 → 平台拿到 user ID Token(sub=alice, tenant=acme)
2. 启动    Runtime 用自身工作负载身份认证;为本 run mint capability token
           = downscope(alice 的权限 ∩ agent:refund-bot 的能力域)
           → scope 只含本任务可能需要的最小集,写入 run_id / tenant / act=refund-bot
3. Admission  Runtime run-admission(PEP)问 PDP:这个用户/租户/Agent 能否启动退款类 run?
4. 每步授权  模型决定调 issue_refund;Tool Gateway(PEP)问 PDP:
             effective_permission 交集是否放行?金额是否超阈值?
5. 决策    金额 800 元 > 阈值 → PDP 返回 require_approval
6. 提权    主管审批 → 签发一次性提权 token(aud=billing-api, scope=refund:write, approval_id)
7. 换发    Runtime 对该 token 做 token exchange → 绑定 billing-api 受众、降到最小 scope
8. 执行    Tool Gateway 注入 billing 系统凭证(模型不可见)→ 调用 → 幂等键防重
9. 审计    全链路记录:sub=alice, act=refund-bot, run_id, approval_id, scope, 结果
```

每一跳都可回答「谁、代表谁、凭什么、被谁批准」——这就是鉴权体系设计到位的标志。

---

## 十、与平台各组件的映射（谁是 PEP）

| 组件 | 鉴权职责 |
|---|---|
| Runtime（[20](./20-Agent%20Runtime专题.md)） | 控制平面:mint / 换发 / 传递 token;run admission PEP;托管委派链 |
| Tool Gateway（[23](./23-Tool%20Gateway与工具平台专题.md)） | 工具 PEP:每次工具调用查授权、注入凭证、审计 |
| Model Gateway（[26](./26-Model%20Gateway与模型治理专题.md)） | 模型 PEP:数据外发/租户隔离/模型访问授权 |
| RAG / 检索（[05](./05-RAG全景.md)） | 数据 PEP:检索结果按用户/租户/数据分级过滤 |
| Memory（[04.1](./04.1-生产级Agent记忆工程.md)） | 记忆访问 PEP:跨会话记忆按主体隔离 |
| Policy Engine（[27](./27-Agent安全生命周期专题.md)） | 中央 PDP:策略即代码,统一决策 |

**核心判断**：鉴权是横切平台的控制平面。**决策集中在一个 PDP,执行分布在各 PEP**,Runtime 负责把身份和委派串成一条可审计的链。

---

## 十一、核心问题（含解答）

### Q1：为什么不能让 Agent 直接用用户的登录 token？

因为会同时踩四个坑:暴露面爆炸(链上每跳都能拿它干任何事)、无法降权(读操作也拿到全权限)、无法归因(下游分不清用户还是 Agent)、无法及时撤销。正确做法是 Agent 有独立身份,用户授权通过逐跳换发的、降权的、绑定受众的短期 token 传递(§5)。

### Q2：Agent 自己的身份怎么发？

用工作负载身份而非长期 API key:SPIFFE SVID / workload OIDC / mTLS 短期证书,可自动轮换、可单独吊销、可审计到具体部署(§4)。

### Q3：多跳委派怎么把用户授权传下去又不泄露凭证？

OAuth 2.0 Token Exchange(RFC 8693)思路:每跳拿旧凭证换一个降权、绑定下一跳受众、短期的新凭证,token 里用 `sub`(代表谁)+ `act`(执行者)记录委派链(§5.2)。

### Q4：Confused Deputy 怎么防？

授权基于「本次调用的真实委派链」而非 Agent 的静态高权限;有效权限取交集,被用户权限和 run scope 夹住;不可信输入(网页/工具返回)不能改变授权决策(§8.1)。

### Q5：长时/异步 run,token 过期了怎么办？

不要靠一个长寿命 token 硬撑。用 run-scoped、可续期的 capability token,由 Runtime 在需要时凭 Vault 里的刷新凭证换新的短期 token;同时保留中央吊销能力,让挂起数天的 run 也能被即时撤权(§7、§8.3)。

### Q6：用户的第三方凭证（如 Gmail）怎么管？模型会看到吗？

per-user connection:每个用户单独 OAuth 授权,token 存 Secret Vault,按 user+tool 隔离,自动刷新、可撤销。模型永远看不到明文——它只发「调 Gmail 工具」的意图,凭证由 Tool Gateway 执行时注入(§7)。

### Q7：RBAC 够吗？什么时候要 ABAC / ReBAC？

粗粒度能力域用 RBAC 打底就够;但「只能退自己经手的订单」「金额超阈值要审批」「按租户和数据分级判」这类需要资源属性和关系,必须叠 ABAC / ReBAC(§6.3)。

---

## 十二、场景设计题（含答案）

### 场景 1：企业 IT Helpdesk Agent

**题目**：Agent 能代表员工做重置密码、开通权限、重启服务等运维动作,这些动作本身是高危的。设计鉴权。

**要点**：
- **身份**：Agent 用工作负载身份;员工经企业 SSO(OIDC)认证,拿到 `sub`+`groups`+`tenant`。
- **委派**：run 启动 mint capability token = 员工权限 ∩ helpdesk-agent 能力域,降到本工单最小 scope;token 带 `act=helpdesk-agent`。
- **授权**：RBAC(helpdesk 能力域)+ ABAC(按资源归属、风险分)。重置密码/开权限 → PDP 返回 require_approval。
- **提权**：主管审批签发一次性提权 token,仅此动作、绑定 approval_id、短期。
- **凭证**：AD/IAM 系统口令存 Vault,Tool Gateway 注入,模型不可见。
- **审计**：每个动作记 `sub`(哪个员工)+`act`(agent)+approval_id+结果,支持完整回放。
- **兜底**：注入防护(不可信工单内容不能提权)、kill-switch、跨部门访问默认拒绝。

### 场景 2：C 端个人助理，连用户的 Gmail / Calendar / 银行

**题目**：多用户、多租户,每个用户连了自己的一堆第三方账户。设计凭证与委派。

**要点**：
- **多租户隔离**：`tenant`/`user` claim 全程强绑定,任何资源访问带用户过滤,跨用户默认硬拒绝(最高优先级)。
- **per-user 凭证**：每个用户对每个 SaaS 单独 OAuth 授权,token 按 user+provider 存 Vault,自动刷新,用户可随时撤销。
- **委派**：调某个 SaaS 时 token exchange 到该 provider 受众、降到最小 scope(读日历的工具拿不到转账权限)。
- **高危分级**：查日历 allow;转账 require_approval + HITL,且限额、限收款人白名单。
- **凭证隔离**：模型只看到「调用 X」,永不接触任何 OAuth token;trace 里凭证脱敏。
- **撤销**：用户注销 / 撤销某授权 → 立即失效对应 token 和所有在途 run 的相关 scope。

---

## 十三、自测清单

- [ ] 用一句话说清鉴权要回答的三件事(谁 / 代表谁 / 能不能)。
- [ ] 列出 Agent 鉴权比传统系统难的至少四个结构性原因。
- [ ] 说明为什么不能把用户 token 直接透传给 Agent,以及正确的替代方案。
- [ ] 画出一次工具调用的委派链,标出每跳的 token 如何降权、绑定受众、留痕。
- [ ] 解释 `sub` 与 `act` 两个 claim 分别记录什么,为什么审计需要它们。
- [ ] 说清 PEP/PDP/PAP/PIP 各是谁,以及「决策集中、执行分布」的含义。
- [ ] 写出有效权限交集公式,并说明每一项从哪来、在哪个 PEP 评估。
- [ ] 说明什么时候 RBAC 不够、必须叠 ABAC/ReBAC。
- [ ] 解释 confused deputy 的成因和三条防护。
- [ ] 说明 HITL 如何作为「临时、一次性提权」而非常备高权限。
- [ ] 说清模型为什么永远看不到凭证明文,凭证在哪个环节注入。

---

## 十四、关键术语 Cheat Sheet

| 术语 | 含义 |
|---|---|
| Principal / 主体 | 发起或代表某次调用的身份(用户 / Agent / 工具 / 模型) |
| AuthN / AuthZ | 认证(你是谁)/ 授权(你能不能做) |
| Delegation / OBO | 委派 / On-Behalf-Of:Agent 代表用户行动 |
| Token Exchange (RFC 8693) | 用一个 token 换另一个降权、换受众的 token 的标准流程 |
| Capability Token | 只含「本 run/本次调用所需最小权限」的短期 token |
| Downscoping | 逐跳降权,scope 只减不增 |
| Audience Binding (`aud`) | token 绑定唯一接收方,防跨服务盗用 |
| `sub` / `act` claim | 代表谁 / 实际执行者,构成可审计的委派链 |
| Confused Deputy | 混淆代理:高权限主体被诱导替攻击者行权 |
| Workload Identity / SPIFFE SVID | 工作负载(Agent/服务)的身份与短期证书 |
| PEP / PDP / PAP / PIP | 执行点 / 决策点 / 管理点 / 信息点 |
| RBAC / ABAC / ReBAC | 基于角色 / 属性 / 关系的访问控制 |
| Least Privilege | 最小权限:每一跳只给恰好够用的授权 |
| Secret Vault | 凭证保险库,运行时注入,模型不可见 |

---

## 十五、延伸阅读

- OAuth 2.0（RFC 6749）、OAuth 2.0 Token Exchange（RFC 8693）、OIDC Core
- SPIFFE / SPIRE 工作负载身份规范
- 策略即代码：OPA（Rego）、AWS Cedar
- XACML 的 PEP/PDP/PAP/PIP 参考架构
- MCP 授权规范（工具/Server 的 OAuth 接入）
- 本库关联：[20 Agent Runtime](./20-Agent%20Runtime专题.md)、[23 Tool Gateway](./23-Tool%20Gateway与工具平台专题.md)、[26 Model Gateway](./26-Model%20Gateway与模型治理专题.md)、[27 安全生命周期](./27-Agent安全生命周期专题.md)、[13 A2A](./13-A2A与Agent互操作协议.md)、[16.3 HITL](./16.3-Agent%20Loop人在回路.md)、[09 安全与对齐](./09-安全与对齐.md)

---

## 十六、核心要点

1. **鉴权是平台的一等子系统,不是散在各处的 if-else**：决策集中在一个 PDP,执行分布在 Runtime / Tool Gateway / Model Gateway / RAG / Memory 各 PEP。
2. **Agent 有独立身份**：工作负载身份 + 短期证书,绝不用用户 token 当万能钥匙,也不用长期 API key。
3. **委派是最难的部分**：逐跳换发、只减不增、绑定受众、`sub`/`act` 全程留痕——这是 Agent 鉴权区别于传统系统的核心。
4. **有效权限取交集**：用户 ∩ 租户 ∩ Agent 角色 ∩ run scope ∩ 工具 scope ∩ 数据策略,Agent 永远不能超过委派给它的授权。
5. **凭证与模型隔离 + HITL 临时提权**：secret 永不进上下文,高危动作靠一次性审批提权而非常备高权限;配短期 token、可撤销、kill-switch 和多租户强隔离兜底。
