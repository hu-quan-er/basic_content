# Agent 推理加速落地：从少调用到可观测优化

> 目标：把《[19-推理加速底层系统](./19-推理加速底层系统.md)》里的底层技术映射到 Agent 系统设计。重点不是单次模型 forward 多快，而是如何减少串行 LLM hop、控制上下文增长、并行工具、分层用模型、缓存稳定前缀，并用可观测数据证明优化有效。

---

## 一、Agent 为什么特别慢

普通 Chatbot 通常是一次模型调用：

```
User → LLM → Answer
```

Agent 往往是多轮 loop：

```
User
  ↓
Planner LLM
  ↓
Tool Call(s)
  ↓
Observation
  ↓
Executor LLM
  ↓
More Tool Call(s)
  ↓
Verifier/Critic LLM
  ↓
Final Answer
```

慢的来源：
- LLM 调用次数多。
- 每轮 history 变长，prefill 越来越贵。
- 工具调用可能串行等待。
- reasoning model 会消耗大量 thinking/output tokens。
- 失败重试和自纠进一步拉长路径。
- function calling / JSON schema 失败会导致额外修复轮。
- 多 Agent 协作如果没有依赖图，容易把可并行任务串行化。

**核心判断**：Agent 慢通常先慢在系统编排，而不是单次模型 forward。先减少调用次数和 token，再考虑推理引擎调参。

---

## 二、Agent 延迟拆解

一次 Agent 任务的端到端延迟可以拆成：

```
Agent Latency =
  Σ LLM_TTFT
+ Σ LLM_decode_time
+ Σ reasoning_token_time
+ Σ tool_latency
+ Σ queue/scheduling_time
+ Σ retry/repair_time
+ network/browser/db overhead
```

更细一点，每次 LLM 调用：

```
LLM_call =
  queue + network + prefill(input_tokens) + decode(output_tokens) + parsing/validation
```

优化顺序：
1. 先减少 **串行 LLM hop**。
2. 再缩短每次调用的 **input/output/reasoning tokens**。
3. 再并行独立工具。
4. 再做缓存与模型路由。
5. 最后才调推理引擎细参。

很多 Agent 慢不是推理引擎不够快，而是设计成了"每做一步都问一次大模型"。

---

## 三、Agent 推理加速的五层模型

| 层级 | 优化问题 | 典型手段 |
|------|----------|----------|
| 产品层 | 真的需要实时完成吗 | 流式反馈、后台任务、部分结果、异步通知 |
| 编排层 | 串行 LLM hop 是否过多 | plan-execute、依赖图、工具并行、确定性 executor |
| 上下文层 | 每次 prefill 是否过大 | 状态外置、history compaction、RAG 限量、工具结果摘要 |
| 模型层 | 是否每步都用大模型 | 模型路由、planner/executor/critic 分层、reasoning budget |
| Serving 层 | GPU 是否被充分利用 | prefix cache、continuous batching、量化、spec decode、P/D 分离 |

优先级通常从上到下。产品层和编排层的一次改动，常常比换推理引擎更有效。

---

## 四、底层加速技术在 Agent 中的映射

| 底层技术 | Agent 落地点 |
|----------|--------------|
| Prefix caching | system prompt、tool schema、固定策略文档放稳定前缀 |
| PagedAttention | 多用户多轮 agent serving，提高并发与 KV 利用率 |
| Radix/prefix cache | 多轮任务、共享工具说明、共享长文档上下文复用 |
| Continuous batching | 高并发 Agent API，共享 GPU 吞吐 |
| Chunked prefill | 长 RAG/context agent 减少 decode tail 抖动 |
| Speculative decoding | 长报告、代码生成、总结输出阶段提速 |
| Quantization | worker/executor 小模型、分类/摘要/路由模型降本 |
| Guided decoding | function calling、JSON agent 减少解析失败和修复轮 |
| P/D disaggregation | 长上下文 Deep Research / Coding Agent 的大规模 serving |
| KV tiering | 多轮长任务、会话恢复、共享长文档上下文 |

注意：底层技术不是越多越好。比如 guided decoding 会减少格式失败，但 schema 复杂时单次 decode 可能更慢；speculative decoding 对长输出有用，对短工具选择调用可能没收益。

---

## 五、第一性优化：减少串行 LLM 调用

### 5.1 坏模式：每一步都重新思考

```
想一步 → 调工具 → 想一步 → 调工具 → 想一步 → ...
```

问题：
- 每一步都要 prefill 历史。
- 每一步都有网络和队列开销。
- 工具之间本可并行却被串行化。
- 早期错误会被 history 固化，后面继续放大。

### 5.2 更好的模式：一次规划，确定性执行

```
Planner 一次性生成任务图
  ↓
Executor 按依赖图执行工具
  ↓
只在异常/歧义/汇总阶段再调用 LLM
```

原则：
- 能用代码判断的，不要问 LLM。
- 能一次计划的，不要每一步重新计划。
- 能批量工具调用的，不要串行工具调用。
- 工具参数能由 schema 校验的，不要让模型自由解释。

### 5.3 依赖图比自然语言计划更重要

自然语言计划适合人读，但执行时应变成结构化依赖图：

```json
{
  "tasks": [
    {"id": "search_docs", "deps": [], "tool": "search"},
    {"id": "query_db", "deps": [], "tool": "sql"},
    {"id": "summarize", "deps": ["search_docs", "query_db"], "tool": "llm"}
  ]
}
```

好处：
- 依赖为空的任务可以并行。
- 可以单独重试失败节点。
- 可以做超时和降级。
- trace 更容易评估。

---

## 六、控制上下文增长：减少 Prefill 税

Agent 每轮都把完整历史塞回模型，会让 prefill 越来越贵。上下文成本不仅是 token 账单，也是 TTFT 和 KV cache 压力。

### 6.1 上下文分层

| 信息类型 | 放哪里 | 是否每轮注入 |
|----------|--------|--------------|
| 固定规则/system prompt | 稳定前缀 | 是，适合 prefix cache |
| 工具 schema | 稳定前缀 | 是，适合 prefix cache |
| 当前任务目标 | 短上下文 | 是 |
| 关键状态/变量 | 结构化 state | 按需注入 |
| 工具原始结果 | 外部存储 | 不直接全量注入 |
| 工具结果摘要 | working memory | 按需注入 |
| 老对话历史 | 压缩摘要 | 超阈值后注入 |
| 大文档/RAG 片段 | 检索系统 | 只召回相关片段 |

### 6.2 工具结果压缩

工具结果不要直接丢回 history。推荐结构：

```json
{
  "source_id": "doc_42",
  "claim": "合同退款窗口为支付后 30 天",
  "evidence_span": "section 3.2",
  "confidence": "high",
  "raw_ref": "s3://bucket/doc_42"
}
```

模型看到的是 evidence card，不是完整网页、完整日志或完整 SQL 结果。需要复查时再按 `raw_ref` 拉原文。

### 6.3 稳定前缀设计

为了提高 prompt/prefix cache 命中：

```
[稳定前缀]
  System Prompt
  Tool Schemas
  Output Contract
  Fixed Rubric
  Static Examples
--- cache boundary ---
[动态部分]
  User Query
  Current State
  Retrieved Evidence
```

规则：
- 动态时间戳、request id、用户私有字段不要放在稳定前缀中间。
- 工具 schema 顺序要稳定。
- few-shot 示例不要每次随机抽样插入前缀。
- 多租户系统要按权限隔离 cache key。

---

## 七、工具并行与慢工具治理

### 7.1 并行独立工具

如果工具之间无依赖，应并行：

```
检索 A ┐
检索 B ├─ 汇总 → LLM
API C  ┘
```

Agent loop 中最常见的浪费，是 LLM 明明已经知道要查 5 个来源，却一个一个查，每次查完还重新思考。

### 7.2 慢工具要有预算

每个工具应定义：
- 单次超时。
- 最大重试次数。
- 是否可缓存。
- 是否可部分失败。
- 是否阻塞最终回答。
- 降级策略。

例：

| 工具 | 超时 | 失败策略 |
|------|------|----------|
| FAQ 检索 | 800ms | 失败后走小模型常识回答，但标记低置信 |
| 订单 API | 2s | 失败后要求用户稍后重试，不编造状态 |
| 网页搜索 | 5s | 返回已完成来源，后台继续补充 |
| 代码测试 | 60s | 超时返回测试未完成，不声称通过 |

### 7.3 工具批处理

能批处理就不要多次 round-trip：
- 多个检索 query 合并。
- 多个数据库 id 一次查。
- 多个网页并发抓取。
- 多个 embedding 一批算。
- 多个文件一次读取后局部摘要。

---

## 八、模型分层与 Reasoning Budget

### 8.1 分层架构

| 阶段 | 推荐模型 |
|------|----------|
| Intent/router | 小模型或规则 |
| Planner | 强模型 / reasoning model |
| Executor | 中小模型或确定性代码 |
| Tool result summarizer | 小模型 |
| Verifier/Critic | 强模型或专用 judge |
| Final response | 中等模型，按用户体验优化 |

核心思路：把强模型用在"错了代价高"的少数决策点，而不是每个 token 都用最贵模型生成。

### 8.2 Reasoning Model 的使用边界

适合用 reasoning model：
- 复杂任务拆解。
- 多约束规划。
- 代码修改策略。
- 高风险决策前的 review。
- 失败后的根因分析。

不适合总是用：
- 简单分类。
- 信息抽取。
- 工具参数填充。
- 短客服回答。
- 大量重复摘要。

### 8.3 Reasoning Budget

给推理模型设预算：
- low/medium/high reasoning effort。
- 最大 thinking tokens。
- 最大 plan steps。
- 超预算后转人工或降级。

策略：
- 默认低预算，检测到复杂度再升级。
- Planner 可以高预算，Executor 默认低预算。
- Critic 只审关键路径，不审每个普通步骤。
- 失败重试不要无限提高 reasoning budget，要看失败类型。

---

## 九、多层缓存：缓存的不只有 LLM

Agent 可缓存层很多：

| 缓存 | 缓存对象 | 关键风险 |
|------|----------|----------|
| Prompt/prefix cache | 稳定 system/tool 前缀 | 动态字段破坏命中 |
| Semantic cache | 相似问题最终答案 | 过期、权限、错复用 |
| Tool cache | API/RAG/网页结果 | 时效性和权限 |
| Embedding cache | 文档向量 | 文档版本变化 |
| Rerank cache | query-doc 排序结果 | query rewrite 改变 |
| Plan cache | 常见任务执行计划 | 计划不适配当前上下文 |
| KV cache | 自部署长会话前缀 | 显存占用和隔离 |

### 9.1 Cache Key 设计

缓存 key 不能只看自然语言 query。至少考虑：
- 用户/租户权限。
- 数据版本。
- 工具版本。
- prompt/template 版本。
- 模型版本。
- 时间窗口。
- locale / business unit。

### 9.2 缓存失效

常见失效策略：
- TTL：适合网页、FAQ、低风险信息。
- 版本号：适合文档、prompt、工具 schema。
- 事件驱动：订单状态、库存、权限变更。
- 手动 purge：合规和安全场景。
- 置信阈值：semantic cache 相似度不足不复用。

**核心判断**：缓存是性能优化，也是正确性风险。Agent 缓存必须把权限和数据版本放进设计。

---

## 十、结构化输出：减少失败轮，而不是只看单次速度

Agent 大量依赖 function calling / JSON：
- 计划输出。
- 工具参数。
- 状态更新。
- 验证结果。
- 最终答案结构。

### 10.1 常见失败

- JSON 语法非法。
- 字段缺失。
- enum 值漂移。
- 参数类型错误。
- 工具名不存在。
- 模型把解释文字混入 JSON。
- 修复 JSON 又触发一次 LLM 调用。

### 10.2 优化策略

- 能用 function calling / structured outputs，就不要纯 prompt 约束。
- schema 保持小而清晰，不要让模型生成巨大嵌套对象。
- 工具参数先 schema 校验，再执行。
- 可用默认值的字段由代码补齐。
- 多步复杂输出拆成多个小 schema。
- 失败后把 validator error 反馈给模型重试，但设重试预算。

### 10.3 性能账

```
总成本 = 单次结构化 decode 开销 + 失败率 × 修复轮成本
```

如果普通 JSON 失败率高，guided decoding 即使单次慢一点，也可能让总延迟和失败率下降。如果 schema 很简单、模型本身稳定，强约束解码未必必要。

---

## 十一、流式体验：让用户看到系统在工作

Agent 流式不只是 token：
- 先返回"已开始处理"。
- 流式展示 plan、工具调用、检索进度。
- 工具结果可边到边处理。
- 最终答案边生成边输出。
- 长任务给用户可取消/继续/后台完成的选择。

推荐事件流：

```json
{"type": "started", "run_id": "r1"}
{"type": "plan", "steps": ["search", "quote", "summarize"]}
{"type": "tool_start", "tool": "search"}
{"type": "tool_result", "tool": "search", "items": 5}
{"type": "answer_delta", "text": "根据检索结果..."}
{"type": "done"}
```

这样不一定降低 E2E，但能显著降低用户感知等待，也方便中途取消和调试。

---

## 十二、典型场景设计

### 12.1 低延迟客服 Agent

目标：首响快、答案短、成本低。

策略：
- 小模型做意图分类和路由。
- 高频 FAQ 走 semantic cache。
- RAG 只取少量高置信片段。
- 强模型只处理复杂投诉/多约束问题。
- 输出限制短句，开启流式。
- 工具调用并行，超时走降级回答。

底层：
- API 场景用 prompt caching。
- 自部署用 vLLM/TGI continuous batching。
- 工具 schema 稳定前缀化。

### 12.2 Deep Research Agent

目标：复杂检索、长上下文、最终报告质量高。

策略：
- Planner 用 reasoning model，一次规划检索子问题。
- 多来源检索并行。
- 每批资料先摘要成 evidence cards。
- 中间状态结构化存储，不把全部网页塞进 history。
- 最终报告阶段可用 speculative decoding 或大模型长输出。
- 离线任务用 batch queue，不强求实时。

底层：
- prefix cache 缓存研究协议、引用格式、工具 schema。
- 长上下文自部署可考虑 P/D disaggregation、KV tiering。
- 监控 TTFT、长输出 TPOT、RAG 工具耗时。

### 12.3 Coding Agent

目标：多步执行可靠，能跑测试，成本可控。

策略：
- 读取代码和搜索尽量用工具，LLM 只做判断和 patch。
- 多个独立探索任务并行。
- 测试、lint、格式化用确定性工具。
- Planner/Reviewer 用强模型，机械执行可用较小模型。
- 每轮只注入相关文件片段和当前计划。

底层：
- 代码生成长输出可考虑 speculative decoding。
- 常驻 harness 的 system prompt 和工具说明适合 prefix caching。
- 自部署代码模型可用 AWQ/GPTQ/FP8 量化，但必须跑代码能力 eval。

### 12.4 企业知识库 Agent

目标：权限安全、引用准确、多人并发。

策略：
- 检索按权限过滤。
- query rewrite 小模型化。
- RAG chunk 限量，rerank 后只注入必要片段。
- 引用与答案分离校验。
- 相同文档/政策上下文做 prefix/KV cache。

底层：
- 高并发用 continuous batching。
- 长文档固定前缀或缓存 evidence。
- 监控 cache hit rate、RAG latency、LLM TTFT。

### 12.5 Realtime / Voice Agent

目标：极低感知延迟、可打断、稳定交互。

策略：
- 对话层用低延迟模型，不默认 reasoning。
- 工具调用异步，先回答可确认部分。
- 长任务转后台，不阻塞语音回合。
- 短输出、分段响应、允许 barge-in。
- 高频意图走本地规则或小模型。

底层：
- 优先优化 TTFT 和首音频延迟。
- 流式 token 到 TTS pipeline。
- 小模型或边缘推理可用量化。
- 大工具/检索结果晚到时做后续补充。

---

## 十三、可观测与优化闭环

### 13.1 必采 Trace 字段

| 字段 | 用途 |
|------|------|
| run_id / step_id | 串起一次 Agent 任务 |
| model_name / model_version | 分析路由和退化 |
| input_tokens / output_tokens / reasoning_tokens | token 成本拆解 |
| TTFT / TPOT / E2E | 延迟瓶颈定位 |
| tool_name / tool_latency | 找慢工具 |
| cache_hit / cache_type | 评估缓存收益 |
| retry_count / validation_error | 发现结构化失败 |
| plan_steps / executed_steps | 找无效步骤 |
| queue_time | 区分服务排队和模型计算 |
| final_status | 成功、部分成功、降级、失败 |

### 13.2 优化报表

每次优化后至少对比：
- 平均和 p95/p99 E2E。
- 平均和 p95 TTFT。
- LLM 调用次数/task。
- input/output/reasoning token/task。
- 工具串行等待时间。
- cache hit rate。
- 成功率和降级率。
- 业务质量指标。

### 13.3 A/B 验证

常见 A/B：
- full history vs compacted state。
- serial tools vs parallel tools。
- all-big-model vs routed model。
- JSON prompt vs structured output。
- no cache vs prefix/tool/semantic cache。
- high reasoning vs adaptive reasoning。

不要只看延迟下降，还要看任务成功率、答案质量、人工接管率和安全事件。

---

## 十四、Agent 推理加速反模式

| 反模式 | 问题 | 替代方案 |
|--------|------|----------|
| 每一步都问大模型 | LLM hop 过多 | 一次规划 + 确定性执行 |
| 完整 history 永远拼接 | prefill 越来越贵 | 结构化 state + compaction |
| 工具结果全塞上下文 | token 爆炸 | evidence card + raw ref |
| 所有工具串行调用 | 等待时间叠加 | 依赖图并行 |
| reasoning always high | 成本/延迟暴涨 | adaptive reasoning budget |
| JSON 靠 prompt 硬约束 | 格式失败重试 | structured output + schema validator |
| 只缓存最终答案 | 命中少、风险高 | prefix/tool/RAG/plan 多层缓存 |
| 只看 tokens/sec | 忽略用户体验 | TTFT/TPOT/p95/E2E 一起看 |
| 加速后不跑质量 eval | 可能静默退化 | 业务评测 + 回放 trace |

---

## 十五、核心问题（含解答）

### Q1：Agent 调用慢，优先优化什么？

**答**：

优先级通常是：
1. 减少串行 LLM 调用次数。
2. 并行独立工具调用。
3. 压缩上下文和工具结果。
4. 做模型路由：简单任务用小模型。
5. 缓存：prefix、tool、semantic、RAG。
6. 限制输出/reasoning token。
7. 最后再调推理引擎参数。

原因：Agent 慢通常是系统编排慢，不只是单次模型 forward 慢。

---

### Q2：为什么 Agent 多轮后越来越慢？

**答**：

因为每轮通常会把越来越长的 history、工具结果和中间推理塞回 prompt，导致 prefill 成本、TTFT、KV cache 占用一起增长。解决方法是把状态外置到结构化 state，工具结果摘要成 evidence card，老 history compaction，只注入当前轮必要信息。

---

### Q3：如何让 Agent 更好利用 prompt/prefix cache？

**答**：

把稳定内容放在 prompt 前缀：system prompt、工具 schema、输出 contract、固定 rubric、稳定 few-shot。动态内容放到后面：用户问题、当前状态、检索证据。不要在稳定前缀中间插入时间戳、request id、随机示例或用户私有字段。多租户系统要把权限和版本放入 cache key。

---

### Q4：模型路由怎么避免质量下降？

**答**：

路由不能只按成本做。需要：
- 难度分类器或规则。
- 置信度阈值。
- 低置信升级到强模型。
- 对关键步骤使用 verifier。
- 用真实任务回放评测路由策略。
- 监控每个模型路径的成功率和人工接管率。

简单任务走小模型，复杂规划和高风险 review 走强模型。

---

### Q5：工具并行有什么风险？

**答**：

风险是依赖关系判断错误、外部系统压力上升、并发失败处理复杂。解决方法是先让 planner 输出结构化依赖图，只并行 `deps=[]` 或依赖已满足的节点；给每个工具设置超时、重试、降级和批处理上限；trace 里记录每个工具耗时和失败原因。

---

### Q6：Semantic cache 为什么危险？

**答**：

因为"语义相似"不等于"答案可复用"。权限、时间、数据版本、用户上下文不同，都可能导致相似问题答案不同。semantic cache 必须把权限、数据版本、时间窗口、prompt/model 版本放进 key 或 validator，并设置高相似度阈值和过期策略。

---

### Q7：结构化输出会不会让 Agent 更快？

**答**：

看总账。结构化输出或 guided decoding 可能让单次 decode 变慢，但能减少 JSON 解析失败、参数错误和修复轮。如果原始失败率高，它会让端到端更快更稳；如果 schema 简单且模型稳定，额外约束未必有必要。

---

### Q8：什么时候用 speculative decoding 加速 Agent？

**答**：

适合长输出阶段，例如报告生成、代码生成、长总结。它不适合短工具选择、短 JSON 参数生成、主要瓶颈在工具等待的任务。判断标准是：decode 是否占总延迟大头，输出是否足够长，draft 接受率是否高。

---

### Q9：Reasoning model 在 Agent 中该放哪里？

**答**：

优先放在 Planner 和 Critic/Reviewer。Planner 决定任务路径，错了代价高；Critic 用来审关键结果和失败恢复。Executor、分类、摘要、工具参数填充通常不需要默认用 reasoning model。要用 adaptive reasoning budget，而不是所有步骤 high。

---

### Q10：如何证明 Agent 加速没有牺牲质量？

**答**：

用回放集和线上 A/B 同时看：
- 任务成功率。
- 人工接管率。
- 引用/工具结果正确率。
- 格式失败率。
- 安全拦截率。
- 用户满意度或业务 KPI。
- p95/p99 latency 和 cost/task。

只看延迟下降不够，必须证明成功率和安全没有退化。

---

## 十六、关键术语 Cheat Sheet

| 术语 | 一句话 |
|------|--------|
| LLM hop | 一次模型调用；Agent 延迟经常由串行 hop 数决定 |
| Planning tax | 为了规划而额外消耗的模型调用和 reasoning token |
| Prefill tax | 上下文越长，每轮首 token 越慢 |
| Tool latency | 外部工具/API/检索/浏览器调用耗时 |
| Evidence card | 工具结果的结构化摘要，替代全量原文塞上下文 |
| Structured state | 外置的任务状态，替代只靠 message history |
| Dependency graph | 工具和子任务的依赖图，用于并行和重试 |
| Model routing | 按任务难度/风险选择不同模型 |
| Reasoning budget | 对 thinking/reasoning token 或 effort 的上限 |
| Semantic cache | 相似问题复用答案或中间结果的缓存 |
| Prefix cache | 复用稳定 prompt 前缀的 KV cache |
| Guided decoding | 用 schema/grammar 限制输出，减少格式失败 |
| Progressive disclosure | 先给用户可见进度，再逐步补结果 |

---

## 十七、延伸阅读

- 《[19-推理加速底层系统](./19-推理加速底层系统.md)》：prefill/decode、KV cache、batching、量化、推理引擎。
- 《[08-工程化性能成本可靠性](./08-工程化性能成本可靠性.md)》：性能、成本、可靠性的系统权衡。
- 《[12-Reasoning Models专题](./12-Reasoning%20Models专题.md)》：reasoning token、test-time compute、推理预算。
- 《[14-Agent工程细节与可靠执行](./14-Agent工程细节与可靠执行.md)》：结构化计划、Executor、重试幂等。
- 《[16-Agent Loop专题](./16-Agent%20Loop专题.md)》：loop 如何放大调用次数和上下文。
- 《[16.2-Agent Loop上下文管理](./16.2-Agent%20Loop上下文管理.md)》：compaction、外置状态、上下文控制。
- 《[17-上下文窗口的模型原理](./17-上下文窗口的模型原理.md)》：自注意力、KV cache、长上下文退化的底层原因。

---

## 十八、核心要点速记

1. Agent 慢通常不是单次模型太慢，而是串行 LLM hop、上下文膨胀、工具等待和修复轮叠加。
2. 优化顺序：少调用、少 token、并行工具、模型分层、多层缓存、流式反馈、预算降级。
3. 稳定 system/tool/schema 前缀是 prefix cache 的基础，动态字段插错位置会毁掉命中率。
4. 工具结果要变成 evidence card 和 raw ref，不要把完整网页、日志、SQL 结果塞进 history。
5. Planner 用强模型，Executor 尽量确定性或小模型；reasoning model 应集中在高风险决策点。
6. 多层缓存必须处理权限、数据版本、时效和模型/prompt 版本，否则会把性能优化变成正确性事故。
7. 结构化输出减少格式失败和修复轮，但复杂 schema 单次 decode 可能变慢，要看总账。
8. Speculative decoding 适合长报告/代码/总结，不适合短工具选择和工具等待型任务。
9. Agent 流式应流 plan/tool/progress/answer，不只是最终 token。
10. 加速必须用 trace 和回放集验证：延迟、成本、成功率、质量、安全一起看。
