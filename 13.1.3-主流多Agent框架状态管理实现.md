# 主流多 Agent 框架状态管理实现

> 目标：对比主流 Agent 框架和协议在“状态管理、上下文同步、handoff、memory、artifact、trace”上的落点，并给出更细粒度的使用示例。本篇关注实现层；底层状态设计见 [13.1.1-多Agent状态模型与读写契约.md](./13.1.1-多Agent状态模型与读写契约.md)，A2A 边界见 [13.1.2-A2A上下文同步与Handoff设计.md](./13.1.2-A2A上下文同步与Handoff设计.md)。如果框架片段仍然看不出完整逻辑，先运行 [examples/multi_agent_state_demo.py](./examples/multi_agent_state_demo.py)，再读 [13.1.5-多Agent状态同步端到端代码示例.md](./13.1.5-多Agent状态同步端到端代码示例.md)。

---

## 一、先看总览矩阵

| 实现 | 状态原语 | 上下文同步方式 | 持久化/恢复 | 多 Agent 协作模型 | 适合场景 |
|------|----------|----------------|-------------|-------------------|----------|
| A2A | Task、Message、Artifact、Agent Card | Message/DataPart/Artifact | Task store 由实现方负责 | 跨系统黑盒 Agent 协作 | 跨组织、跨厂商 |
| LangGraph | Typed State、Reducer、Checkpoint、Store | state update + graph edge | checkpointer、thread_id、store | StateGraph / supervisor / subgraph | 内部复杂状态机 |
| AutoGen | Message thread、Team/Agent state | group chat / routed messages | `save_state` / `load_state` | team、group chat、selector | 对话式多 Agent |
| CrewAI | Agent、Task、Crew、Process、Memory、Knowledge | task context / task output / memory | memory、kickoff outputs、外部存储 | sequential / hierarchical | 角色化任务流 |
| OpenAI Agents SDK | Agent、Run context、Handoff、Session、Trace | handoff input / context / session | sessions、external state、trace | handoff / triage / specialized agents | OpenAI 栈应用 |
| Google ADK | Session、State、Event、Artifact、Memory | session state / events / artifacts | session service、artifact service | parent/sub-agent、workflow agents | Google 生态 Agent |
| LlamaIndex AgentWorkflow | Workflow Context、events、memory、handoff | event/context store | workflow state / external store | AgentWorkflow、多 Agent handoff | RAG + agent workflow |
| Semantic Kernel Agents | ChatHistory、AgentGroupChat、Selection/Termination Strategy | group chat history / kernel context | 外部持久化为主 | group chat、planner、tools | .NET/企业集成 |
| MCP | Resource、Tool、Prompt | resources/tools 暴露上下文 | server 自己负责 | 不是多 Agent 协议 | 工具和资源接入 |

选型直觉：

```text
跨组织互操作：A2A
内部状态复杂、需要恢复：LangGraph
多角色对话协作：AutoGen
业务任务流快速搭建：CrewAI
OpenAI handoff 应用：OpenAI Agents SDK
Google Cloud / Gemini 生态：Google ADK
RAG workflow 和事件驱动：LlamaIndex AgentWorkflow
.NET 企业应用：Semantic Kernel Agents
工具/资源标准化：MCP
```

---

## 二、A2A：协议边界上的任务状态合同

A2A 不是内部状态管理框架，而是跨系统互操作协议。

### 2.1 状态原语

| 原语 | 作用 |
|------|------|
| Agent Card | 描述远程 Agent 能力、认证、安全、输入输出模式 |
| Task | 长任务工作单元，有生命周期 |
| Message | 通信事件 |
| Part | text / file / data 等内容片段 |
| Artifact | 远程 Agent 产物 |
| Status | submitted、working、input-required、completed、failed 等 |

### 2.2 使用方式

```text
内部状态：
  LangGraph / workflow engine / custom state store

A2A 边界：
  Agent Card 暴露能力
  Task 对齐工作单元
  Message DataPart 传结构化 handoff
  Artifact 传产物引用
  metadata 传 correlation id / trace id / state version
```

### 2.3 示例：内部 state 到 A2A task

```json
{
  "internal_task_id": "vendor_review_001",
  "a2a_task": {
    "goal": "完成供应商安全评估",
    "input": {
      "facts": [
        {
          "claim": "供应商已提交 SOC2 报告",
          "evidence_refs": ["artifact:soc2_meta"]
        }
      ],
      "constraints": [
        "不要请求供应商报价",
        "只返回安全风险和整改建议"
      ]
    },
    "expected_artifacts": ["risk_report"]
  }
}
```

### 2.4 风险

- A2A 不帮你定义内部 state schema。
- A2A 不帮你做 reducer、checkpoint、memory。
- 不要把 A2A Task store 当内部数据库。
- 不要通过 A2A 传对方不该看到的 memory 或 prompt。

---

## 三、LangGraph：显式 StateGraph + Reducer + Checkpoint

LangGraph 的优势在于“状态显式”。它适合把多 Agent 系统建成一个可恢复、可审计、可分支的状态图。

### 3.1 状态原语

| 原语 | 状态含义 |
|------|----------|
| State | 图的共享结构化状态 |
| Node | Agent、工具、验证器、人工节点等 |
| Edge | 状态流转和路由 |
| Reducer | 并发更新合并规则 |
| Checkpointer | 按 thread 保存状态快照 |
| Store | 跨 thread 或长期数据存储 |
| Interrupt | 人工介入或暂停恢复 |

### 3.2 推荐状态拆法

```python
from typing import Annotated, TypedDict
from langgraph.graph.message import add_messages

def merge_risks(old: list[dict], new: list[dict]) -> list[dict]:
    by_id = {item["id"]: item for item in old}
    for item in new:
        by_id[item["id"]] = {**by_id.get(item["id"], {}), **item}
    return list(by_id.values())

class AgentState(TypedDict):
    messages: Annotated[list, add_messages]
    task_status: str
    requirements: dict
    facts: list[dict]
    risks: Annotated[list[dict], merge_risks]
    artifacts: list[dict]
    approvals: list[dict]
```

不要把所有内容都塞进 `messages`。更好的拆法：

| 字段 | 用途 |
|------|------|
| `messages` | 保留必要对话事件 |
| `requirements` | 用户确认的任务需求 |
| `facts` | 已验证事实 |
| `risks` | 风险项，可 reducer 合并 |
| `artifacts` | 产物引用 |
| `approvals` | 人工审批记录 |

### 3.3 示例：多 Agent 写 state

```python
from langgraph.graph import StateGraph, START, END
from langgraph.checkpoint.memory import MemorySaver

def research_agent(state: AgentState):
    return {
        "facts": [
            {
                "id": "fact_001",
                "claim": "供应商提供 SOC2 报告",
                "evidence_refs": ["artifact:soc2_meta"],
                "confidence": 0.95,
            }
        ],
        "artifacts": [{"id": "research_notes_v1", "type": "markdown"}],
    }

def security_agent(state: AgentState):
    return {
        "risks": [
            {
                "id": "risk_001",
                "severity": "medium",
                "description": "渗透测试报告超过 12 个月",
            }
        ]
    }

def supervisor(state: AgentState):
    if state["risks"]:
        return {"task_status": "review_required"}
    return {"task_status": "completed"}

builder = StateGraph(AgentState)
builder.add_node("research", research_agent)
builder.add_node("security", security_agent)
builder.add_node("supervisor", supervisor)
builder.add_edge(START, "research")
builder.add_edge("research", "security")
builder.add_edge("security", "supervisor")
builder.add_edge("supervisor", END)

graph = builder.compile(checkpointer=MemorySaver())
graph.invoke(
    {
        "messages": [],
        "task_status": "submitted",
        "requirements": {},
        "facts": [],
        "risks": [],
        "artifacts": [],
        "approvals": [],
    },
    config={"configurable": {"thread_id": "vendor_review_001"}},
)
```

### 3.4 LangGraph 状态治理建议

- `thread_id` 对齐业务 `task_id` 或 `context_id`。
- reducer 只用于可合并字段，不用于审批、预算、权限。
- checkpointer 用于恢复执行，不是长期 memory。
- artifact 只存引用，不把大文件塞进 state。
- 人工审批用 interrupt/resume，而不是让模型自己决定。
- 若要对外暴露能力，在 node 中调用 A2A client 或把 graph 包成 A2A server。

---

## 四、AutoGen：消息线程、Team State 与外部状态

AutoGen 更适合“多个 Agent 通过消息协作”的模式，例如研究、辩论、代码审查、多角色讨论。

### 4.1 状态原语

| 原语 | 状态含义 |
|------|----------|
| Agent | 单个对话参与者 |
| Team / GroupChat | 多 Agent 协作容器 |
| Message thread | 对话历史和协作轨迹 |
| Team state | team/agent 的可保存状态 |
| Termination condition | 停止条件 |
| Tool / Workbench | Agent 可调用能力 |

### 4.2 示例：保存和恢复 team state

```python
# 伪代码，表达状态管理重点
team = create_research_team()

result = await team.run(task="分析供应商安全风险")

team_state = await team.save_state()
save_to_store(
    task_id="vendor_review_001",
    state_type="autogen_team_state",
    state=team_state,
)

restored_state = load_from_store("vendor_review_001")
await team.load_state(restored_state)

result = await team.run(task="继续补充整改建议")
```

### 4.3 推荐架构

```text
AutoGen Team
   |
   +-- message thread: 协作过程
   +-- team state: runtime 恢复
   |
External State Store
   +-- task status
   +-- facts
   +-- risks
   +-- artifacts
   +-- approvals
```

AutoGen 的 message thread 不应该承担所有业务状态。长任务里建议外置 task state：

```json
{
  "task_id": "vendor_review_001",
  "facts": [],
  "risks": [],
  "artifacts": [],
  "autogen_state_ref": "store://autogen/team_state/vendor_review_001"
}
```

### 4.4 风险

- group chat 容易把上下文广播给所有 Agent。
- 消息历史增长会导致成本和漂移。
- 如果没有外部结构化 state，很难审计“最终事实从哪来”。
- 需要额外定义 artifact store 和 state patch 规范。

---

## 五、CrewAI：Task Context、Process、Memory、Knowledge

CrewAI 的心智是“角色化 Agent + 任务流”。它适合业务流程原型、内容生产流水线、顺序/层级任务。

### 5.1 状态原语

| 原语 | 状态含义 |
|------|----------|
| Agent | 角色、目标、工具 |
| Task | 任务描述、期望输出、上下文依赖 |
| Crew | Agent 和 Task 的组合 |
| Process | sequential / hierarchical 等执行方式 |
| Memory | 短期、长期或实体记忆 |
| Knowledge | Agent/Crew 可访问知识源 |
| Task Output | 上游任务给下游的上下文来源 |

### 5.2 示例：任务输出作为上下文

```python
from crewai import Agent, Task, Crew, Process

researcher = Agent(
    role="Security Researcher",
    goal="收集供应商安全事实并保留来源",
)

risk_reviewer = Agent(
    role="Risk Reviewer",
    goal="基于已验证事实生成风险评估",
)

research_task = Task(
    description="调研供应商安全材料，输出 facts.json 和 artifact 引用。",
    expected_output="结构化事实列表，每条事实带 evidence_refs。",
    agent=researcher,
)

risk_task = Task(
    description="基于调研结果生成风险清单。",
    expected_output="risk_register.json，包含 severity、description、evidence_refs。",
    agent=risk_reviewer,
    context=[research_task],
)

crew = Crew(
    agents=[researcher, risk_reviewer],
    tasks=[research_task, risk_task],
    process=Process.sequential,
)

result = crew.kickoff()
```

### 5.3 CrewAI 的状态治理建议

- `Task.expected_output` 写清结构化输出，不要只写“生成报告”。
- 上游 task output 应输出 artifact 引用和 evidence，不要只输出总结。
- Memory 用于辅助召回，不要当当前任务强一致状态。
- Knowledge 适合注入稳定知识，不适合保存动态审批状态。
- 高风险流程仍要外置 state store、approval、event log。

### 5.4 适合与不适合

适合：

- 顺序内容生产。
- 业务流程原型。
- 角色清晰的任务流水线。
- 对一致性要求不高的协作。

不适合单独承担：

- 强一致审批流。
- 复杂并发状态合并。
- 跨组织协议互操作。
- 严格审计的长任务。

---

## 六、OpenAI Agents SDK：Run Context、Handoff、Session、Trace

OpenAI Agents SDK 更适合在 OpenAI 栈内构建“triage agent + specialized agents + tools + guardrails”的应用。

### 6.1 状态原语

| 原语 | 状态含义 |
|------|----------|
| Agent | 指令、工具、handoff 目标 |
| Runner | 执行入口 |
| Context | 传给 tools/guardrails/handoffs 的应用对象 |
| Handoff | 控制权从一个 Agent 转给另一个 Agent |
| Session | 多轮会话上下文 |
| Guardrail | 输入/输出约束和拦截 |
| Trace | 调用链路与调试审计 |

### 6.2 示例：context 只放系统可验证信息

```python
from dataclasses import dataclass

@dataclass
class RunContext:
    user_id: str
    tenant_id: str
    task_id: str
    state_version: int
    permissions: list[str]

triage_agent = Agent(
    name="Triage",
    instructions="判断任务类型，必要时 handoff 给专业 Agent。",
    handoffs=[legal_agent, finance_agent],
)

result = Runner.run_sync(
    triage_agent,
    input="审查这份合同的付款和赔偿条款",
    context=RunContext(
        user_id="u_123",
        tenant_id="t_acme",
        task_id="contract_001",
        state_version=8,
        permissions=["read_contract", "write_risks"],
    ),
)
```

不要把无限增长的聊天历史、工具凭证、内部 prompt 放进 context。

### 6.3 Handoff 建议

| 需求 | 做法 |
|------|------|
| 控制下游看到什么 | 使用 handoff input filter / 结构化 payload |
| 传递业务身份 | 放入 context，不让模型生成 |
| 记录交接 | trace + event log |
| 返回结构化结果 | output schema / state patch |
| 高风险工具 | guardrail + permission check |

### 6.4 状态治理建议

- Session 解决对话连续性，不等于业务 state store。
- Trace 解决可观测，不等于审计数据库。
- Handoff 是控制权交接，不等于共享 memory。
- 生产系统仍要外部 state store 保存 task、artifact、approval。

---

## 七、Google ADK：Session State、Events、Artifacts、Memory

Google Agent Development Kit 更强调 Agent 运行时中的 session、state、artifact、memory 和多 Agent 组合。

### 7.1 状态原语

| 原语 | 状态含义 |
|------|----------|
| Session | 一段会话或任务运行上下文 |
| Session State | 当前 session 里的键值状态 |
| Event | Agent 执行过程中的事件 |
| Artifact | 文件、报告、结构化产物 |
| Memory | 跨 session 可召回信息 |
| Tool Context | 工具执行时可访问的上下文 |
| Sub-agent | 父子 Agent 协作结构 |

### 7.2 示例：session state 保存短期任务状态

```python
# 伪代码，表达设计重点
def security_review_agent(tool_context):
    task_state = tool_context.state.get("vendor_review", {})
    facts = task_state.get("facts", [])

    risk = {
        "id": "risk_001",
        "severity": "medium",
        "description": "渗透测试报告超过 12 个月",
    }

    tool_context.state["vendor_review"] = {
        **task_state,
        "risks": task_state.get("risks", []) + [risk],
    }

    return {"risk_id": risk["id"]}
```

### 7.3 ADK 治理建议

- Session State 适合当前会话/任务，不要无限期保存业务真相。
- Artifact Service 存大对象，state 中只保存 artifact 引用。
- Memory 适合跨 session 召回，不要覆盖当前 task state。
- 多 Agent 下要明确哪些 state key 可被哪个 sub-agent 改。
- 事件和 trace 要与外部 task id 对齐。

---

## 八、LlamaIndex AgentWorkflow：事件、Context 与 RAG 状态

LlamaIndex 的强项是 RAG、索引、检索和 workflow。AgentWorkflow 适合把多个 Agent、工具和检索步骤组织成事件驱动流程。

### 8.1 状态原语

| 原语 | 状态含义 |
|------|----------|
| AgentWorkflow | 多 Agent / tool workflow |
| Context | workflow 运行期间共享上下文 |
| Event | 步骤之间的通信和驱动 |
| Memory | 对话或任务记忆 |
| Tool | 检索、计算、外部系统动作 |
| Index / Retriever | RAG 事实来源 |

### 8.2 示例：Context 中保存任务状态

```python
# 伪代码，表达 workflow context 思路
async def research_step(ctx, event):
    facts = await retrieve_facts(event.query)
    await ctx.set("facts", facts)
    return {"next": "risk_step"}

async def risk_step(ctx, event):
    facts = await ctx.get("facts")
    risks = assess_risks(facts)
    await ctx.set("risks", risks)
    return {"next": "writer_step"}
```

### 8.3 治理建议

- RAG 检索结果必须带来源，不要直接变成 verified facts。
- Context 保存 workflow 中间态，但高风险业务状态要外置。
- 多 Agent handoff 要明确输入输出 schema。
- 适合“检索 -> 分析 -> 汇总 -> 验证”的流程。

---

## 九、Semantic Kernel Agents：ChatHistory 与企业集成

Semantic Kernel Agents 常用于 .NET / Python 企业应用中，和已有服务、插件、planner、kernel function 集成。

### 9.1 状态原语

| 原语 | 状态含义 |
|------|----------|
| Agent | 具有 instructions 和 tools 的执行单元 |
| AgentGroupChat | 多 Agent 对话容器 |
| ChatHistory | 对话上下文 |
| Selection Strategy | 选择下一个发言 Agent |
| Termination Strategy | 停止条件 |
| Kernel / Plugins | 工具和业务函数 |

### 9.2 推荐架构

```text
Semantic Kernel AgentGroupChat
   |
   +-- ChatHistory: 协作上下文
   +-- Selection Strategy: 谁下一步发言
   +-- Termination Strategy: 何时停止
   |
External Business State
   +-- task status
   +-- approvals
   +-- artifacts
   +-- audit events
```

### 9.3 治理建议

- ChatHistory 适合对话，不适合承载全部业务状态。
- Selection/Termination Strategy 要结合结构化 state，而不是只看最后一句话。
- 关键业务动作通过 Kernel plugin 调用真实系统，并记录 event。
- 企业场景建议把审批、权限、审计放在外部服务。

---

## 十、MCP：资源和工具接入层，不是 Agent 状态协议

MCP 主要解决模型/Agent 如何连接工具和上下文资源：

- Tools：可执行动作。
- Resources：可读取上下文数据。
- Prompts：可复用指令模板。

典型用法：

```text
Agent A / Agent B
   |
   +-- MCP server: project-memory
   |      +-- resources/read("state://task/123")
   |      +-- tools/call("append_fact", ...)
   |
   +-- MCP server: artifact-store
          +-- resources/read("artifact://report/v2")
          +-- tools/call("create_artifact", ...)
```

注意：

- MCP server 暴露资源和工具，不代表所有 Agent 都能任意读写。
- 权限要在 MCP server 或上游 gateway 检查。
- MCP 适合“给 Agent 手和资料”，A2A 适合“让 Agent 和 Agent 协商任务”。
- 如果用 MCP 暴露 state store，仍要自己处理 schema、权限、冲突和审计。

---

## 十一、实现选型矩阵

| 需求 | 首选 | 组合建议 |
|------|------|----------|
| 内部复杂状态机 | LangGraph | LangGraph + artifact store + event log |
| 跨组织 Agent 互调 | A2A | 内部 LangGraph，边界 A2A |
| 对话式多 Agent 协作 | AutoGen | AutoGen + external state store |
| 快速角色化流程 | CrewAI | CrewAI + structured task output |
| OpenAI handoff 应用 | OpenAI Agents SDK | Agents SDK + external task state |
| Google Gemini 生态 | Google ADK | ADK session + artifact/memory service |
| RAG 多阶段 workflow | LlamaIndex | AgentWorkflow + evidence-backed facts |
| .NET 企业集成 | Semantic Kernel | AgentGroupChat + business state service |
| 工具和资源标准化 | MCP | MCP server + policy gateway |

---

## 十二、框架无关的落地建议

- 不要让框架的 message history 成为唯一状态源。
- 不要让 memory 替代当前任务 state。
- 不要让 checkpoint 替代 artifact store。
- 不要让 trace 替代 event log。
- 每个框架都要补一层业务 state catalog。
- 每个 Agent 输出都应能转成 state patch 或 artifact。
- 高风险字段必须走权限、审批和 verifier。

---

## 十三、延伸阅读

- A2A Core Concepts：<https://a2a-protocol.org/latest/topics/key-concepts/>
- LangGraph Persistence：<https://docs.langchain.com/oss/python/langgraph/persistence>
- LangGraph Overview：<https://docs.langchain.com/oss/python/langgraph/overview>
- AutoGen Managing State：<https://microsoft.github.io/autogen/dev/user-guide/agentchat-user-guide/tutorial/state.html>
- AutoGen AgentChat Teams：<https://microsoft.github.io/autogen/stable/user-guide/agentchat-user-guide/tutorial/teams.html>
- CrewAI Memory：<https://docs.crewai.com/en/concepts/memory>
- CrewAI Knowledge：<https://docs.crewai.com/en/concepts/knowledge>
- CrewAI Processes：<https://docs.crewai.com/en/concepts/processes>
- OpenAI Agents SDK Handoffs：<https://openai.github.io/openai-agents-python/handoffs/>
- OpenAI Agents SDK Context：<https://openai.github.io/openai-agents-python/context/>
- OpenAI Agents SDK Sessions：<https://openai.github.io/openai-agents-python/sessions/>
- OpenAI Agents SDK Tracing：<https://openai.github.io/openai-agents-python/tracing/>
- Google ADK Sessions：<https://google.github.io/adk-docs/sessions/>
- Google ADK Artifacts：<https://google.github.io/adk-docs/artifacts/>
- Google ADK Memory：<https://google.github.io/adk-docs/memory/>
- LlamaIndex AgentWorkflow：<https://docs.llamaindex.ai/en/stable/examples/agent/agent_workflow_basic/>
- Semantic Kernel Agents：<https://learn.microsoft.com/en-us/semantic-kernel/frameworks/agent/>
- MCP Architecture：<https://modelcontextprotocol.io/docs/learn/architecture>
