# 推理加速底层系统：从 Prefill/Decode 到推理引擎

> 目标：从 LLM 推理的真实执行路径理解"为什么慢、慢在哪里、每种加速技术到底省了什么"。本篇只讲底层推理系统：模型执行路径、KV cache、attention kernel、batching 调度、speculative decoding、量化、并行与评测。Agent 落地见《[19.1-Agent推理加速落地](./19.1-Agent推理加速落地.md)》。

---

## 一、推理加速到底在优化什么

推理加速不是单点技术，而是一组围绕 **延迟、吞吐、显存、成本、质量** 的系统优化。

| 指标 | 一句话 | 常见英文 |
|------|--------|----------|
| 首 token 延迟 | 用户发请求到看到第一个 token 的时间 | TTFT / Time To First Token |
| token 间隔 | 生成阶段相邻 token 的间隔 | ITL / TPOT |
| 端到端延迟 | 请求开始到完整回答结束 | E2E latency |
| 输出吞吐 | 单位时间生成多少 output token | decode throughput |
| 请求吞吐 | 单位时间完成多少请求 | requests/sec |
| Goodput | 满足 SLO 的有效吞吐 | SLO-aware throughput |
| 显存占用 | 权重 + KV cache + 临时激活 | GPU memory |
| 单请求成本 | 一次请求消耗的 GPU 时间或 API 费用 | cost/request |

**核心判断**：不同技术优化的是不同指标。流式输出降低用户感知延迟，但不一定降低总延迟；continuous batching 提高吞吐，但低并发单请求收益有限；量化降低显存和带宽，但如果 kernel 不匹配可能不提速。

---

## 二、LLM 推理的两阶段：Prefill 与 Decode

一次自回归生成可以拆成两个阶段：

```
输入 prompt
  ↓ tokenize
Prefill：一次性读完整 prompt，计算输入 token 的隐藏状态与 KV cache
  ↓
第一个 token
  ↓
Decode loop：每轮生成 1 个新 token，并把新 token 的 KV 追加进 cache
  ↓
完整输出
```

### 2.1 Prefill：读 prompt，决定 TTFT

Prefill 的特点：
- 输入长度是 `N`，模型可以并行处理 prompt 中的 token。
- 注意力在 prompt 内部是 `O(N²)`，但 GPU 并行度高。
- 更接近 **compute-bound**：大矩阵乘法多，SM 利用率通常更高。
- 主要影响 **TTFT**。

常见慢因：
- prompt 太长：system prompt、tool schema、history、RAG context 过大。
- 长上下文 attention 成本高。
- 队列里有大量 prefill 任务互相阻塞。
- prefix cache 没命中，稳定前缀被反复重算。

### 2.2 Decode：逐 token 写输出，决定 ITL/TPOT

Decode 的特点：
- 每次只处理刚生成的 1 个 token。
- 每个新 token 要 attend 到已有上下文的 KV cache。
- 生成 `M` 个 output token，需要近似 `M` 次串行 forward。
- 更接近 **memory-bandwidth-bound**：每步都要读模型权重和 KV cache。
- 主要影响 **ITL/TPOT** 和总生成时间。

常见慢因：
- 输出太长：自回归串行是硬约束。
- batch 太小：GPU 没吃满。
- KV cache 太大：长上下文 + 高并发让显存和带宽吃紧。
- 采样、约束解码、CPU 调度成为额外开销。

### 2.3 延迟公式

可以把一次调用粗略拆成：

```
TTFT ≈ 排队时间 + 网络时间 + tokenize + prefill + 首次 decode + 采样

E2E ≈ TTFT + output_tokens × TPOT + detokenize/stream
```

如果是多轮系统，还要再加工具和编排：

```
System E2E ≈ Σ(LLM 调用延迟) + Σ(工具调用延迟) + 编排/重试/等待时间
```

**核心结论**：优化前先判断瓶颈在 prefill 还是 decode。长 prompt 首响慢，优先看 prefill；长输出慢，优先看 decode；高并发吞吐低，优先看 KV cache、batching 和调度。

---

## 三、Roofline 视角：算力瓶颈还是带宽瓶颈

GPU 推理性能经常被两个上限夹住：

| 资源 | 表现 | 常见瓶颈阶段 |
|------|------|--------------|
| 算力 | Tensor Core/SM 没算完 | prefill、大 batch GEMM |
| 内存带宽 | HBM 读写跟不上 | decode、KV cache 读取、权重读取 |
| 显存容量 | OOM 或 batch 上不去 | 长上下文、高并发、大模型 |
| 通信带宽 | 多卡同步拖慢 | tensor parallel、expert parallel、P/D 分离 |
| CPU 调度 | GPU 等 CPU 发 kernel | 小模型、高 QPS、复杂采样 |

简化理解：
- **Prefill** token 多、矩阵大，通常更容易吃满算力。
- **Decode** 每步只有 1 个新 token，矩阵形状瘦，常常被权重和 KV cache 的 HBM 读带宽限制。
- **低并发** 下 GPU 可能吃不满；**高并发** 下显存/KV cache 又可能先爆。

这解释了两个常见现象：
1. 同一个模型，短输出聊天和长报告生成的瓶颈完全不同。
2. 单请求 latency 最优配置和高吞吐 serving 最优配置往往不是同一个。

---

## 四、瓶颈全景图：每种技术在省什么

| 瓶颈 | 典型表现 | 主要技术 |
|------|----------|----------|
| Prompt 过长 | TTFT 高、首 token 慢 | prompt/prefix caching、上下文压缩、chunked prefill |
| KV cache 显存爆 | 并发上不去、OOM | PagedAttention、MQA/GQA、KV quantization、KV offload/tiering |
| Decode 串行慢 | 输出长时总延迟高 | speculative decoding、Medusa/EAGLE 类多 token 预测、输出预算 |
| GPU 利用率低 | tokens/sec 低但显存没满 | continuous batching、in-flight batching、request scheduling |
| Attention IO 慢 | 长上下文或大 batch 慢 | FlashAttention、FlashAttention-2/3、fused kernels |
| 权重读带宽大 | decode 阶段吞吐低 | FP8/INT8/INT4 量化、weight-only quantization |
| 多卡通信慢 | 大模型横向扩展差 | tensor parallel、pipeline parallel、expert parallel、通信优化 |
| prefill/decode 干扰 | TTFT 或 tail ITL 抖动 | chunked prefill、prefill-decode disaggregation |
| CPU/调度开销 | 小模型高 QPS 时 CPU 飙升 | CUDA Graph、zero-overhead scheduler、批量采样 |
| 约束解码过慢 | JSON/grammar 输出吞吐下降 | FSM/regex 优化、token mask cache、结构化 schema 简化 |

**核心判断**：不要把所有慢都归因于"模型大"。推理系统的慢经常来自 KV cache 管理、调度策略、batch 形状、长 prompt、约束解码和 CPU 开销。

---

## 五、显存组成：权重、KV Cache、临时激活

推理时显存大致由三块组成：

| 组成 | 是否随请求变化 | 说明 |
|------|----------------|------|
| 模型权重 | 固定 | 参数量 × 精度；量化主要压这一块 |
| KV cache | 动态 | layers × kv_heads × seq_len × batch；长上下文和并发核心瓶颈 |
| 临时激活/工作区 | 动态 | attention/MLP 中间张量、kernel workspace、CUDA graph buffer |

简单估算：

```
权重显存 ≈ 参数量 × bytes_per_weight

KV cache 显存
≈ 2 × layers × kv_heads × head_dim × seq_len × batch × bytes_per_kv
  ↑ K/V
```

例子：70B 模型权重即使量化到 INT4，也可能因为长上下文和高并发被 KV cache 打满。反过来，7B 模型权重很小，但如果服务 128k 上下文、高并发多轮对话，KV cache 仍然会成为主要瓶颈。

---

## 六、KV Cache：推理系统的显存核心

### 6.1 KV Cache 生命周期

一次请求的 KV cache 通常经历：

```
分配 cache block
  ↓
prefill 写入 prompt 的 KV
  ↓
decode 每步追加新 token 的 KV
  ↓
请求结束释放 / 缓存为 prefix / 下沉到 CPU 或远端
```

调度器要同时回答几个问题：
- 新请求有没有足够 KV 空间？
- 哪些旧 cache 可以驱逐？
- 哪些请求共享前缀，能复用 cache？
- 高优请求能不能抢占低优请求？
- beam search / parallel sampling 的多个分支是否共享前缀？

### 6.2 MHA / MQA / GQA

| 机制 | KV 头数 | 效果 |
|------|---------|------|
| MHA | 每个 query head 各有一组 K/V | 质量强，但 KV cache 大 |
| MQA | 所有 query head 共享 1 组 K/V | KV cache 大幅下降，可能有质量损失 |
| GQA | 多组 query head 共享少量 K/V | 在质量和显存之间折中 |

现代大模型常用 GQA，因为它直接降低 decode 阶段的 KV cache 显存和内存带宽。

### 6.3 PagedAttention

传统做法会给每个请求预留连续 KV cache 空间，但生成长度动态变化，容易出现：
- 内部碎片：预留空间没用完。
- 外部碎片：显存剩余空间零散，放不下新请求。
- 复制浪费：beam search、多样本采样、共享前缀不能高效复用。

**PagedAttention 的思想**：像操作系统虚拟内存一样，把每个序列的 KV cache 切成固定大小 block，用 block table 映射到非连续物理显存。

```
逻辑 token blocks:  [0] [1] [2] [3]
                      ↓   ↓   ↓   ↓
物理 KV blocks:     B7  B2  B9  B4   （不要求连续）
```

收益：
- 减少 KV cache 碎片。
- 支持更大的有效 batch。
- 便于共享前缀、beam、parallel sampling 的 KV。
- 高并发 serving 的吞吐显著提升。

### 6.4 Prefix Cache、RadixAttention 与多级 KV Cache

很多请求共享稳定前缀：

```
System Prompt
Tool Schemas
Few-shot Examples
固定知识背景
---
动态历史
当前用户问题
```

如果前缀相同，可以直接复用已经计算好的 KV cache，跳过这部分 prefill。

常见实现思路：
- **精确前缀匹配**：完全相同 token prefix 命中。
- **Block-level prefix cache**：以 KV block 为粒度复用。
- **Radix tree**：把共享前缀组织成压缩前缀树，多个请求共享路径上的 KV。
- **多级缓存**：GPU HBM 做 L1，CPU DRAM 做 L2，远端存储/其他节点做 L3。

RadixAttention / HiCache 这类设计的核心价值，是让多轮、多用户、共享前缀明显的 workload 不再重复 prefill 同一段上下文。

注意：
- 动态字段不要插到稳定前缀中间，否则会破坏缓存命中。
- prefix cache 需要权限隔离；不同租户/不同用户的私有上下文不能误共享。
- cache eviction 策略会影响 tail latency；命中率高但频繁换入换出也可能慢。

### 6.5 KV Quantization / Offload / Tiering

当显存不够时，可以进一步做：
- **KV quantization**：把 KV cache 从 FP16/BF16 压到 FP8/INT8 等格式。
- **CPU offload**：把暂时不用的 KV 放到主机内存。
- **分层缓存**：GPU HBM → CPU DRAM → 本地/远端存储。

权衡：
- KV 量化可能影响质量，尤其是长上下文、检索召回和高精度推理任务。
- CPU offload 容易被 PCIe/NVLink 带宽限制，尾延迟可能变差。
- 分层缓存适合多轮长上下文和共享前缀明显的业务，不适合所有低延迟场景。

---

## 七、Attention 与 Kernel：把同样的数学算得更快

### 7.1 FlashAttention

标准 attention 会形成 `N × N` 注意力矩阵，显存读写压力很大。FlashAttention 的核心不是近似，而是 **IO-aware exact attention**：
- 分块计算 attention。
- 使用 online softmax。
- 不把完整 `N × N` attention matrix 实体化到 HBM。
- 降低显存读写和中间激活占用。

重要边界：
- FlashAttention 不改变 attention 的 `O(N²)` 算力复杂度。
- 它让同样的精确 attention 更省显存、更少 HBM 访问、更快。
- 长上下文和训练/推理 prefill 阶段收益明显。

### 7.2 FlashAttention-2 / 3

FlashAttention-2 主要改善 GPU work partitioning：
- 减少非矩阵乘法 FLOPs。
- 提高 occupancy。
- 更好地在线程块和 warp 间分配工作。

FlashAttention-3 进一步面向 H100/Hopper：
- 利用 asynchrony。
- 更好使用低精度能力，如 FP8。
- 在特定硬件上提升 attention kernel 利用率。

工程含义：
- 同一个模型在不同 GPU、CUDA、驱动、kernel 版本下，速度差异可能很大。
- "支持 FP8/FlashAttention"不等于实际业务一定加速，要用自己的 prompt 长度、batch、输出长度测。

### 7.3 Fused Kernels 与 CUDA Graph

LLM 推理不是只有 attention，还有 RMSNorm、RoPE、MLP、sampling、logits processor 等步骤。Kernel 越碎，CPU launch overhead 和显存往返越多。

优化方向：
- 把多个小算子 fuse 成一个 kernel。
- CUDA Graph 复用固定形状的执行图，减少 launch overhead。
- 对常见 batch/seq shape 做专门优化。

适用场景：
- 小模型高 QPS。
- decode 阶段每步计算很小，CPU 调度和 kernel launch 占比上升。
- 线上请求形状相对稳定。

### 7.4 Sparse / Sliding Window / Long Context Attention

长上下文模型还会用近似或结构化 attention：
- **Sliding window attention**：每层只看最近窗口，靠层数扩大感受野。
- **Sparse attention**：局部 attention + 少量全局 token。
- **Block sparse attention**：按块跳过不重要区域。
- **Hybrid attention/SSM**：部分层用 attention，部分层用状态空间模型。

这些技术和 FlashAttention 不同：它们改变了模型的注意力连接方式，可能影响长程精确检索能力。生产上要区分"kernel 更快"和"模型架构更省"。

---

## 八、Batching 与调度：让 GPU 一直有活干

### 8.1 Static Batching 的问题

传统 static batching 会等一批请求凑齐后一起跑，必须等 batch 里最慢的请求结束才能释放资源。

问题：
- 输出长度不同，短请求被长请求拖住。
- 新请求来了也要等当前 batch 结束。
- GPU 利用率和尾延迟都不好。

### 8.2 Continuous Batching / In-flight Batching

Continuous batching，也叫 in-flight batching / iteration-level batching：
- 每个 decode step 后检查哪些序列完成。
- 完成的序列退出 batch。
- 新请求可以插入下一轮 batch。
- prefill 和 decode 可以更细粒度交错。

收益：
- 高并发时 GPU 利用率更高。
- 请求吞吐提升。
- 减少 batch 内部等待。

权衡：
- 调度器更复杂。
- prefill 任务可能干扰 decode 的稳定 ITL。
- 需要设置 `max_num_seqs`、`max_num_batched_tokens`、GPU memory utilization 等参数。

### 8.3 Chunked Prefill

长 prompt 的 prefill 很重，如果一次性执行，会长时间占用 GPU，影响正在 decode 的请求。

Chunked prefill 把长 prompt 切成块：

```
长 prompt prefill:
[chunk1] → [decode step] → [chunk2] → [decode step] → [chunk3] ...
```

收益：
- 降低 decode 请求被 prefill 阻塞的尾延迟。
- 让长 prompt 和短输出请求更好共存。

代价：
- 单个长 prompt 的 TTFT 可能变长。
- chunk 大小要结合业务 SLO 调。

### 8.4 Admission Control 与优先级

推理系统不能无限接请求。高质量 serving 要做准入控制：
- 超长 prompt 是否拒绝或降级。
- 高优请求是否插队。
- 是否按租户/用户隔离配额。
- 是否根据 SLO 选择更小模型或更短输出。
- GPU memory 快满时是否提前排队，而不是等 OOM。

常见调度策略：
- **FCFS**：简单，但高优和短请求可能被长请求拖住。
- **Shortest remaining token first**：偏向短请求，降低平均延迟，但可能饿死长请求。
- **SLO-aware scheduling**：围绕 p95/p99 或 deadline 调度。
- **Priority queues**：按租户/任务等级分队列。
- **Preemption**：抢占低优请求，释放 KV cache 给高优请求。

**核心结论**：推理引擎不是"模型加载器"，而是一个围绕 KV cache、batch shape 和 SLO 的操作系统式调度器。

---

## 九、Speculative Decoding：用并行验证突破逐 token 串行

### 9.1 基本思想

自回归生成慢，是因为大模型每次只能吐 1 个 token。Speculative decoding 引入一个便宜的 draft model：

```
Draft model 快速猜 k 个 token：
  t1, t2, t3, ... tk

Target model 一次 forward 并行验证这些 token：
  接受前若干个 token
  拒绝处由 target model 修正
```

如果 draft 猜得准，target model 一次 forward 就能确认多个 token，相当于减少 target model 的串行 decode 次数。

### 9.2 为什么可以不改变输出分布

严格的 speculative sampling 会用接受/拒绝机制修正 draft 的分布，使最终采样分布与直接从 target model 采样一致。也就是说，它不是简单地"让小模型代写"，而是"小模型提出候选，大模型校验并保持分布正确"。

工程上也有一些非严格变体，会牺牲一点分布等价性换速度，例如某些多 token head、tree speculation、n-gram speculation。

### 9.3 收益由什么决定

粗略看，收益取决于：

```
有效加速 ≈ target 串行步数减少收益 - draft 开销 - 验证/调度开销
```

关键变量：
- `k`：每轮 draft token 数。
- 接受率：draft token 被 target 接受的比例。
- draft 成本：小模型是否真的便宜。
- target 验证效率：一次验证多个 token 是否吃满 GPU。
- 输出长度：越长越容易摊薄额外开销。

适合：
- 输出较长。
- target model decode 是瓶颈。
- draft model 足够便宜。
- draft 与 target 分布接近，接受率高。
- 任务文本模式稳定，如代码补全、格式化生成、常规说明文。

不适合：
- 输出很短，额外 draft 开销抵不过收益。
- draft 猜不准，接受率低。
- 主要瓶颈是 prefill、工具调用或网络。
- 约束解码很复杂，验证路径变重。

### 9.4 常见变体

| 变体 | 思路 | 适用 |
|------|------|------|
| 小模型 draft | 小模型先猜，大模型验证 | 通用，部署复杂度较高 |
| N-gram speculation | 从 prompt/history 里找重复片段作为 draft | 代码、重复文本、RAG 摘要 |
| Medusa 类多头 | 在目标模型上加多个预测头 | 需要模型改造或微调 |
| EAGLE 类特征预测 | 用中间特征预测未来 token | 更高接受率，系统复杂 |
| Tree speculation | 一次探索多个候选分支 | 高吞吐 serving |

**核心判断**：speculative decoding 是 decode 加速，不是 prefill 加速；它对长输出更有价值，对短决策调用帮助有限。

---

## 十、量化：少读一点，少存一点，可能更快

### 10.1 量化优化的对象

| 对象 | 例子 | 主要收益 |
|------|------|----------|
| 权重量化 | FP16 → INT8/INT4/FP8 | 降权重显存、降 decode 权重读带宽 |
| 激活量化 | W8A8、FP8 activation | 提升矩阵乘效率，依赖硬件支持 |
| KV 量化 | FP16 KV → FP8/INT8 KV | 降 KV cache 显存和带宽 |
| 混合精度 | 部分层/部分张量保高精度 | 质量与速度折中 |

### 10.2 常见路线

| 方法 | 一句话 | 常见场景 |
|------|--------|----------|
| GPTQ | 基于近似二阶信息的 post-training weight quantization | 离线压缩，INT4 常见 |
| AWQ | 识别 activation 重要权重，保护少量关键权重 | 端侧/自部署 W4A16 常见 |
| SmoothQuant | 把 activation outlier 平滑到 weight，支持 W8A8 | 服务器 INT8 推理 |
| FP8 | 利用新硬件 FP8 Tensor Core | H100/B200 等新卡 |
| GGUF/GGML | llama.cpp 生态格式 | CPU/端侧/消费级 GPU |

### 10.3 Weight-only 与 W8A8 的差异

**Weight-only quantization**：
- 只压权重，activation 仍用 FP16/BF16。
- 主要省权重显存和 decode 权重读取带宽。
- 常见于 GPTQ/AWQ/INT4。
- dequant 发生在矩阵乘前或矩阵乘中，kernel 质量决定速度。

**Weight + activation quantization**：
- 权重和 activation 都低精度。
- 可以真正利用 INT8/FP8 Tensor Core。
- 对 calibration 和 outlier 更敏感。
- 更适合服务器端追求吞吐。

### 10.4 量化不是一定更快

量化收益取决于：
- 硬件是否原生支持该精度。
- 推理引擎是否有高质量 kernel。
- batch size 和序列长度。
- 是 prefill 受益还是 decode 受益。
- 质量是否能接受。

常见反例：
- INT4 权重变小了，但 dequant 开销大，实际不快。
- 某些量化格式在当前引擎里只是能跑，不是最快。
- 小模型低 batch 时 CPU/调度才是瓶颈，量化收益小。
- 推理任务敏感，量化导致格式、数学、代码能力退化。

工程原则：
1. 先用真实业务 eval 测质量。
2. 再用真实请求分布测 TTFT/TPOT/p95。
3. 最后再决定量化位宽和格式。

---

## 十一、约束解码与结构化输出性能

结构化输出常用于 JSON、function calling、grammar、regex。它能提高可靠性，但也可能拖慢 decode。

### 11.1 为什么会慢

约束解码每一步都要根据当前 partial output 计算哪些 token 合法：

```
当前前缀: {"name": "
  ↓
语法状态机判断合法 token
  ↓
对 logits 做 mask
  ↓
采样/贪心选择
```

慢因：
- token mask 构造在 CPU 上执行。
- schema 过大，状态机复杂。
- 正则/grammar 和 tokenizer 边界不匹配。
- 每步 logits processor 太多。
- batch 内每个请求的 grammar 状态不同，难以高效并行。

### 11.2 优化方向

- 简化 JSON schema，不要让模型生成不必要的嵌套。
- 固定 schema 编译成 FSM/CFG 后复用。
- 对常见 token mask 做 cache。
- 能用 tool/function calling 原生协议时，不手写复杂格式。
- 把大对象拆成多次小对象生成。
- 用确定性代码补齐固定字段，让模型只生成不确定字段。

**核心判断**：约束解码是可靠性优化，不总是性能优化。它减少解析失败和重试，但单次 decode 可能变慢，要看失败率和重试成本的总账。

---

## 十二、并行与分布式推理

### 12.1 Data / Tensor / Pipeline / Expert Parallel

| 并行方式 | 怎么切 | 适用 |
|----------|--------|------|
| Data Parallel | 多份完整模型处理不同请求 | 模型单卡放得下，要扩吞吐 |
| Tensor Parallel | 一个层的矩阵切到多卡 | 模型单卡放不下，低延迟多卡 |
| Pipeline Parallel | 不同层放不同卡 | 超大模型，多卡流水 |
| Expert Parallel | MoE 专家分到多卡 | MoE 模型，如 Mixtral/DeepSeek 类 |
| Context/Sequence Parallel | 长上下文序列维度切分 | 超长上下文 |

权衡：
- 单卡放得下时，data parallel 最简单。
- tensor parallel 会引入高频 all-reduce，依赖 NVLink/IB。
- pipeline parallel 有气泡，batch 太小不划算。
- MoE 的 expert parallel 要处理路由不均衡。
- context parallel 适合超长上下文，但通信复杂。

### 12.2 Prefill-Decode Disaggregation

Prefill 和 decode 的资源特征不同：
- Prefill 更 compute-bound，长 prompt 需要大块算力。
- Decode 更 memory-bandwidth-bound，逐 token 串行、对 tail latency 敏感。

Prefill-decode disaggregation 把两阶段拆到不同实例/机器：

```
Prefill Pool：计算 prompt KV
      ↓ 传输 KV cache
Decode Pool：稳定生成 output tokens
```

收益：
- TTFT 和 ITL 可以分开调优。
- 长 prompt prefill 不容易干扰 decode tail latency。
- 大规模服务可以按 workload 独立扩容。

代价：
- KV cache 传输成为新瓶颈。
- 系统复杂度显著上升。
- 低并发或小模型场景可能不划算。
- 某些引擎里该能力仍在快速演进，要谨慎上生产。

### 12.3 多租户与多模型 Serving

生产系统常常不是只服务一个模型：
- 一个大模型负责复杂任务。
- 多个小模型负责分类、路由、摘要、embedding。
- 多个 LoRA adapter 服务不同业务。
- 不同租户有不同 SLO 和配额。

推理加速不只是单模型跑分，还包括：
- 模型池化与热加载。
- LoRA batching。
- 请求路由。
- 配额隔离。
- 低优任务批处理。
- 失败降级。

---

## 十三、主流推理引擎与适用场景

| 引擎 | 核心优势 | 适合 |
|------|----------|------|
| **vLLM** | PagedAttention、continuous batching、OpenAI-compatible API、生态成熟 | 通用 GPU serving、自部署 API |
| **SGLang** | RadixAttention/prefix cache、结构化生成、agent/program workload 友好 | 多轮/共享前缀/结构化输出重的系统 |
| **TensorRT-LLM** | NVIDIA 深度优化、FP8/INT4、in-flight batching、Triton 集成 | NVIDIA GPU 上追求极致性能 |
| **TGI** | HuggingFace 生态、部署简单 | HF 模型服务、常规生产部署 |
| **llama.cpp** | CPU/端侧/消费级设备、GGUF 生态 | 本地、边缘、低成本部署 |
| **Triton Inference Server** | 模型服务编排、TensorRT-LLM backend | 企业级统一 serving |
| **LMCache / HiCache 类系统** | KV cache 分层、跨请求复用 | 长上下文、多轮、多租户场景 |

选择顺序建议：
1. 先明确硬件：CPU、消费级 GPU、A10/A100/H100/B200、单机还是多机。
2. 再明确 workload：短问答、长上下文、代码、RAG、Agent、多 LoRA、batch 离线。
3. 再看功能：prefix cache、structured output、spec decode、quantization、P/D disaggregation。
4. 最后用业务 trace 跑基准，不用单条 demo 的 tokens/sec 做结论。

---

## 十四、推理引擎调参 Checklist

| 参数/策略 | 影响 | 典型风险 |
|-----------|------|----------|
| `max_model_len` | 最大上下文和 KV 上限 | 设太大导致并发下降 |
| `gpu_memory_utilization` | 可用于 KV 的显存比例 | 太高容易 OOM，太低浪费 |
| `max_num_seqs` | 同时服务序列数 | 太高 tail latency 上升 |
| `max_num_batched_tokens` | 单批 token 上限 | 影响 prefill/decode 混排 |
| block size/page size | KV 管理粒度 | 太大碎片多，太小 metadata 开销高 |
| prefix caching | prefill 命中 | 动态前缀会导致命中率低 |
| chunked prefill | decode tail latency | chunk 太小调度开销高 |
| quantization format | 显存/速度/质量 | kernel 不匹配可能变慢 |
| speculative decoding | decode 加速 | 接受率低时反而慢 |
| tensor parallel size | 大模型多卡 | 通信开销和部署复杂度 |

调参顺序：
1. 固定业务 workload 和 SLO。
2. 先跑 baseline，记录 TTFT/TPOT/p95/GPU/KV。
3. 一次只改一个关键参数。
4. 同时看吞吐和 tail latency。
5. 最后用业务质量 eval 验证量化、spec decode、长上下文设置。

---

## 十五、如何做推理性能评测

### 15.1 必测指标

| 指标 | 为什么重要 |
|------|------------|
| TTFT p50/p95/p99 | 用户是否觉得"有响应" |
| ITL/TPOT p50/p95/p99 | 流式输出是否顺滑 |
| E2E latency | 完整任务等待时间 |
| output tokens/sec | 生成吞吐 |
| requests/sec | 服务吞吐 |
| cache hit rate | prefix/KV cache 是否真的生效 |
| GPU SM utilization | 算力是否吃满 |
| GPU memory / KV usage | 显存瓶颈在哪里 |
| HBM bandwidth | decode 是否带宽瓶颈 |
| queue time | 是模型慢还是排队慢 |
| error/OOM/abort rate | 高负载下是否稳定 |

### 15.2 Workload 维度

不要只测一个 prompt。至少覆盖：
- input length：短 prompt、中 prompt、长上下文。
- output length：短答、长答、报告生成。
- 并发：1、4、16、64、256。
- 采样参数：temperature、top_p、beam、best-of。
- 结构化输出：JSON/grammar/function calling。
- prefix 共享程度：无共享、同系统 prompt、同工具 schema、同长文档。
- 多轮系统：单轮、多轮、工具串行/并行。

### 15.3 常见评测陷阱

1. 只看平均 tokens/sec，不看 p95/p99。
2. 用固定长度 synthetic prompt，忽略真实业务分布。
3. 没区分 prefill tokens/sec 和 decode tokens/sec。
4. warm-up 不充分，把模型加载/编译时间算进来。
5. client 端生成请求太慢，误以为 server 慢。
6. 只测低并发，continuous batching 看不到收益。
7. 只测质量，不测失败率、OOM、tail latency。
8. 量化只测速度，不跑业务 eval。

**核心原则**：推理加速的结论必须绑定 workload。没有请求分布的 benchmark 只能做参考，不能做架构决策。

---

## 十六、核心问题（含解答）

### Q1：TTFT 和 TPOT 分别受什么影响？

**答**：

TTFT 主要受排队、网络、tokenize、prefill 和首轮 decode 影响。input token 越多，prefill 越重，TTFT 越高。

TPOT/ITL 主要受 decode 阶段影响。每个输出 token 都要一次自回归 forward，并读取权重和 KV cache，所以输出越长，总延迟越高。decode 常常是 memory-bandwidth-bound。

---

### Q2：为什么 prompt 很长时首 token 特别慢？

**答**：

因为生成第一个 token 前必须先做 prefill：模型要读完整 prompt，计算每层每个输入 token 的 hidden state 和 KV cache。长 prompt 会增加 attention 计算、显存占用和 KV cache 写入。即使之后 decode 很快，TTFT 也会被 prefill 拉高。

优化手段：
- prompt/prefix caching。
- 删除冗余 system prompt 和 history。
- RAG 只注入必要片段。
- chunked prefill。
- 长上下文场景考虑 P/D disaggregation。

---

### Q3：为什么输出长时很难加速？

**答**：

自回归模型一次只能基于已有 token 预测下一个 token。生成 `M` 个 token 需要近似 `M` 次串行 forward，这是结构性约束。可以用 speculative decoding 一次验证多个候选 token，但前提是 draft 接受率高；也可以限制输出长度、分段生成、让工具生成模板内容，但不能完全消除串行性。

---

### Q4：PagedAttention 和 FlashAttention 的区别是什么？

**答**：

PagedAttention 解决 **KV cache 内存管理**：把每个请求的 KV cache 分块，用类似虚拟内存的方式减少碎片、支持共享、提高 batch 能力。

FlashAttention 解决 **attention kernel 的 IO 效率**：分块计算精确 attention，不实体化完整 `N × N` attention 矩阵，减少 HBM 读写。

一句话：PagedAttention 管 cache，FlashAttention 算 attention。

---

### Q5：Continuous batching 为什么能提升吞吐？

**答**：

因为它不让一个 batch 被最慢请求拖死。每一轮 decode 后，完成的请求退出，新请求加入，GPU 始终保持较高利用率。高并发、输出长度差异大的场景收益明显。

但它也可能带来调度复杂度和 tail latency 抖动，尤其是 prefill 与 decode 混在一起时，需要 chunked prefill、优先级或 P/D disaggregation 配合。

---

### Q6：Speculative decoding 是不是会降低质量？

**答**：

严格 speculative sampling 可以保持与 target model 相同的采样分布，因此理论上不降低质量。工程变体如果使用非严格接受策略、多 token head 或启发式 draft，可能引入质量差异，需要评测。

它的收益取决于 draft 成本和接受率。接受率低时，不仅不快，还会增加开销。

---

### Q7：量化是不是总能降延迟？

**答**：

不是。量化一定能降低模型大小和显存占用，但不一定降低延迟。是否更快取决于硬件、kernel、batch、序列长度和 dequant 开销。某些 INT4 格式在某些引擎中只是省显存，不一定比 FP16/FP8 快。

生产上要同时测：
- 业务质量。
- TTFT/TPOT。
- p95/p99。
- OOM 和错误率。
- 成本/request。

---

### Q8：为什么 P/D disaggregation 不一定提升吞吐？

**答**：

P/D disaggregation 的主要价值是隔离 prefill 和 decode，让 TTFT、ITL、tail latency 更可控。它会引入 KV cache 传输和多实例调度成本，所以不一定提升总吞吐。低并发、小模型、短 prompt 场景可能得不偿失；长上下文、高并发、prefill/decode 干扰严重时才更值得。

---

### Q9：约束解码会不会加速结构化输出？

**答**：

它加速的是"系统成功完成任务"而不是一定加速单次 decode。约束解码能减少 JSON 解析失败和重试，但每步 token mask/FSM 状态更新可能变慢。复杂 schema、高并发不同 grammar 状态时，开销更明显。生产上要比较"约束解码一次成功"和"普通生成失败重试"的总延迟。

---

### Q10：vLLM、SGLang、TensorRT-LLM 怎么选？

**答**：

- **vLLM**：默认通用选择，生态成熟，OpenAI-compatible API 方便，PagedAttention/continuous batching 完整。
- **SGLang**：如果工作负载有大量共享前缀、多轮 program、结构化输出、agent workflow，值得重点看。
- **TensorRT-LLM**：如果硬件是 NVIDIA 并追求极致性能、FP8/INT4、多卡优化和 Triton 集成，适合投入更高工程成本。

最终以业务 trace benchmark 为准。

---

## 十七、关键术语 Cheat Sheet

| 术语 | 一句话 |
|------|--------|
| TTFT | 从请求到第一个 token 的时间，主要受 prefill 影响 |
| TPOT / ITL | 每个输出 token 的生成间隔，主要受 decode 影响 |
| Prefill | 读完整 prompt，计算输入 token 的 KV cache |
| Decode | 逐 token 生成，每步读取权重和历史 KV |
| KV Cache | 缓存每层历史 token 的 Key/Value，推理显存核心 |
| MQA/GQA | 减少 KV 头数，降低 KV cache 显存和带宽 |
| PagedAttention | 把 KV cache 分块管理，减少碎片，提高 batch 能力 |
| Prefix Caching | 复用相同前缀的 KV cache，跳过重复 prefill |
| RadixAttention | 用前缀树组织共享 KV cache，提高多轮/共享前缀复用 |
| FlashAttention | IO-aware 精确 attention，不实体化完整注意力矩阵 |
| Continuous Batching | 每个 decode step 动态加入/移除请求，提高吞吐 |
| In-flight Batching | continuous batching 的另一种叫法 |
| Chunked Prefill | 把长 prompt prefill 切块，降低对 decode 的阻塞 |
| Speculative Decoding | 小模型猜 token，大模型并行验证 |
| Quantization | 降低权重/激活/KV 精度，换显存和速度 |
| Guided Decoding | 用 schema/grammar 限制合法 token，提高结构化可靠性 |
| P/D Disaggregation | prefill 与 decode 分离部署，各自调优 |
| Goodput | 满足延迟 SLO 的有效吞吐 |

---

## 十八、延伸阅读

### 底层论文与系统
- [Efficient Memory Management for Large Language Model Serving with PagedAttention](https://arxiv.org/abs/2309.06180)
- [vLLM Paged Attention 文档](https://docs.vllm.ai/en/v0.13.0/design/paged_attention/)
- [SGLang HiCache / RadixAttention 文档](https://docs.sglang.io/docs/advanced_features/hicache_design)
- [TensorRT-LLM: Paged Attention, IFB, and Request Scheduling](https://nvidia.github.io/TensorRT-LLM/features/paged-attention-ifb-scheduler.html)
- [FlashAttention-2: Faster Attention with Better Parallelism and Work Partitioning](https://arxiv.org/abs/2307.08691)
- [FlashAttention-3: Fast and Accurate Attention with Asynchrony and Low-precision](https://arxiv.org/abs/2407.08608)
- [Fast Inference from Transformers via Speculative Decoding](https://proceedings.mlr.press/v202/leviathan23a.html)
- [GQA: Training Generalized Multi-Query Transformer Models from Multi-Head Checkpoints](https://arxiv.org/abs/2305.13245)
- [SmoothQuant: Accurate and Efficient Post-Training Quantization for Large Language Models](https://arxiv.org/abs/2211.10438)
- [AWQ: Activation-aware Weight Quantization for LLM Compression and Acceleration](https://arxiv.org/abs/2306.00978)
- [GPTQ: Accurate Post-Training Quantization for Generative Pre-trained Transformers](https://arxiv.org/abs/2210.17323)
- [vLLM Disaggregated Prefilling 文档](https://docs.vllm.ai/en/latest/features/disagg_prefill.html)

### 本仓库关联
- 《[08-工程化性能成本可靠性](./08-工程化性能成本可靠性.md)》：性能、成本、可靠性的系统权衡。
- 《[12-Reasoning Models专题](./12-Reasoning%20Models专题.md)》：test-time compute 与 reasoning token 的成本/延迟。
- 《[17-上下文窗口的模型原理](./17-上下文窗口的模型原理.md)》：自注意力、KV cache、长上下文退化的底层原因。
- 《[19.1-Agent推理加速落地](./19.1-Agent推理加速落地.md)》：把本篇底层技术映射到 Agent 架构与工程落地。

---

## 十九、核心要点速记

1. LLM 推理分 prefill 和 decode：prefill 决定 TTFT，decode 决定 TPOT/长输出总延迟。
2. Prefill 更偏 compute-bound，decode 更偏 memory-bandwidth-bound；调优前先定位瓶颈。
3. KV cache 是推理显存核心，随上下文长度和并发增长；长上下文服务常先卡显存和带宽。
4. PagedAttention 管 KV cache 内存，FlashAttention 优化 attention kernel IO，二者解决的问题不同。
5. Prefix/Radix cache 的关键是稳定 token 前缀；动态字段插错位置会直接毁掉命中率。
6. Continuous batching 提高高并发吞吐，但要控制 prefill 对 decode tail latency 的干扰。
7. Speculative decoding 主要加速 decode，对长输出更有价值，对短调用帮助有限。
8. 量化先省显存，再看是否提速；实际速度取决于硬件、kernel、batch 和 dequant 开销。
9. Guided decoding 提升结构化可靠性，但单次 decode 可能更慢，要算失败重试总账。
10. 所有推理加速结论都必须绑定真实 workload，用 TTFT/TPOT/p95/cache hit/GPU 利用率一起评估。
