# 记忆与上下文工程

> 目标：建立 Agent 记忆与上下文工程的总览框架。本文是入口文档；生产级记忆和 prompt/context 组装已拆成两篇子文档。

---

## 本文职责边界

| 本文负责 | 本文不展开 |
|---|---|
| Memory、Context、State、Artifact、Trace 的概念边界、阅读路径和术语总览 | 记忆生命周期见 [04.1-生产级Agent记忆工程.md](./04.1-生产级Agent记忆工程.md)；单次上下文组装见 [04.2-Agent上下文工程.md](./04.2-Agent上下文工程.md)；多轮 loop 历史增长治理见 [16.2-Agent Loop上下文管理.md](./16.2-Agent%20Loop上下文管理.md)；模型窗口原理见 [17-上下文窗口的模型原理.md](./17-上下文窗口的模型原理.md) |

---

## 一、先区分两件事

很多 Agent 系统把“记忆”和“上下文”混在一起，最后会出现两类问题：

- 记忆乱写：什么都存、什么都召回、越用越脏。
- 上下文乱塞：prompt 越拼越长，模型看了很多但真正有用的信息很少。

先用一句话区分：

```text
记忆工程：决定哪些信息要跨轮/跨会话留下来，以及未来怎么找回来。
上下文工程：决定本次模型调用前，哪些信息应该进入 context window，以及按什么结构进入。
```

更细一点：

| 概念 | 解决的问题 | 生命周期 | 典型载体 |
|------|------------|----------|----------|
| Memory | 以后是否还能记得 | 跨轮、跨会话、跨任务 | 文件、DB、向量库、图谱、session store |
| Context | 这一次推理能看到什么 | 单次模型调用 | system/developer/user messages、retrieved snippets、state snapshot |
| State | 当前任务事实是什么 | 当前 run / task | Typed state、SQL、KV、checkpoint |
| Artifact | 可复查产物在哪里 | 任务内或长期 | 文件、报告、代码 diff、日志、object store |
| Trace | 为什么走到这一步 | 执行过程 | observability span、event log |

核心判断：

> 记忆不是把历史塞进 prompt；上下文也不是把所有记忆都取出来。生产级 Agent 要把“存什么”和“本次用什么”分开设计。

---

## 二、分篇阅读路径

| 文档 | 重点问题 | 适合什么时候读 |
|------|----------|----------------|
| [04.1-生产级Agent记忆工程.md](./04.1-生产级Agent记忆工程.md) | 生产级 Agent 如何提取、验证、存储、召回、注入、更新、删除和评估记忆 | 设计跨会话长期记忆、用户画像、项目记忆、经验复用时 |
| [04.2-Agent上下文工程.md](./04.2-Agent上下文工程.md) | 每次模型调用前如何拼 prompt/context，如何做预算、排序、压缩、引用化、防污染和可观测 | 设计 prompt 模板、RAG 注入、工具结果注入、长上下文调用时 |
| [16.2-Agent Loop上下文管理.md](./16.2-Agent%20Loop上下文管理.md) | Agent loop 多轮运行中历史单调增长时，如何截断、摘要、外置、引用化 | 处理长任务跑 30 轮、工具结果撑爆窗口、auto-compaction 时 |
| [17-上下文窗口的模型原理.md](./17-上下文窗口的模型原理.md) | 为什么上下文窗口有成本、注意力退化、Lost in the Middle、KV cache 压力 | 想理解上下文策略背后的模型/系统原因时 |

推荐顺序：

```text
先读本文总览
  -> 04.1 搭记忆流水线
  -> 04.2 学会组装单次调用上下文
  -> 16.2 处理 agent loop 长程增长
  -> 17 理解模型原理约束
```

---

## 三、Agent 记忆的基本分类

| 记忆类型 | 含义 | Agent 中的对应 | 典型用途 |
|----------|------|----------------|----------|
| 工作记忆 | 当前任务内临时信息 | 当前 prompt、scratchpad、run state | 当前步骤决策 |
| 情景记忆 | 具体发生过什么 | 对话、工具调用、任务事件 | 回忆“上次怎么处理” |
| 语义记忆 | 抽象事实和偏好 | 用户偏好、项目事实、领域知识 | 个性化、减少重复说明 |
| 程序记忆 | 怎么做某类任务 | workflow、runbook、prompt 模板、经验规则 | 复用成功做法 |
| 反思记忆 | 做错过什么、如何改 | reviewer finding、postmortem、lesson learned | 防止重复犯错 |

生产系统里常见的是混合结构：

```text
Core memory      小而稳定，启动时显式注入
Session memory   当前会话历史，由 session/checkpoint 管
Archival memory  大而长尾，按需检索
Artifact store   大对象和证据，不直接当记忆塞 prompt
Procedural docs  人写/Agent 总结的流程规则
```

---

## 四、上下文工程的基本动作

上下文工程关注的是“本次调用前如何组织输入”。常见动作：

| 动作 | 问题 |
|------|------|
| Write | 哪些信息要写入状态、记忆或 artifact |
| Select | 本轮从记忆、RAG、state、工具结果中选哪些 |
| Compress | 过长内容如何摘要、抽取、结构化、引用化 |
| Order | 重要信息放在哪里，如何对抗 Lost in the Middle |
| Isolate | 指令、用户输入、检索材料、工具输出如何隔离 |
| Cite | 每段上下文的来源和可信度如何保留 |
| Budget | system、history、memory、RAG、tool、schema 各占多少 token |
| Observe | 记录本次到底给模型塞了什么，方便回归和调试 |

上下文工程不是“把 prompt 写得更优美”，而是一个上下文供应链：

```text
state + memory + RAG + tool results + user request
  -> filter
  -> rank
  -> compress
  -> render
  -> model call
  -> trace context manifest
```

---

## 五、记忆和上下文的关键区别

### 5.1 记忆不一定进上下文

长期记忆库里可能有 10 万条信息，但本轮只应该注入少量高相关、高可信、当前任务需要的信息。

例如：

```text
用户喜欢 TypeScript            -> coding 任务常驻或高优先级召回
用户去年去过东京               -> 当前问数据库迁移时不召回
项目曾经因为时区 bug 出过事故   -> 涉及时区代码时召回
```

### 5.2 上下文不一定写成记忆

本轮 prompt 里出现的信息不一定要存长期记忆。

不应写入长期记忆的例子：

- 一次性任务参数。
- 中间推理草稿。
- 可从 artifact 或日志重新获取的长数据。
- 未验证的模型猜测。
- 用户临时说“这次先这样”。

### 5.3 State 优先于 Memory

当前任务 state 是强约束，长期 memory 是辅助。

如果两者冲突：

```text
当前任务 state：用户这次明确要求 Python
长期 memory：用户通常偏好 TypeScript
```

本轮应服从当前任务 state，并可把冲突记录为“本次例外”，而不是覆盖用户偏好。

---

## 六、生产里最常见的设计分层

```text
Stable Prefix
  - system/developer instructions
  - tool schema
  - core memory

Task Context
  - user request
  - task state
  - constraints
  - output contract

Retrieved Context
  - long-term memory snippets
  - RAG documents
  - prior decisions

Operational Context
  - recent messages
  - tool summaries
  - artifact references
  - errors / retry info
```

设计顺序：

1. 先确定任务 state。
2. 再确定哪些 core memory 必须常驻。
3. 再决定本轮需要召回哪些长期记忆和文档。
4. 再处理工具结果和历史压缩。
5. 最后渲染成 prompt/context。

---

## 七、核心术语

| 术语 | 含义 |
|------|------|
| Core Memory | 小而稳定、经常显式注入的核心记忆 |
| Archival Memory | 大而长尾、按需检索的长期记忆 |
| Session Memory | 当前会话历史和短期状态 |
| Memory Extraction | 从对话、工具结果、任务事件中抽取值得记的信息 |
| Memory Consolidation | 去重、合并、晋升、降级、过期记忆 |
| Explicit Injection | 会话启动或本轮 prompt 中直接放入记忆 |
| On-demand Recall | 根据任务需要检索或用工具搜索记忆 |
| Context Manifest | 本次调用使用了哪些上下文片段的记录 |
| Context Budget | 各类上下文的 token 分配 |
| Artifact Reference | 大对象不直接塞 prompt，只传 id、摘要、引用 |
| Lost in the Middle | 长上下文中间位置的信息利用率下降 |
| Context Pollution | 错误、无关或恶意内容进入上下文并影响推理 |

---

## 八、自测清单

- [ ] 能区分 Memory、Context、State、Artifact、Trace。
- [ ] 能说明为什么不能把所有记忆都注入 prompt。
- [ ] 能设计一条记忆提取、验证、存储、召回、注入流水线。
- [ ] 能判断哪些记忆适合显式注入，哪些适合按需召回。
- [ ] 能解释 core memory 与 archival memory 的区别。
- [ ] 能设计 prompt/context 的分层结构。
- [ ] 能给不同上下文类别分配 token 预算。
- [ ] 能解释摘要压缩、结构化压缩、引用化的取舍。
- [ ] 能说明上下文工程和 agent loop 上下文管理的区别。
- [ ] 能设计 context manifest 以便调试和评估。

---

## 九、延伸阅读

- LangGraph Persistence 与 Store：<https://docs.langchain.com/oss/python/langgraph/persistence>
- OpenAI Agents SDK Sessions：<https://openai.github.io/openai-agents-python/sessions/>
- OpenAI Prompt Engineering：<https://platform.openai.com/docs/guides/prompt-engineering>
- OpenAI Prompt Caching：<https://platform.openai.com/docs/guides/prompt-caching>
- Letta Memory Management：<https://docs.letta.com/concepts/memory-management>
- Letta Context Engineering：<https://docs.letta.com/guides/agents/context-engineering>
- LlamaIndex Agent Memory：<https://docs.llamaindex.ai/en/stable/module_guides/deploying/agents/memory/>
- 本仓库相关文档：[04.1-生产级Agent记忆工程.md](./04.1-生产级Agent记忆工程.md)、[04.2-Agent上下文工程.md](./04.2-Agent上下文工程.md)、[16.2-Agent Loop上下文管理.md](./16.2-Agent%20Loop上下文管理.md)、[17-上下文窗口的模型原理.md](./17-上下文窗口的模型原理.md)
