# Agent Runtime 完整实现

> 目标：用一个可运行的最小实现把 Agent Runtime 拆开看清楚。示例不依赖外部库，不调用真实模型，重点展示 runtime 的完整控制逻辑：`RunState`、模型动作、工具网关、策略审批、暂停恢复、checkpoint、event log。

示例文件：

- [examples/agent_runtime_demo.py](./examples/agent_runtime_demo.py)

运行方式：

```bash
python3 examples/agent_runtime_demo.py
```

---

## 本文职责边界

| 本文负责 | 本文不展开 |
|---|---|
| 用 `examples/agent_runtime_demo.py` 展示一个最小完整 Runtime 的控制逻辑、状态流转、审批、checkpoint 和 event log | Runtime 总体概念见 [20-Agent Runtime专题.md](./20-Agent%20Runtime专题.md)；主流实现对比见 [20.2-Agent Runtime主流实现对比.md](./20.2-Agent%20Runtime主流实现对比.md) |

---

## 一、示例要解决的问题

场景是一个退款 Agent：

```text
用户：Please refund order ORD-1001 because the package was lost.

Agent 需要：
1. 查询订单
2. 判断是否符合退款条件
3. 如果退款金额较高，暂停并等待人工审批
4. 审批通过后执行退款
5. 生成最终答复
6. 整个过程可追踪、可恢复、可审计
```

这个例子故意选择"退款"，因为它具备生产 Agent 的几个关键特征：

| 特征 | 为什么重要 |
|---|---|
| 需要查询事实 | 模型不能凭空决定订单状态 |
| 有副作用工具 | 退款会改变外部系统 |
| 有风险策略 | 高金额退款需要人工审批 |
| 需要暂停恢复 | 审批可能在几分钟或几小时后发生 |
| 需要幂等 | resume 或重试不能重复退款 |
| 需要事件日志 | 出问题时要知道每一步发生了什么 |

---

## 二、最小 Runtime 结构

示例代码由五部分组成：

```text
Data Model
  Message / ToolCall / ToolResult / RuntimeEvent / AgentSpec / RunState

Tool Gateway
  ToolSpec / ToolGateway
  schema-like dispatch / approval policy / idempotency cache

Model Stub
  MockRefundModel.next_action()
  用确定性逻辑模拟 LLM 决策

Runtime Controller
  create_run()
  run_until_blocked_or_done()
  resume_with_approval()
  checkpoint / event log

Business Tools
  lookup_order()
  issue_refund()
  refund_approval_policy()
```

真实系统中，`MockRefundModel` 会替换成 OpenAI、Claude、Gemini 或本地模型调用；`ToolGateway` 会接真实 API、MCP server、数据库、队列或沙箱。

---

## 三、核心数据模型

### 3.1 AgentSpec：静态配置

```python
@dataclass
class AgentSpec:
    agent_id: str
    name: str
    instructions: str
    tools: list[str]
    max_turns: int = 8
```

它描述"这个 Agent 是谁、能用什么工具、最大能跑多少轮"。

生产里还可以扩展：

| 字段 | 用途 |
|---|---|
| `model_profile` | 模型、温度、reasoning budget |
| `tool_policy` | 工具权限和风险等级 |
| `memory_profile` | 默认注入哪些记忆，哪些按需召回 |
| `output_schema` | 最终结构化输出 |
| `handoff_targets` | 可转交的其他 Agent |

### 3.2 RunState：动态状态

```python
@dataclass
class RunState:
    run_id: str
    agent: AgentSpec
    user_input: str
    status: str
    turn: int = 0
    messages: list[Message] = field(default_factory=list)
    tool_results: list[ToolResult] = field(default_factory=list)
    scratchpad: dict[str, Any] = field(default_factory=dict)
    pending_tool_call: ToolCall | None = None
    approvals: list[dict[str, Any]] = field(default_factory=list)
    final_output: str | None = None
    events: list[RuntimeEvent] = field(default_factory=list)
    checkpoints: list[dict[str, Any]] = field(default_factory=list)
```

这里体现了一个重要原则：**不要把 `messages` 当成唯一状态**。

| 状态 | 示例 |
|---|---|
| `messages` | 给模型看的对话和工具 observation |
| `tool_results` | 结构化工具结果 |
| `scratchpad` | runtime 自己维护的结构化中间状态 |
| `pending_tool_call` | 暂停时等待审批的工具调用 |
| `approvals` | 审批记录 |
| `events` | 发生过的事件 |
| `checkpoints` | 可恢复快照 |

如果只保存 messages，很多生产能力都会很难做：审批恢复、幂等、审计、状态迁移、重放、指标统计。

---

## 四、Runtime 创建 Run

示例中的入口是：

```python
run = runtime.create_run(agent, "Please refund order ORD-1001 because the package was lost.")
```

`create_run()` 做了三件事：

```python
run = RunState(
    run_id=f"run_{self._run_seq:03d}",
    agent=agent,
    user_input=user_input,
    status="created",
    messages=[Message(role="user", content=user_input)],
)
self.runs[run.run_id] = run
self._record_event(run, "run.created", {"agent": agent.name})
self._checkpoint(run, "created")
```

这一步对应生产系统中的：

| 动作 | 生产实现 |
|---|---|
| 生成 `run_id` | UUID / snowflake / DB sequence |
| 初始化状态 | 写 `runs` 表 |
| 写事件 | append-only event log |
| 写 checkpoint | durable storage |
| 返回任务句柄 | API 返回 `run_id` 给前端或队列 |

---

## 五、Runtime 如何驱动 Loop

核心方法是：

```python
runtime.run_until_blocked_or_done(run.run_id)
```

它的行为不是"一直跑到死"，而是：

```text
while run.status == running:
  1. 检查 max_turns
  2. 写 checkpoint
  3. 调模型拿下一步 action
  4. 如果是 final，完成
  5. 如果是 tool_call，交给 ToolGateway
  6. 如果需要审批，暂停并保存 pending_tool_call
  7. 如果工具执行完成，写结果并继续下一轮
```

关键点是 `run_until_blocked_or_done` 这个命名：

| 结果 | 含义 |
|---|---|
| `completed` | 任务完成 |
| `failed` | 达到停止条件或异常 |
| `awaiting_approval` | 暂停等待人工 |
| `awaiting_user_input` | 可扩展为等待用户补充信息 |
| `awaiting_remote_agent` | 可扩展为等待远程 Agent |

生产 runtime 不应该假设任务一定能在一次 HTTP 请求内完成。

---

## 六、模型动作：让 LLM 只提出意图

示例用 `MockRefundModel` 模拟模型：

```python
class MockRefundModel:
    def next_action(self, run: RunState) -> ModelAction:
        ...
```

它返回内部标准动作：

```python
@dataclass
class ModelAction:
    kind: str
    reason: str
    tool_call: ToolCall | None = None
    final_output: str | None = None
```

真实模型返回的可能是 OpenAI tool calls、Claude tool use、Gemini function call 或自定义 JSON。Runtime 最好统一转成内部 `ModelAction`：

```text
Provider raw response
  -> Model Adapter
  -> ModelAction(kind=tool_call/final/handoff/ask_user)
  -> Runtime State Machine
```

这样上层状态机不依赖某个模型 API。

---

## 七、Tool Gateway：所有工具都经过网关

示例中工具注册如下：

```python
gateway.register(ToolSpec(name="lookup_order", func=lookup_order))
gateway.register(
    ToolSpec(
        name="issue_refund",
        func=issue_refund,
        approval_policy=refund_approval_policy,
        idempotency_key=refund_idempotency_key,
    )
)
```

`lookup_order` 是读工具，可以直接执行。

`issue_refund` 是有副作用工具，额外绑定：

| 机制 | 作用 |
|---|---|
| `approval_policy` | 判断是否需要人工审批 |
| `idempotency_key` | 避免重复退款 |

### 7.1 审批策略

```python
def refund_approval_policy(_run: RunState, call: ToolCall) -> PolicyDecision:
    amount = int(call.args["amount"])
    if amount >= 50:
        return PolicyDecision(
            required=True,
            reason=f"refund_amount={amount} requires human approval",
            approver_hint="support_lead",
        )
    return PolicyDecision(required=False, reason="low_risk_refund")
```

模型可以提出 `issue_refund`，但能不能执行由 runtime policy 决定。

这就是生产 Agent 的关键边界：

```text
LLM proposes action
Runtime validates action
Policy decides risk
Tool Gateway executes or blocks
```

### 7.2 幂等键

```python
def refund_idempotency_key(call: ToolCall) -> str:
    return f"refund:{call.args['order_id']}:{call.args['amount']}:{call.args['reason']}"
```

如果审批通过后网络超时，runtime 重试 `issue_refund`，幂等键可以避免同一订单重复退款。

真实生产中，幂等键通常要传给下游支付/订单系统，而不只是 runtime 本地缓存。

---

## 八、暂停：从 running 到 awaiting_approval

当模型提出高金额退款时，Tool Gateway 返回 `approval_required`：

```python
if mode == "approval_required":
    run.status = "awaiting_approval"
    run.pending_tool_call = action.tool_call
    self._record_event(...)
    self._checkpoint(run, "awaiting_approval")
    return run
```

这一步特别重要。

错误做法是：

```text
if refund > 50:
  send_slack_message()
  sleep_until_someone_replies()
```

正确做法是把等待变成持久化状态：

```text
status = awaiting_approval
pending_tool_call = issue_refund(...)
checkpoint = saved
event = approval.requested
```

这样即使服务重启、worker 迁移、审批延迟，任务仍然可以恢复。

---

## 九、Checkpoint 保存了什么

示例的 checkpoint 不是简单保存字符串，而是保存可恢复快照：

```python
{
    "run_id": run.run_id,
    "status": run.status,
    "turn": run.turn,
    "agent": asdict(run.agent),
    "user_input": run.user_input,
    "messages": [asdict(message) for message in run.messages],
    "tool_results": [asdict(result) for result in run.tool_results],
    "scratchpad": deepcopy(run.scratchpad),
    "pending_tool_call": asdict(run.pending_tool_call) if run.pending_tool_call else None,
    "approvals": deepcopy(run.approvals),
    "final_output": run.final_output,
}
```

一个好的 checkpoint 至少回答：

| 问题 | checkpoint 需要保存 |
|---|---|
| 现在跑到哪里 | `turn` / `step_cursor` / `status` |
| 为什么暂停 | `pending_tool_call` 或 `pending_action` |
| 之前知道了什么 | `scratchpad` / `tool_results` |
| 给模型看过什么 | `messages` 或可重建 messages 的 state |
| 谁批准了什么 | `approvals` |
| 是否已产生副作用 | 工具结果、幂等键、外部事务 ID |

---

## 十、Resume：审批后继续执行

示例中恢复入口是：

```python
runtime.resume_with_approval(
    run_id=run.run_id,
    approved=True,
    approver="support_lead@example.com",
)
```

恢复逻辑：

```text
1. 检查 run.status 是否为 awaiting_approval
2. 取出 pending_tool_call
3. 记录 approval.resolved 事件
4. 清空 pending_tool_call
5. 用 approved=True 再次通过 ToolGateway 执行工具
6. 写 tool_result
7. checkpoint
8. 回到 run_until_blocked_or_done
```

这里有一个细节：审批通过后不是直接调用业务函数，而是重新经过 Tool Gateway。

原因：

| 原因 | 说明 |
|---|---|
| 统一审计 | 所有工具执行都走同一条路径 |
| 保留幂等 | 仍然需要 idempotency |
| 防止绕过策略 | resume 也是运行时动作，不是后门 |
| 便于扩展 | 后续可加入权限、限流、熔断、沙箱 |

---

## 十一、运行输出如何理解

运行后会先看到暂停状态：

```text
== Paused Run ==
status: awaiting_approval
pending_tool_call: {'tool_call_id': 'tool_002', 'name': 'issue_refund', ...}
```

说明 runtime 已经：

1. 创建了 run
2. 查询了订单
3. 发现需要退款
4. 因金额超过阈值暂停
5. 保存了待审批工具调用

随后恢复并完成：

```text
== Final Run ==
status: completed
final_output: Refund RF-1001 was issued for order ORD-1001 with amount 120.
```

事件日志会类似：

```text
01 step=0 type=run.created
02 step=0 type=run.started
03 step=0 type=model.action
04 step=1 type=tool.dispatch
05 step=1 type=tool.result
06 step=1 type=model.action
07 step=2 type=tool.dispatch
08 step=2 type=approval.requested
09 step=2 type=approval.resolved
10 step=2 type=tool.result
11 step=2 type=model.action
12 step=3 type=run.completed
```

这就是 trace 的雏形。

---

## 十二、从 Demo 到生产的扩展

### 12.1 替换真实模型

把 `MockRefundModel.next_action()` 替换成真实模型调用：

```text
Context Builder -> model API -> Model Adapter -> ModelAction
```

要注意：

- 工具参数必须结构化校验。
- 模型输出必须能区分 final/tool_call/handoff/ask_user。
- 模型异常和 provider error 要可重试。
- usage、latency、finish_reason 要写入 trace。

### 12.2 接入数据库

当前 demo 用内存字典：

```python
self.runs: dict[str, RunState] = {}
```

生产可拆成：

| 表/存储 | 内容 |
|---|---|
| `runs` | run 基本状态 |
| `run_steps` | 每一步状态 |
| `run_events` | append-only 事件 |
| `checkpoints` | 快照 JSON |
| `artifacts` | 大对象引用 |
| `approvals` | 审批记录 |

### 12.3 加入队列和 worker

长任务不要依赖单个 HTTP 请求：

```text
API create_run
  -> enqueue(run_id)
  -> worker picks run
  -> run_until_blocked_or_done
  -> if completed, notify UI
  -> if awaiting_approval, stop worker
  -> approval callback enqueue(run_id)
```

### 12.4 支持用户补充信息

当模型缺少订单号时，可以新增：

```text
ModelAction(kind="ask_user", question="Which order should I refund?")
```

Runtime 状态：

```text
status = awaiting_user_input
pending_action = {question, expected_schema}
```

用户回复后再 resume。

### 12.5 支持多 Agent Handoff

新增：

```text
ModelAction(kind="handoff", target_agent="fraud_review_agent", context=...)
```

Runtime 需要：

- 生成 handoff contract
- 创建子 run 或远程 task
- 保存 `awaiting_remote_agent`
- 等待结果
- 用 state patch 合并结果

### 12.6 支持 Memory

Runtime 可在两个位置处理记忆：

```text
Before model_call:
  retrieve memory -> context builder

After completed / important event:
  extract memory candidate -> verify -> write memory store
```

不要让模型自由写长期记忆。记忆写入应有证据、置信度、命名空间和过期策略。

### 12.7 支持 Streaming

事件日志可以直接变成前端流：

```text
run.created
model.delta
tool.call.started
tool.call.completed
approval.requested
run.completed
```

前端不需要知道内部 while loop，只消费 runtime event。

### 12.8 支持可靠重试

重试要分层：

| 层 | 策略 |
|---|---|
| 模型调用 | provider error 可重试，结构错误可修复重试 |
| 读工具 | 通常可安全重试 |
| 写工具 | 必须幂等或人工确认 |
| checkpoint | 写失败不能继续执行高风险动作 |
| handoff | 要能识别远程 task 是否已创建 |

---

## 十三、与主流框架的映射

| Demo 组件 | OpenAI Agents SDK | LangGraph | Google ADK | AutoGen |
|---|---|---|---|---|
| `AgentSpec` | `Agent` | graph/node 配置 | Agent | AssistantAgent 等 |
| `run_until_blocked_or_done` | `Runner.run` | compiled graph invoke/stream | Runner | team run |
| `RunState` | run/session state | graph state | session state | team/message state |
| `ToolGateway` | tool functions + guardrails | node/tool wrappers | tools | tools/functions |
| `pending_tool_call` | HITL 需要应用层处理 | interrupt/resume | event/session 模型可承载 | user proxy / intervention |
| `checkpoint` | sessions/应用存储 | checkpointer | sessions/state | save/load state |
| `events` | tracing/events | stream events | event model | messages/events |

不同框架的差异不是"有没有 Agent"，而是 Runtime 原语暴露到什么程度：

- OpenAI Agents SDK 更像轻量 runner + tool/handoff/tracing。
- LangGraph 明确把状态图、checkpoint、interrupt 当一等公民。
- Google ADK 强调 session、state、artifact、memory、event。
- AutoGen 强调多 Agent 消息流和 team state。
- CrewAI 强调角色、任务、流程。
- LlamaIndex 强调 workflow 与 RAG/agent 结合。

---

## 十四、真正掌握 Runtime 的判断标准

如果你能回答下面问题，说明已经理解 Agent Runtime 的核心：

1. 一个 run 暂停在审批状态时，最少需要保存哪些字段才能恢复？
2. 为什么有副作用工具不能只靠 retry？
3. `messages`、`scratchpad`、`artifact`、`memory` 分别保存什么？
4. Runtime 与 Agent Loop 的边界在哪里？
5. 人工审批为什么应是状态机状态，而不是业务代码里的阻塞等待？
6. 多 Agent handoff 时，为什么不能复制全量上下文？
7. 如何从 event log 构造 trace 和 eval dataset？
8. 如果 worker 在工具执行后、checkpoint 前崩溃，怎么避免重复副作用？

这些问题比"会不会调用某个框架 API"更接近生产级 Agent 的核心。
