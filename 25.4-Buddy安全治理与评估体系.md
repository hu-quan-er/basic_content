# Buddy 安全治理与评估体系

> 目标：把 Buddy 型产品从 demo 拉回生产现实。只要 Agent 能发消息、改文档、跑命令、改代码、调用企业系统，安全和评估就必须成为架构的一部分。

---

## 本文职责边界

| 本文负责 | 本文不展开 |
|---|---|
| Buddy 产品中的动作风险分层、权限边界、审计、HITL、产品级评估指标和上线治理 | 通用安全与对齐见 [09-安全与对齐.md](./09-安全与对齐.md)；Eval Harness 工程系统见 [24-Agent Eval Harness专题.md](./24-Agent%20Eval%20Harness专题.md)；Tool Gateway 风险控制见 [23-Tool Gateway与工具平台专题.md](./23-Tool%20Gateway与工具平台专题.md) |

---

## 一、核心原则

**Buddy 产品的风险不来自"回答错"，而来自"执行错"。**

普通问答错了，用户可以忽略；工作台智能体错了，可能已经发出消息、覆盖文件、泄露数据、提交代码或修改业务状态。

---

## 二、风险分层

| 风险等级 | 典型动作 | 处理策略 |
|---|---|---|
| 低 | 读取公开文档、解释代码、生成草稿 | 自动执行，记录 trace |
| 中 | 修改本地草稿、生成表格、运行测试、创建临时文件 | 自动执行或轻确认，保留回滚路径 |
| 高 | 发消息、分享文件、提交代码、调用写 API、部署预览 | 执行前 HITL，强审计 |
| 极高 | 金额、权限、生产部署、删除数据、外发敏感信息 | 默认禁止或多级审批 |

---

## 三、权限模型

不要让 Agent 继承用户的全部权限。推荐取交集：

```text
effective_permission =
  user permission
  ∩ agent permission
  ∩ session permission
  ∩ tool required scope
  ∩ policy constraints
```

---

## 四、WorkBuddy 类安全重点

| 风险 | 场景 | 防护 |
|---|---|---|
| 误发消息 | 给错误群或错误人发送总结 | 发送前展示收件人、正文、附件 |
| 文件泄露 | 把内部文档分享到外部 | 外部联系人识别、分享审批 |
| 表格误改 | 覆盖公式或数据源 | diff 预览、版本备份、只改指定 range |
| 越权查询 | 读取用户不该访问的客户数据 | 资源级权限过滤 |
| 业务误执行 | 创建工单、审批、退款、改 CRM 状态 | 高风险 API 默认 HITL |
| 提示注入 | 文档里诱导 Agent 外发数据 | 文档内容与系统指令分离，工具策略兜底 |

---

## 五、Qoder / CodeBuddy 类安全重点

| 风险 | 场景 | 防护 |
|---|---|---|
| 覆盖用户改动 | 工作区有未提交变更 | 修改前检查 git status，冲突时暂停 |
| 破坏性命令 | rm、git reset、force push | 命令 allowlist / denylist，审批 |
| 泄露源码 | 把私有代码发到外部服务 | 数据边界、模型路由策略、脱敏 |
| 引入漏洞 | 生成不安全代码 | 静态扫描、review、测试 |
| 误部署 | 自动发布到生产 | 环境分级，生产部署强审批 |
| 依赖投毒 | 安装不可信包 | registry policy、lockfile review |

---

## 六、Buddy 产品评估体系映射

本节只列 Buddy 产品需要关注的评估指标。通用评估方法见 [07-评估与可观测性.md](./07-评估与可观测性.md)，可运行的 Eval Harness、trace replay 和 CI gate 见 [24-Agent Eval Harness专题.md](./24-Agent%20Eval%20Harness专题.md)。

| 类型 | WorkBuddy 类指标 | Qoder / CodeBuddy 类指标 |
|---|---|---|
| 结果质量 | task success rate、side-effect error rate | patch correctness、test pass rate |
| 安全 | permission violation rate、approval rejection rate | command safety、diff scope control |
| 可恢复 | recovery rate、human takeover rate | failed test repair rate、rollback readiness |
| 成本 | time saved、tool latency | cost per task、token per accepted diff |
